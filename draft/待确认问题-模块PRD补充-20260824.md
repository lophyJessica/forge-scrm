# 待确认问题-模块PRD补充（2026-08-24）

> 状态：✅ 全部回收（2026-08-24）
> 来源：prd-docs/modules/ 各模块 §9 待确认事项汇总
> 说明：9 项全部有姐夫回复，见"回复记录"与"回复原文"。

---

姐夫，第二轮问题来啦，都是小问题，不用急，有空回我就行：

1. **标签分组**：资料标签可不可以按"客户行业、内容主题"这类维度分个组？（比如"制造业客户"放客户行业组）——也可以不做分组，先自由打标签，你觉得呢？

2. **采集频率**：系统自动采集数据（评论/私信/对标账号），你希望多久跑一次？每天自动跑、还是每周、还是你手动点一下才采集？

3. **选题去重**：批量生成选题的时候，前后两批如果出现"完全相同"的选题会自动去掉，这个没问题；如果只是"意思接近"的（比如《如何获客》和《获客方法大全》），要不要也自动合并？我建议先只做完全重复去重，意思接近的二期再说。

4. **脚本"已发布"标记**：一个脚本你用完之后，要不要手动把它标记成"已发布/已使用"？还是不用标记（那这块二期再做）？

5. **视频号数据**：系统自动采集视频号数据，需要账号是企业认证才能用官方接口。你现在的视频号是企业认证的吗？如果不是，就先用手动录入+表格导入的方式。

6. **数据分析字段**：分析视频号数据时，你想看哪些指标？播放量、点赞、评论、涨粉这些全都要，还是只要其中几个？（默认全采集，但想确认下你的重点）

7. **登录方式**：系统一期需要登录，默认建议先做账号密码。要不要支持别的方式（比如扫码、验证码）？

8. **选题版本**：选题本身修改后，要不要像脚本一样保留历史版本并支持回退？我建议一期不做独立选题版本表，二期再评估。

9. **成员数量上限**：一期需要限制可创建的成员数量吗？我建议一期不设业务上限，按系统容量控制。

---

## 回复记录

| # | 问题 | 模块 | 回复 | 状态 |
|---|---|---|---|---|
| 1 | 标签分组维度 | 01 资料库 | 先不做分组，自由打标签 | ✅ |
| 2 | 采集频率 | 04 数据分析 | 先做手动 | ✅ |
| 3 | 选题去重阈值 | 02 选题库 | 按建议（完全重复去重，语义二期） | ✅ |
| 4 | 脚本已发布标记 | 03 脚本库 | 增加状态"已使用" | ✅ |
| 5 | 视频号认证状态 | 04 数据分析 | 目前没有视频号，先手动录入+表格导入 | ✅ |
| 6 | 分析指标重点 | 04 数据分析 | 数据分析等后面再迭代 | ✅ |
| 7 | 登录方式 | 05 权限账号 | 一期账号密码，二期再说 | ✅ |
| 8 | 选题版本 | 02 选题库 | 按建议（一期不做独立版本表，二期评估） | ✅ |
| 9 | 成员数量上限 | 05 权限账号 | 先不做限制 | ✅ |

## 回复原文（姐夫原话，2026-08-24）

> 留存原始回复做追溯，摘要表与原文不一致时以原文为准。

1. **标签分组**：「先不做分组，自由打标签」
2. **采集频率**：「先做手动」
3. **选题去重**：「按建议的来」（完全重复去重，语义去重二期）
4. **脚本已发布标记**：「增加状态，已使用」
5. **视频号数据**：「目前还没有视频号，先手动录入+表格导入的方式」
6. **数据分析字段**：「数据分析等后面再迭代」
7. **登录方式**：「一期账号密码，二期再说」
8. **选题版本**：「按建议的来」（一期不做独立选题版本表，二期再评估）
9. **成员数量上限**：「先不做限制」

## 关键结论（回写 context/PRD 时依据）

- **标签**：自由打标签，不强制分组 → context/05 标签组字段降为可选
- **采集**：一期手动触发采集，不做定时 → 模块04 采集频率待确认项消除
- **脚本状态**：四态改为 待审核/已通过/**已使用**/已废弃（"已使用"成为正式状态，从二期移一期）→ context/04 状态机 + 模块03 同步
- **视频号**：一期无视频号账号 → 自动采集整体降级为"预留"，MVP 以手动录入+CSV 导入为主 → context/01 数据采集范围调整
- **数据分析**：一期只做基础录入+导入+分析框架，"指标重点/深度分析"二期迭代 → 模块04 分析维度降级
- **权限**：账号密码登录、成员数不设上限 → 模块05 待确认项消除
