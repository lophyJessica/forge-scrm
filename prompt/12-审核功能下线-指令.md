Context（背景）
你在 /Users/liulongfei/个人文件/forge-scrm 仓库工作（Forge 新媒体运营系统）。
动手前先 git pull origin main，最新 HEAD 为 209e7e9（布局修复2指令）。
用户决策（已拍板）：下线审核功能——入口隐藏 + 审核流程取消。
原因：只隐藏入口会导致"保存后进待审核状态、但没有审核页面可用"的死锁。
要求：创建/生成后直接生效，不再需要人工审核。数据库状态字段与历史数据保留（不删字段、不迁移）。

现状（已盘点，审核分布）：
- 前端菜单：MainLayout.tsx 64 行「资料审核」/materials/review、90 行「脚本审核」/scripts/review
- 前端路由：App.tsx 48 行 /materials/review（MaterialReview.tsx）、65 行 /scripts/review（ScriptReview.tsx）
- 前端页面审核交互：
  * materials/MaterialList.tsx + MaterialImport.tsx + MaterialForm.tsx：提交审核按钮/待审核状态展示
  * scripts/ScriptList.tsx + ScriptDetail.tsx：提交审核/审核按钮
  * analysis/AnalysisTasks.tsx + AnalysisTaskDetail.tsx：分析结果「确认/废弃」审核按钮
- 后端：
  * materials.py：创建 submit_for_review → 待审核；审核路由 review/通过
  * scripts.py：生成/创建 → 待审核；审核路由
  * analysis.py：470 行任务完成置「待审核」、500 行 review_task（结果必须人工审核 R6）、565 行回写前置校验、596 行回写产物置「待审核」
  * 状态枚举 enums.py：资料/脚本/分析各有「待审核」「已通过」等（保留，不动）

Request（任务）
取消审核流程，创建/生成后直接生效：

1. 前端（隐藏入口 + 去掉审核交互）：
   - MainLayout.tsx：删除「资料审核」「脚本审核」菜单项
   - App.tsx：删除 /materials/review、/scripts/review 路由（组件文件保留不删，仅不可达）
   - MaterialForm.tsx / MaterialImport.tsx / MaterialList.tsx：去掉「提交审核」相关按钮/提示（保存即生效；submit_for_review 不再使用）；列表「待审核」状态按新状态机展示（无待审核时该列自然消失，无需特殊处理）
   - ScriptDetail.tsx / ScriptList.tsx：去掉「提交审核/审核通过/驳回」按钮
   - AnalysisTasks.tsx / AnalysisTaskDetail.tsx：去掉「确认/废弃」审核按钮（任务完成即可用/可回写）
2. 后端（创建/生成直接生效）：
   - materials.py：创建接口忽略 submit_for_review，直接置「已生效」（MaterialStatus.已生效）；审核相关路由保留代码但前端不再调用（不改动历史数据）
   - scripts.py：脚本创建/生成后直接置「已通过」（不再进待审核）
   - analysis.py：470 行任务完成后直接置「已完成」可用（不再置待审核）；500 行 review_task 保留但不再被前端调用；565 行回写校验放行（已完成即可回写）；596 行回写产物直接置「已生效」（不再置待审核）
   - 所有「待审核」状态枚举/数据库字段保留不动（历史数据兼容）
3. 验证：
   - npm run build 通过
   - TestClient：创建资料 → 状态直接已生效；生成脚本 → 已通过；分析任务完成 → 已完成可回写
   - 页面不再出现审核入口/按钮

Output format
1) 新增/修改文件清单；2) 关键改动；3) build + 测试结果

红线
- 只改 frontend/src/ + backend/app/{routers,services}/；不动数据库模型/迁移/context/
- 状态枚举与数据库字段保留；历史数据不改
- 禁止 git commit/push；不写 key
- 审核路由/组件代码保留（只是不再暴露），不删除文件

Checkpoint
a) 菜单/路由/按钮全部隐藏；b) 创建/生成直接生效（无待审核死锁）；c) 历史数据兼容；
d) build+测试通过；e) 自检报告存 check-reports/ 并如实汇报上传状态
