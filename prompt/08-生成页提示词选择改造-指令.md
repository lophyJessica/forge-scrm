Context（背景）
你在 /Users/liulongfei/个人文件/forge-scrm 仓库工作（Forge 新媒体运营系统）。
动手前先 git pull origin main，当前最新 HEAD 为 207716d（二期路由 prefix 修复）。
任务：批量生成页的提示词模板选择体验改造（用户验收反馈：用户不知道内置模板是什么，需显式可选）。
现状（已查证）：
- 前端 topics/TopicGenerate.tsx 324 行 + scripts/ScriptGenerate.tsx 92 行：Select 下拉显示数据库模板（prompt_template_id），空时"暂无数据"，label 文案"提示词模板（可选，不选用内置模板）"
- 后端已有：GET /api/prompt-templates/builtin（返回 [{task_type: 选题生成|脚本生成, content: 内置提示词}]）
- 后端生成请求 schema：prompt_template_id 可选（不传用内置默认）；【无自定义提示词内容字段】
- 内置默认：topic_service.DEFAULT_SYSTEM_PROMPT / script_service.DEFAULT_SCRIPT_SYSTEM_PROMPT
- 生成接口：POST /api/topics/generate、POST /api/scripts/generate（build_prompt 组装 system prompt）

Request（任务）
把"提示词模板选择"改成【单选：内置模板 / 自定义】交互：

1. 前端（topics/TopicGenerate.tsx + scripts/ScriptGenerate.tsx）：
   - 把现有 Select 改成 Form.Item 内 Radio.Group：两个选项「内置模板」「自定义」
   - 选「内置模板」：显示 Select 下拉，选项来自 GET /api/prompt-templates/builtin（label 用 task_type + 内容前 30 字预览，value 用 task_type）；选择后提交时不传 prompt_template_id（走内置默认）；下拉提示"选择内置模板（默认即可）"
   - 选「自定义」：显示 TextArea（rows=6，支持长提示词），placeholder 如"输入自定义提示词，将替代内置模板和已配置模板生效"；提交时传新字段 prompt_content
   - Radio 切换联动显隐；表单默认值=内置模板（选中第一个内置）
   - 已有模板下拉（数据库模板）保留吗？——保留为独立入口不必要；本期用内置/自定义替代，数据库模板仍可通过 prompt_template_id 兼容（后端不变）；前端暂不展示模板库选择（内置+自定义已覆盖使用场景）

2. 后端：
   - schemas/topic.py + schemas/script.py 的生成请求模型加 prompt_content: str | None = None（描述：自定义提示词，优先于模板和内置）
   - topic_service.build_prompt + script_service.build_prompt：优先级 prompt_content > prompt_template_id > 内置默认；prompt_content 传入时作为 system prompt 使用
   - 快照/追溯：prompt_content 存在时，snapshot 记录 {prompt_type: "custom", content: prompt_content 摘要或全文}（对齐现有 snapshot 结构）

3. 验证：
   - npm run build 通过
   - TestClient：不传（内置）生成正常；传 prompt_content 生成正常且走自定义提示词
   - builtin 接口选项在前端正常加载

Output format
1) 新增/修改文件清单；2) 关键改动；3) build + 测试结果

红线
- 只改 frontend/src/pages/topics/TopicGenerate.tsx、scripts/ScriptGenerate.tsx + backend/app/schemas/{topic,script}.py + services/{topic,script}_service.py
- 不改 prompt_template_id 既有语义（兼容）
- context/ 只读；禁止 git commit/push；不写 key
- 内置默认内容不变（DEFAULT_*_SYSTEM_PROMPT 原样）

Checkpoint
a) Radio 内置/自定义联动正确；b) 内置下拉显示 builtin 选项；c) 自定义 TextArea 长提示词可提交；d) 后端 prompt_content 优先生效；e) build 通过；f) 自检报告存 check-reports/ 并如实汇报上传状态
