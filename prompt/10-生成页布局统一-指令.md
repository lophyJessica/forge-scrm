Context（背景）
你在 /Users/liulongfei/个人文件/forge-scrm 仓库工作（Forge 新媒体运营系统）。
动手前先 git pull origin main，最新 HEAD 为 ce7094f。
问题（用户实测确认，同一浏览器窗口下）：
- 「批量生成选题」页（frontend/src/pages/topics/TopicGenerate.tsx）表单【全宽】展示
- 「基于选题生成脚本」页（frontend/src/pages/scripts/ScriptGenerate.tsx）表单【半宽】展示
两页外层代码看似一致（Space width 100% + Card + Form maxWidth 720 + layout vertical），
但实际渲染宽度不同——说明存在隐藏差异（可能：外层多包了容器、Card/Form 有额外 width
或 style、路由布局容器不同、或某个字段/组件把表单撑宽/压窄）。
用户要求两页布局风格统一，不允许一个全宽一个半宽。

Request（任务）
1. 排查：对比两个页面的完整 JSX 外层结构（return 到 </Card>），找出实际宽度差异的根因
   （逐层检查：路由渲染容器 → Space/Card/Form 的 style 与 width → 内部字段的
   Form.Item 布局/栅格/width → 是否有占满/固定宽度的组件）
   同时检查路由（App.tsx）和布局（MainLayout.tsx）里这两个页面是否有不同的容器处理
2. 修复：统一两页布局宽度（建议以更合理的一页为准，另一页对齐；表单主体宽度一致，
   内容区不出现一页撑满一页留半屏）
3. 验证：npm run build 通过；本地 dev server 目测两页同窗口宽度一致（如能起服务）

Output format
1) 根因说明（具体到哪个元素/哪行）；2) 修改文件与关键改动；3) build 结果

红线
- 只改 frontend/src/（App.tsx / MainLayout.tsx / 两个生成页及其依赖）
- 不动后端、不动 context/
- 禁止 git commit/push；不写 key
- 最小改动：只修布局宽度差异，不改动表单字段、业务逻辑、模板库/自定义交互

Checkpoint
a) 根因明确；b) 两页同窗口宽度一致；c) build 通过；d) 自检报告存 check-reports/ 并如实汇报上传状态
