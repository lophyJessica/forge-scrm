Context（背景）
你在 /Users/liulongfei/个人文件/forge-scrm 仓库工作（Forge 新媒体运营系统）。
动手前先 git pull origin main。
本步：二期 P1 数据报告模块开发（文档套件已完成：prd-docs/modules/数据报告/ 5 文档）。
功能范围（P1 In，来自 PRD）：
- 每周运营数据报告：汇总数据分析结果 + 业务运营数据 + 可用采集结果
- 市场分析周报：汇总研究助手报告 + 自动采集结果 + 外部检索结论
- 统一报告实体 report_type 区分两种（运营数据报告/市场分析周报）
- 飞书/微信推送：API/授权/频率待实测——本期做推送任务骨架，实际发送标 TODO
- 定时生成：预留（本期手动触发生成，周期配置待确认）
必读：prd-docs/modules/数据报告/数据报告主PRD.md + 数据报告字段清单.md + 数据报告_业务规则规格.md
      （生成字段/状态机/规则以 PRD 为准，字段以字段清单为准，禁止新造 context 未定义字段）
参考格式：二期已有 backend/app/{models,schemas,routers,services}/phase2.py（采集/研究同款模式）、
      frontend/src/pages/research/（研究助手页面同款模式）
后端路由 prefix 统一 /api（一期风格，禁 /api/v1——历史 404 教训）。

Request（任务）
1. 后端：
   - 模型（models/report.py 或并入二期文件）：report 表
     （report_type=运营数据报告|市场分析周报；title/summary/content/sections(JSON)/conclusions(JSON)/
      status=待生成→生成中→已完成/失败；retry_count；data_source_summary(JSON，来源清单含类型与id)/
      is_ai_product=true（对齐 R8，报告属 AI 产物）；created_by/created_at/updated_at；generated_at；
      关联 source 字段按字段清单）
   - Alembic 迁移（幂等）
   - Schema + CRUD 路由（分页/状态筛选/详情）+ 生成/重试接口
   - 生成服务（services/report_executor.py）：
     * 运营数据报告：聚合分析任务结果（analysis-tasks）+ 已生效采集结果（collection_result）+ 业务数据
     * 市场分析周报：聚合研究助手报告（research_report）+ 采集结果 + 外部检索结论
     * 数据源为空时明确失败/标注（不伪造数据）；来源先于结论；快照来源清单
     * 同步执行（沿用二期风格）；失败可重试（retry_count+1，保留历史）
   - 推送骨架：push_task 表（report_id/target_channel=飞书|微信(待实测)/target_object/summary/status=
     待推送→推送中→已推送/失败；错误码）；推送执行接口返回 501 NotImplemented（渠道 API 待实测，
     TODO 注释），生成报告后可手动创建推送任务但发送暂不可用
2. 前端（pages/reports/）：
   - ReportList.tsx：报告列表（report_type/状态筛选+分页）+ 新建（选类型）+ 生成/重试（failed 显示重试）
   - ReportDetail.tsx：报告详情（title/summary/content/sections/conclusions+来源清单+AI 生成标记）
   - 路由 /reports/* + 菜单（新一级「数据报告」或并入「数据分析」，与用户确认后定；默认建议并入数据分析下二级）
   - 状态 Tag/操作栏对齐全局 UI 规范（紫色主题已全局生效）
3. 验证：alembic upgrade head 幂等；TestClient：生成运营报告 200（有数据源）、空源失败、
   重试 409/计数、推送 501；npm run build 通过

Output format
1) 新增/修改文件清单；2) 关键改动；3) 迁移+测试+build 结果

红线
- 只改 backend/app/{models,schemas,routers,services}/ + backend/alembic/versions/ + frontend/src/
- context/ 只读；禁止 git commit/push；不写 key
- 推送渠道不实现真实发送（API 待实测）；不伪造数据；字段对齐字段清单

Checkpoint
a) report 表+迁移幂等；b) 两种报告生成聚合正确；c) 空源明确失败；d) 推送骨架 501+状态机；
e) 前端列表/详情/生成/重试；f) build+测试通过；g) 自检报告存 check-reports/ 并如实汇报上传状态
