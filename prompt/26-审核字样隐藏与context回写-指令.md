cd ~/个人文件/forge-scrm && git pull origin main

```plaintext
Context:
项目 forge-scrm（Forge 新媒体运营系统），Mac ~/个人文件/forge-scrm，main 分支，HEAD=78d4365。两个小修正，来自用户对 PRD 对齐修复轮的验收反馈。用户已授权修改 context/04-业务规则与状态机.md（审核口径回写，0826 拍板 + 0828 重申）。

背景口径（必须遵守）：二期审核默认通过——审核入口暂时隐藏不使用，逐条审核影响操盘手自用效率。覆盖 R3/R6/R8 及资料新建路径。审核能力后端保留、前端只做字样/入口层面的隐藏。依据 prd-docs/modules/审核流程变更说明.md。

Request:
1. MaterialForm.tsx 按钮字样与提交语义调整（保留口子，前端隐藏审核字样）：
   - "保存草稿"按钮文案改为"保存为草稿"→ 不对，直接改为【保存草稿】保留？——不。最终定稿：新建/编辑页按钮收敛为两个：【保存】（默认主按钮，行为=直接生效，即现 submit('save')/新建直接已生效路径）与【存为草稿】（次要按钮，行为=submit('draft')，保留草稿口子）。
   - 删除"提交审核"按钮及 submit('review') 分支的 UI 触发（isEdit 分支中 action==='review' 调 /submit 的代码路径保留在代码里但不再有入口按钮触发；新建分支 submit_for_review 恒为 false）。
   - message 文案：草稿→"已存为草稿"；保存→"保存成功，资料已生效"。
   - 菜单 MainLayout.tsx 中"资料审核"（/materials/review）入口注释隐藏（保留路由与页面代码，仅菜单不显示；MaterialReview.tsx 页面文件不动）。
   - 全站 grep"提交审核""资料审核""待审核"字样：仅调整用户可见文案（按钮/菜单/空态提示），涉及状态枚举值、接口参数、状态机判断的代码逻辑一律不动。
2. context/04-业务规则与状态机.md 审核口径回写（用户已授权，唯一允许的 context 改动）：
   - R1：`资料必须审核后才能使用` → `资料使用规则：人工新建直接生效（二期默认）；导入与 AI 产物保留审核队列与 AI 产物标记，审核入口暂时隐藏，能力可重新启用（0826 拍板，依据审核流程变更说明）`
   - R3：`选题必须人工筛选` → `选题生成后默认可用；人工筛选变为主动抽查/审核重启时使用，入口保留（0826 拍板）`
   - R6：`分析结果必须人工审核后使用` → `分析结果默认按已确认处理；人工审核变为抽查/审核重启时使用，入口保留（0826 拍板）`
   - R8：保持"AI 产物与原始事实区分"不变，补充一句"AI 产物二期默认可用，保留标识、抽查与撤回能力（0826 拍板）"。
   - 状态机图（资料/选题/分析三处 mermaid）：在"草稿→待审核""待筛选"等阻塞节点上补注释"二期默认通过，跳过阻塞"（不删原有流转线，只加标注）。
   - 每处改动注明"（0826 拍板，依据审核流程变更说明.md，2026-08-28 回写）"。
   - context/ 其余文件、其余规则段落禁改。
3. Output/验证：python -m compileall -q backend/app（应无后端改动，跑一遍确认）；npm run build 通过。
4. 红线禁止：
   - 禁止改后端任何文件（materials.py / schemas 本轮零改动）。
   - 禁止改状态枚举值、接口参数、状态机判断逻辑（只动前端字样/菜单/context 文档）。
   - 禁止改 context/04 以外的任何 context 文件；context/04 仅允许 R1/R3/R6/R8 与状态机标注。
   - 禁止恢复"提交审核"入口按钮（口子保留在代码，UI 隐藏）。
   - 禁止 commit/push/deploy；禁止伪造构建结果。

Checkpoint:
完成后必须执行 AI 自检报告管道上传（沙箱 DNS 不解析 pmlophy.com 时按兜底规则：保存 check-reports/ + 如实汇报"未上传管道"，禁止重试超过1次）：
curl --noproxy '*' -X POST "https://pmlophy.com/p/jarvis/file/upload" -H "X-Jarvis-User: ai-reports" -F "file=@check-reports/审核字样隐藏与context回写-自检报告-20260828.md"
最后汇报：逐项完成状态、context/04 四条规则回写后的原文、改动文件列表、build 结果、报告文件名与字节数、是否已上传管道。
```
