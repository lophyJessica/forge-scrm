Context（背景）
你在 /Users/liulongfei/个人文件/forge-scrm 仓库工作（Forge 新媒体运营系统），前端在 frontend/。
动手前先 git pull origin main。
问题（用户验收反馈）：部分详情页没有「返回」按钮——用户从列表进入详情
（涉及审核/快照操作的页面：脚本详情/选题详情/研究报告等），操作完后只能浏览器后退，体验差。
已确认无返回的页面：scripts/ScriptDetail.tsx、topics/TopicDetail.tsx、research/ResearchReport.tsx。
有返回的参照（不动）：analysis/AnalysisTaskDetail.tsx、reports/ReportDetail.tsx、
scripts/ScriptVersions.tsx（Card title 区 extra="返回脚本"按钮）。

Request（任务）
全量修复详情类页面的返回入口：

1. 给无返回的详情页加「返回」按钮：
   - 统一放页面标题区右侧（Card title extra 或页面标题旁），antd Button + ArrowLeftOutlined，
     文案「返回列表」或对应页名（如「返回脚本列表」「返回选题列表」「返回研究任务」）
   - 点击返回对应列表页：优先 navigate 到明确路径（/scripts、/topics、/research/tasks），
     不依赖浏览器 history（避免从分享链接进入时后退失效）
2. 全量自查（关键）：扫描 frontend/src/pages/ 下所有详情/历史/快照/审核类页面
   （含路由带 :id 的页面：脚本详情、脚本版本历史、选题详情、资料详情如有、分析任务详情、
   研究报告详情、数据报告详情、采集任务展开详情等），凡进入后无返回入口的一律补上；
   自查结论在报告中列明
3. 有返回的页面保持不动；风格与紫色主题（#7c3aed）一致
4. npm run build 通过

Output format
1) 修复页面清单（含返回按钮位置/目标路径）；2) 全量自查结论（是否还有其他漏网）；3) build 结果

红线
- 只改 frontend/src/pages/ 下页面 JSX；不动业务逻辑/API/路由定义/后端
- 不新增依赖；不 commit/push；不写 key

Checkpoint
a) 已确认 3 个无返回页面补齐；b) 全量自查无漏网；c) build 通过；
d) 自检报告存 check-reports/2026-08-26-详情页返回按钮修复自检报告.md 并如实汇报上传状态
