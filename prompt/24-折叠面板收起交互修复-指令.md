cd ~/个人文件/forge-scrm && git pull origin main

```plaintext
Context:
项目 forge-scrm（Forge 新媒体运营系统），Mac ~/个人文件/forge-scrm，main 分支。飞书卡片 2.0 折叠面板已上线，用户实测反馈两个问题：
①折叠面板展开后没有"再收起"功能——collapsible_panel 的 expanded=false 只定了初始收起，但用户展开后无法收回去（面板头没有收起交互）。
②附带问题（用户已报告过）：ReportDetail.tsx 创建推送 Modal 被推送任务 Drawer 遮挡（zIndex 冲突），助手已在仓库直接修复并 push（commit d3f6464，Modal zIndex=1100）——本次 pull 后确认该行存在即可，不要重复修改。

Request:
1. 折叠面板"展开↔收起"双向交互（backend/app/services/feishu_push.py 卡片 2.0 构造处）：
   - 查证飞书卡片 2.0 collapsible_panel 的收起交互机制（用 ego/浏览器查官方文档《折叠面板》页 https://open.feishu.cn/document/feishu-cards/card-json-v2-components/containers/collapsible-panel）：
     a) 该组件 header 是否自带"展开/收起"切换（官方默认交互），是否需要配置 header 展开态样式（header 为对象时可能分 expanded_header / collapsed_header 两个字段——以官方文档为准）；
     b) 若 header 只配了 collapsed 态导致展开后无收起入口，则补全 expanded 态的 header 配置。
   - 白名单纪律：只用官方文档确认存在的字段，查证不到的字段不许写。
2. Modal 遮挡确认：git pull 后 grep ReportDetail.tsx 的 zIndex={1100} 存在则跳过；不存在则补上（与 commit d3f6464 一致）。
3. 禁止扩界：不改卡片其他 elements、不改 28KB 截断逻辑、不改降级逻辑、不动其他模块。

Output format:
- 最小改动。python -m compileall -q backend/app 通过 + npm run build 通过（如动了前端）。
- 自检报告 check-reports/折叠面板收起交互修复-自检报告-20260828.md：含官方文档查证结论（字段依据）+改动点+自检结果。

红线禁止:
- 禁止不执行 git pull 就改文件。
- 禁止修改 context/、prd-docs/、requirements/。
- 禁止 commit/push/deploy。
- 禁止臆造飞书卡片字段。
- 禁止伪造构建结果。

Checkpoint:
完成后必须执行 AI 自检报告管道上传（沙箱 DNS 不解析 pmlophy.com 时按兜底规则：保存 check-reports/ + 如实汇报"未上传管道"，禁止重试超过1次）：
curl --noproxy '*' -X POST "https://pmlophy.com/p/jarvis/file/upload" -H "X-Jarvis-User: ai-reports" -F "file=@check-reports/折叠面板收起交互修复-自检报告-20260828.md"
最后汇报：官方文档查证结论、改动文件、build/compileall 结果、报告文件名与字节数、是否已上传管道。
```
