cd ~/个人文件/forge-scrm && git pull origin main

```plaintext
Context:
项目 forge-scrm（Forge 新媒体运营系统），Mac ~/个人文件/forge-scrm，main 分支。两个子任务：①新增报告删除功能（用户拍板 B，修订 PRD 口径）；②飞书卡片正文真折叠（当前是 500 字静态截断降级，用户反馈"展开不了"）。

任务一背景：PRD 原口径「删除报告/推送记录 ❌ 默认不提供」（prd-docs/modules/数据报告/数据报告主PRD.md:331），用户 0828 拍板修订为提供。注意：本指令不改 prd-docs/（受保护），PRD 回写由用户后续手动做——报告中注明此遗留。
任务二背景：backend/app/services/feishu_push.py 当前 _report_card 用卡片 JSON 旧版结构（config.wide_screen_mode），正文 [:500] 静态截断 + COLLAPSED_PREVIEW_NOTICE。用户要真折叠（点击展开/收起）。

Request:
【一、报告删除】
1. 后端 DELETE /api/reports/{report_id}（reports.py）：
   - 权限 require_permission(Permission.报告推送)（对齐推送链路口径）。
   - 拦截规则：报告存在推送任务记录（report_push_task.report_id 引用）→ BizError("该报告已有推送记录，为保持对账完整不可删除")；否则删除报告及关联记录（按 Report 模型现有关系处理）。
   - 生成中状态不可删除（BizError）。
2. 前端：
   - ReportList.tsx 操作列：行状态非"生成中"时加"删除"按钮（type="link" size="small" danger）+ Modal.confirm「确认删除该报告？删除后不可恢复」（已有推送记录的会在后端被拦，前端无需预判）。
   - ReportDetail.tsx 页头操作区：同规则加"删除"（danger 链接按钮 + Modal.confirm），删除成功后 navigate 回列表。
   - 按钮层级遵守 UI-DESIGN.md（操作列 link+small、danger 仅不可逆）。

【二、卡片真折叠】
1. 先查证（不许臆造）：读项目 site-packages lark_oapi 版本 + 检索官方卡片 JSON 2.0 的 collapsible_panel 组件结构（若环境无文档可访问，按已知飞书卡片 2.0 规范实现：顶层 "schema": "2.0"，body.elements 内使用 tag="collapsible_panel" 组件，含 header（title+背景色）、border、vertical_spacing、actions 等字段——实现时保守用最简字段集）。
2. feishu_push.py _report_card 升级为双版本尝试：
   - 优先构造 2.0 卡片：header 紫色标题 + elements：时间/摘要/概览 div + collapsible_panel（默认收起，header 行"报告正文（点击展开）"，内含完整 lark_md 正文，28KB 逻辑照旧作用于正文）。
   - 发送后检测飞书返回 code：若 400/参数错误（如 99991672 组件不支持），自动降级为现有旧版 500 字平铺卡片重发一次，并在 record 摘要注明"卡片2.0降级"。
   - 降级重试逻辑复用现有 force_refresh 重试框架（第二次尝试不与 token 重试冲突）。
3. 白名单字段纪律：collapsible_panel 只用官方文档确定存在的字段，不自造。

Output format:
- 最小改动。python -m compileall -q backend/app 通过 + npm run build 通过。
- 自检报告 check-reports/报告删除与卡片真折叠-自检报告-20260828.md：含①卡片2.0查证结论（lark_oapi版本+collapsible_panel 可用性依据）②逐项改动③测试结果④遗留风险（含"PRD 主PRD.md:331 口径待用户手动回写"）。

红线禁止:
- 禁止不执行 git pull 就改文件。
- 禁止修改 context/、prd-docs/、requirements/。
- 禁止 commit/push/deploy。
- 禁止臆造飞书组件字段：collapsible_panel 结构必须有出处依据，查证不到就走保守降级。
- 禁止伪造构建结果。

Checkpoint:
完成后必须执行 AI 自检报告管道上传（沙箱 DNS 不解析 pmlophy.com 时按兜底规则：保存 check-reports/ + 如实汇报"未上传管道"，禁止重试超过1次）：
curl --noproxy '*' -X POST "https://pmlophy.com/p/jarvis/file/upload" -H "X-Jarvis-User: ai-reports" -F "file=@check-reports/报告删除与卡片真折叠-自检报告-20260828.md"
最后汇报：逐项完成状态、卡片2.0查证结论、改动文件列表、build/compileall 结果、报告文件名与字节数、是否已上传管道。
```
