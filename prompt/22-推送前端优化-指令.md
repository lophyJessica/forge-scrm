cd ~/个人文件/forge-scrm && git pull origin main

```plaintext
Context:
项目 forge-scrm（Forge 新媒体运营系统），仓库位于 Mac ~/个人文件/forge-scrm，工作分支 main。飞书推送已上线实测通过，本任务一次性清理推送模块与模板模块的全部前端遗留问题。需求 PRD：prd-docs/modules/推送模块前端优化PRD.md（先读）。
现状要点：
- 推送后端已可用（reports.py 已接 feishu_push），但前端 ReportDetail.tsx 仍是骨架期状态。
- 新增推送配置查询后端接口一处（见 Request 2）。
- 模板表单里有一段面向开发者的实现说明文案暴露给用户（admin/PromptTemplates.tsx 约176行）。

Request:
1. 推送交互重构（frontend/src/pages/reports/ReportDetail.tsx）：
   a) 头部操作区（"返回列表/创建推送"所在行）新增「推送任务」按钮，Badge 显示任务数（含待推送/失败计数），点击打开右侧抽屉。
   b) 推送任务列表从页尾 Card 改为 antd Drawer（宽 480px，title="推送任务"）：任务列表、发送按钮、状态标签、失败原因全部在抽屉内；页尾原 Card 整体移除。
   c) 执行反馈：点「发送」成功 → message.success('推送成功，接收人已在飞书收到卡片')；失败 → message.error(records[0].error_message 或通用失败文案)；完成后刷新任务列表（现有逻辑保留改造）。
   d) 清理过时文案（共3处）：49行 success 提示改「推送任务已创建，可点发送推给接收人」；138行空态改「暂无推送任务。已完成报告可创建飞书推送，创建后点发送即推送给接收人。」；163行 Alert 改「推送将通过飞书应用发送给系统配置的接收人。」
2. 目标人字段诚实化：
   a) 后端新增 GET /api/reports/push-config（reports.py，权限同现有推送接口）：返回 { channel: "feishu", receivers_count: <feishu_push_open_ids 逗号分隔个数>, configured: <bool 三变量是否齐> }——不返回任何 open_id 明文。
   b) 前端创建推送弹窗：「目标人或群」输入框改为只读展示——加载 push-config，configured=true 显示「系统接收人 ×N（飞书）」；configured=false 显示「未配置，请联系管理员」并禁用创建按钮（提示文案走现有 BizError 口径）。原 placeholder「不保存凭据，仅记录用户指定的目标标识」删除。
3. 模板表单文案清理（frontend/src/pages/admin/PromptTemplates.tsx 约176行 + analysis/PromptTemplates.tsx 同款若有）：
   - 删除「当前后端模板模型未提供可持久化的描述字段；版本号会在正文变更时由后端自动递增。」这段面向开发者的说明——用户不需要知道实现细节；帮助文案保留「勾选后，每次使用该模板生成时会自动附带这些资料的最新内容；资料更新无需修改模板」即可。
4. 禁止扩界：不动推送后端逻辑（仅新增 push-config 只读接口）；不改选题/脚本/资料库其他页面；不动 context/、prd-docs/；不改数据库结构。

Output format:
- 最小改动。完成后前端 npm run build 必须成功 + 后端 python -m compileall -q backend/app 通过。
- 生成自检报告 check-reports/推送前端优化-自检报告-20260828.md（任务/改动文件/每文件改动点/自检结果/遗留风险）。

红线禁止:
- 禁止不执行 git pull 就改文件。
- 禁止修改 context/、prd-docs/、requirements/。
- 禁止 commit/push/deploy（交付代码与报告即可）。
- 禁止把 open_id/secret 明文返回给前端或写进日志。
- 禁止伪造构建结果：build 失败必须修复后再交付。

Checkpoint:
完成后必须执行 AI 自检报告管道上传（沙箱 DNS 不解析 pmlophy.com 时按兜底规则：保存 check-reports/ + 如实汇报"未上传管道"，禁止重试超过1次）：
curl --noproxy '*' -X POST "https://pmlophy.com/p/jarvis/file/upload" -H "X-Jarvis-User: ai-reports" -F "file=@check-reports/推送前端优化-自检报告-20260828.md"
最后汇报：改动文件列表、build/compileall 结果、报告文件名与字节数、是否已上传管道。
```
