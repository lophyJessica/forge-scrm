# 全局列表默认排序落地 — 指令

> 日期：2026-08-30
> 依据：draft/待办-全局列表默认排序规则.md（已确认待办）+ 复审报告 DRAFT-1（未做）
> 范围：后端 order_by 机械修正 + UI-DESIGN.md 补一行。适合 Cursor 小活。

## 指令正文（复制给 Cursor，独立会话）

```plaintext
cd forge-scrm 仓库根目录（Mac 本地实际路径），不新建目录。开工前先 git pull origin main。

Context:
全局列表默认排序规则已由用户定稿（draft/待办-全局列表默认排序规则.md）：所有列表页默认"创建/生成时间 desc → 修改时间 desc"依次排序。复审报告（audit-reviews/PRD与前端对齐复审-20260830.md DRAFT-1）确认后端多数列表仍用 id.desc()，UI-DESIGN.md 第 7 节未补规范。注意：本指令与 27/28 号 Codex 指令可能并行执行，改完立即自查，只改列出的内容。

Request:

1. 后端逐个列表接口核对 order_by，凡不符合规则的统一改为显式时间字段排序：
   - 通用：created_at.desc, id.desc（id 作稳定兜底，避免时钟回拨/批量插入乱序）
   - 报告列表用 generated_at（生成时间）desc
   - 涉及文件（复审报告 DRAFT-1 实测证据，以实际代码为准）：backend/app/routers/ 下 phase2.py、materials.py、analysis.py、reports.py、prompts.py、scripts.py、users.py、topics.py 的列表路由
   - 只改排序，不改任何其他逻辑；无 created_at 字段的表停下来汇报，不得自造字段

2. UI-DESIGN.md 第 7 节「页面骨架/列表页」补一行：列表默认排序=创建/生成时间倒序（回退修改时间倒序），后端 order_by 落实。

3. 前端表格无需加 sorter（后端已排）；如发现前端有本地排序逻辑与规则冲突，一并修正并记录。

红线禁止:
- 禁止改列表排序以外的任何逻辑
- 前端不得新增任何"审核/抽查"字样或入口
- 禁止 commit/push/部署
- 遵守 AGENTS.md 工作流铁律

Checkpoint:
完成后生成自检报告 check-reports/全局列表排序-自检报告-20260830.md（含改动文件清单、每处 order_by 前后对比、遗留风险），沙箱网络受限则留本地如实说明。
```
