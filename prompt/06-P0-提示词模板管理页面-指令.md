Context（背景）
你在 /Users/liulongfei/个人文件/forge-scrm 仓库工作（Forge 新媒体运营系统）。
动手前先 git pull origin main，最新 HEAD 为 00bcbb2（开发第二步指令）。
这是 P0 任务：提示词模板管理页面（一期欠账——后端模板 CRUD API 已存在，前端缺管理界面，内置模板不可见）。
前端为 React + antd（一期风格）。context/ 只读。

现状（已查证，不必重新排查）：
- 后端 backend/app/routers/prompts.py 已有完整 CRUD：GET/POST /api/v1/prompt-templates、PUT/DELETE /{template_id}（修改时 version+1）
- PromptTemplate 模型字段：name/task_type/content/version/description 等（以 models/prompt.py 为准）
- 生成页已接模板下拉：topics/TopicGenerate.tsx 324 行、scripts/ScriptGenerate.tsx 92 行（"提示词模板（可选，不选用内置模板）"）
- 内置默认提示词：backend/app/services/topic_service.py 的 DEFAULT_SYSTEM_PROMPT（代码常量，用户不可见）
- 前端 admin 目录只有 Users.tsx，无模板管理页

Request（任务）
P0：提示词模板管理页面 + 内置模板展示

1. 后端：新增只读接口 GET /api/v1/prompt-templates/builtin
   - 返回内置默认提示词（选题 DEFAULT_SYSTEM_PROMPT + 脚本侧如有内置也返回）
   - 结构：{ task_type: '选题生成'|'脚本生成', content: '...' } 列表
   - 只读展示用途，不落库、不可编辑

2. 前端：frontend/src/pages/admin/PromptTemplates.tsx
   - 模板列表：表格展示 name/task_type/version/content(截断)/description/更新时间；操作列：编辑/删除
   - 新建/编辑弹窗：name/task_type(下拉：选题生成/脚本生成)/description/content(textarea)
   - 删除：二次确认（Popconfirm）
   - 内置模板区：页面顶部卡片展示 builtin 接口返回的内置提示词（只读，标注"内置默认，仅展示不可编辑"），按 task_type 分开展示或 tab
   - 全部沿用一期 antd 风格（参考 admin/Users.tsx 的写法）
   - 路由注册：admin 菜单/路由加"提示词模板"入口

3. 生成页联动（可选小改）：TopicGenerate/ScriptGenerate 的下拉 label 加"内置"选项不可选？不做——下拉保持现状即可（不选=内置已明确）

4. 构建验证：npm run build 通过

Output format
1) 新增/修改文件清单；2) 关键改动；3) build 结果

红线
- 只改 backend/app/routers/prompts.py（加 builtin 接口）+ frontend/src/pages/admin/ + 路由/菜单
- 禁止修改 PromptTemplate 模型和既有模板 CRUD 逻辑
- 禁止 git commit/push；改完 git status 确认
- 不写任何 key/token

Checkpoint
a) builtin 接口返回内置提示词；b) 模板列表/新建/编辑(version+1)/删除可用；c) 内置模板只读展示；d) build 通过；e) 自检报告存 check-reports/ 并如实汇报上传状态
