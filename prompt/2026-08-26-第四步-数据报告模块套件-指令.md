Context（背景）
你在 /Users/liulongfei/个人文件/forge-scrm 仓库工作（Forge 新媒体运营系统）。
动手前先 git pull origin main，当前最新 HEAD 为 ffc26ab（含第三步研究助手模块套件）。
文档权威层级：context/ > templates/ > prd-docs/ > draft/。context/ 只读，禁止修改。
本步为二期四步中的第四步（最后一步）：生成"数据报告模块"PRD 套件（只做文档，不做代码、不做原型）。

已确认的权威输入（必须先读）：
1. requirements/09-二期调研结论.md（2026-08-26 用户拍板：P1=周报+市场分析+飞书/微信推送）
2. prd-docs/modules/00-二期总PRD.md（二期总览，重点看 P1 定义、1.6 设计原则、5.3 审核与抽查状态机）
3. prd-docs/modules/审核流程变更说明.md（默认通过 + 轻量抽查）
4. templates/prd-template.md（模块 PRD 模板，9 节结构）
5. context/05-术语与字段口径.md（字段 SSOT）
6. prd-docs/modules/自动采集/ 与 prd-docs/modules/研究助手/（前两步套件，格式对齐范例 + 数据来源关系）

格式参考（完全对齐）：
- prd-docs/modules/研究助手/研究助手主PRD.md
- prd-docs/modules/研究助手/研究助手字段清单.md
- prd-docs/modules/研究助手/研究助手_业务规则规格.md
- prd-docs/modules/研究助手/研究助手_用例数据推演.md
- prd-docs/modules/研究助手/研究助手_业务流程推演.md

Request（任务）
新建目录 prd-docs/modules/数据报告/，生成以下 5 份文档：

1. 数据报告主PRD.md
按 templates/prd-template.md 的 9 节结构：模块概述/功能清单/交互流程/关键规则/状态机/数据字段/权限/验收标准/待确认事项。
内容边界以 00-二期总PRD.md 为准：P1 三件套 = ①每周运营数据报告 ②市场分析周报 ③分析报告/生成结果推送飞书/微信给指定人或群。数据来源包括：数据分析结果、自动采集结果、研究助手报告、运营业务数据。实体预期为：报告模板/报告实例（运营数据报告与市场分析周报可用统一报告实体+报告类型区分，或拆两类，以不越界为原则给出建议并标注待确认）+ 推送任务/推送记录。推送渠道（飞书/微信）的 API 能力、授权方式、频率限制、消息格式均标注待实测/待确认。功能清单给出功能名称、优先级（P0/P1）、In/Out、关键规则列。状态机：报告生成沿用"待生成→生成中→已完成/失败（可重试）"，审核沿用总 PRD 5.3 默认通过→抽查中→已确认/待审核/已废弃；推送任务"待推送→推送中→已推送/失败（可重试）"。用 mermaid stateDiagram-v2 表达。

2. 数据报告字段清单.md
对齐研究助手字段清单格式。覆盖实体：报告（含运营数据报告/市场分析周报的类型区分）、报告模板（如拆出）、推送任务、推送记录（含订阅配置如目标人或群）。每个表给出：表名、字段名、类型、必填、默认值、说明。规则：
- 复用 context/05 已有字段口径（is_ai_product、来源链接、创建追踪、可信度等）时直接引用，不重造。
- 报告本身属于 AI 产物：is_ai_product=true（除非报告纯为运营数据聚合，是否例外标注待确认）。
- 新增字段全部标注"新增建议，待确认"。
- 推送记录必须保留：目标渠道、目标对象、消息摘要、发送时间、状态、错误码（失败时）。
- 末尾附表关系总览（mermaid erDiagram）。

3. 数据报告_业务规则规格.md
细化规则：报告生成周期（周报按周生成，周期配置为新增建议待确认）；报告数据来源与口径（运营数据取自数据分析/业务数据，市场分析取自研究助手/采集/外部检索，来源先于结论）；AI 产物标记（报告 is_ai_product=true，沿用 R8）；审核默认通过 + 轻量抽查（对齐审核流程变更说明与总 PRD 5.3，抽查比例/触发时机待确认）；推送合规（目标对象授权、渠道频率限制、失败重试、不泄露敏感数据）；推送记录可追溯；报告沉淀资料库遵循目标模块规则。每条规则编号（RPT-R1、RPT-R2...），并与 context/04 已有规则编号建立引用关系。

4. 数据报告_用例数据推演.md（强制关卡）
对齐研究助手用例数据推演格式，至少覆盖 5 个用例：周报生成全流程（数据聚合→报告生成→默认通过→查看）、报告生成失败重试、推送飞书成功与失败重试、推送微信成功与失败重试、报告沉淀资料库/被研究助手引用（AI 产物标记与追溯）。每个用例包含：场景、前置条件、操作步骤、预期结果、关键数据示例（具体到字段值）。推演不通过不能进入后续设计，这是铁律。

5. 数据报告_业务流程推演.md
对齐研究助手业务流程推演格式：主流程步骤推演（配置报告→触发生成→数据聚合→报告生成→审核/抽查→推送→查看与沉淀）、异常分支（数据源不可用、生成失败、推送失败、渠道限流）、页面与状态对应关系。

Output format（输出要求）
- 5 份文档全部中文，结构对齐研究助手套件对应文件，markdown 格式。
- 新增实体、字段、状态、配置一律标注"新增建议，待确认"，禁止冒充 context/05 已确认口径。
- 引用一期规则（R1-R20）时沿用原编号，新增规则用 RPT- 前缀（不要用 AC-/RS-，那是前两步的前缀）。
- 完成后生成自检报告 markdown，存到 check-reports/2026-08-26-数据报告模块套件-自检报告.md，并上传 AI 自检报告管道：curl -X POST "https://pmlophy.com/p/jarvis/file/upload" -H "X-Jarvis-User: ai-reports" -F "file=@<报告绝对路径>"（URL 中 /p/jarvis/ 为历史命名，与罗宾助手无关）。若上传失败（DNS/审批/网络），如实说明失败原因，不得声称已上传。

红线禁止（违反=返工）
- 不 pull 就动手
- 修改 context/、requirements/、一期模块 PRD、一期字段清单、主PRD.md、mvp验收清单.md、第二步自动采集套件、第三步研究助手套件
- 在 prd-docs/ 写 context 未定义且未标注"新增建议，待确认"的字段/状态/枚举
- 把 token/key/密码写进任何文件
- 本步不生成 Demo 原型 PRD、不写代码、不动前端（那是后续步骤）
- 不 commit、不 push（交付后由用户确认）
- 声称"已部署/已上线"

Checkpoint（完成自检后停止）
对照 00-二期总PRD.md 检查：实体是否只覆盖报告/模板/推送任务/推送记录（自动采集、研究任务、选题/脚本实体属于其他模块，本步不展开）；报告生成状态机与总 PRD 5.3 审核抽查对齐；9 节结构完整；用例推演覆盖周报生成/失败重试/飞书推送/微信推送/沉淀引用五类场景；推送渠道与报告来源均标注待实测/待确认。全部通过后输出文件清单（含行数统计），停止等待确认，不进入下一步（四步全部完成后等待用户统一验收）。
