# 审核字样存量清理 + 脚本列表间距修复 — 指令

> 日期：2026-08-31
> 依据：用户 0828+0830 审核口径（前端永不展示审核/抽查字样或入口，预留仅后端）+ 用户实测截图反馈（脚本列表筛选区与表格间距过小，违反 UI-DESIGN.md 间距规范）
> 范围：前端存量审核字样清理 + 脚本列表页间距修复。禁止改后端。

## 指令正文（复制给 Codex，独立会话）

```plaintext
cd forge-scrm 仓库根目录（Mac 本地实际路径），不新建任何目录。开工前先 git pull origin main。

Context:
两项任务：
1. 审核字样存量清理。用户口径（0828+0830 定稿）：二期默认通过，前端【永不】展示任何"审核/抽查/待审核/转审核"字样、状态徽章、菜单入口或操作按钮。审核能力仅后端预留（review_status 等字段保留），前端全部隐藏。上一轮已移除 ReportDetail.tsx 的审核状态展示，但历史存量仍在——已实测发现的至少包括：
   - frontend/src/pages/analysis/AnalysisTasks.tsx（"执行完成，结果待人工审核"提示、"待审核"状态标签、分析结果审核按钮）
   - frontend/src/pages/analysis/AnalysisTaskDetail.tsx（"结果待人工审核"提示、"待审核"标签与审核操作、"回写产物需人工审核"Alert）
   - 脚本库相关：侧栏"脚本审核"菜单项、脚本列表"待审核"状态徽章
2. 脚本列表间距修复（用户实测截图反馈）：frontend/src/pages/scripts/ScriptList.tsx 筛选区与数据表格两个主区块间距过小，违反根目录 UI-DESIGN.md 第 2 节（主区块间应用 L=24px）。

Request:

一、全局排查并清理审核字样（前端 frontend/src/ 全目录）：
1. grep 全量扫描"审核|抽查|待审核|转审核"（排除注释），列出所有命中文件与行号。
2. 逐处清理，按类型处理：
   - 状态徽章/标签（如"待审核"橙色 badge）：不展示。若状态枚举仍需传给后端则保留数据逻辑仅隐藏 UI 展示；展示映射中"待审核"归入既有"已通过/已生效"或其他默认态展示，不得新造状态名。
   - 菜单入口（如侧栏"脚本审核"）：从导航移除。路由与页面组件文件保留（预留口子），仅从菜单/导航隐藏，不删除文件。
   - 操作按钮（如"审核通过/驳回"类）：移除入口。
   - message 提示文案（如"结果待人工审核"）：改为不含审核字样的中性表述（如"执行完成"）。
   - Alert 说明（如"需按流程审核"）：移除或改中性表述。
3. 权限相关：若清理后 PERM.xxx审核 权限判断失去使用点，保留常量定义不删（后端契约），仅移除前端判断分支。
4. 注意禁止连锁破坏：清理"待审核"展示时，确保状态过滤下拉（若有"状态"筛选含待审核选项）同步移除该选项，但其余选项与查询逻辑不动。

二、脚本列表间距修复：
1. frontend/src/pages/scripts/ScriptList.tsx：筛选区与表格之间增加区块间距，对齐 UI-DESIGN.md 第 2 节（L=24px；用 Stack spacing=24 或等效 marginBottom:24，禁止 6/12/18 等游离值）。
2. 若其他列表页存在同样贴身布局（筛选区与表格无间距），一并按同规范修正并列清单——但只改间距，不做其他重构。

红线禁止:
- 禁止修改 backend/ 任何文件
- 禁止删除路由、组件文件或状态枚举（只隐藏 UI，保留预留层）
- 禁止新造 context 未定义的状态/字段
- 遵守 UI-DESIGN.md（间距8px栅格、theme.ts 唯一色板、行内操作禁 primary、Table 必设 emptyText）
- 禁止 commit/push/部署
- 遵守 AGENTS.md 工作流铁律（先 pull、最小改动）

Checkpoint:
完成后生成自检报告 check-reports/审核字样存量清理与间距修复-自检报告-20260831.md，必须包含：
1. 全量扫描命中清单（文件:行号）与每处的处理方式
2. 间距修复的文件与改动点
3. 保留未删的预留层清单（路由/枚举/权限常量）
4. npm run build + tsc 通过证明
5. 复扫"审核|抽查"在 frontend/src/ 的最终结果（应为零 UI 命中，注释除外）
沙箱网络受限则报告留本地如实说明，由杰西卡代传管道。
```
