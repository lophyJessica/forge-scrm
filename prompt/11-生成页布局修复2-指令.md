Context（背景）
你在 /Users/liulongfei/个人文件/forge-scrm 仓库工作（Forge 新媒体运营系统）。
动手前先 git pull origin main，最新 HEAD 为 32a6584（生成页布局统一，已含修复）。
现状：
- 修复已上线：ScriptGenerate.tsx Form style={{ width: '100%', maxWidth: 720 }}、
  Card style={{ width: '100%' }}；线上 bundle index-DieNsg1w.js 已含这些特征
  （grep 确认：Form width+maxWidth 对象 1 处、Card title 1 处、maxWidth:720 共 2 处）
- 但用户强刷（Cmd+Shift+R）后实测：脚本页「基于选题生成脚本」表单仍是【全宽】
  （之前你实测 1603px / max-width:none），选题页正常 720px
- 说明 inline style 或渲染路径有隐藏问题：可能 antd Form 的 style 未透传到 <form>、
  或全局 CSS 覆盖、或组件层级中 style 被吞/被覆盖、或浏览器实际加载的 bundle 并非该文件

Request（任务）
1. 线上实测（必须做，用浏览器 DevTools，不是猜）：
   - 打开 https://scrm.pmlophy.com/scripts/generate（无痕/清缓存）
   - 检查 <form> 元素的 computed style（width/max-width）和实际渲染宽度
   - 查看 <form> 的 inline style 是否真的带 max-width:720px；如果带了但 computed 是
     none，找覆盖它的 CSS 规则来源（DevTools 的 Styles 面板被谁覆盖）
   - 确认浏览器实际加载的 bundle 文件名（Network 面板）是否 index-DieNsg1w.js
   - 同时对比 https://scrm.pmlophy.com/topics/generate 的 <form>（正常 720px）——两者
     computed/inline 差异就是根因
2. 修复：
   - 若 Form style 未透传/被覆盖：改用更稳的方式（如 Card body 容器加
     style={{ maxWidth: 720, width: '100%' }}，或给 Form 包一层 div 约束宽度，
     或 antd ConfigProvider 组件级，任选最稳的）
   - 两页保持一致的最终效果（同窗口同宽）
3. 验证：npm run build 通过；本地 dev server 目测两页一致（能起服务时）

Output format
1) DevTools 实测证据（form inline/computed/实际 bundle 文件名）；2) 根因；3) 修复改动；4) build 结果

红线
- 只改 frontend/src/；不动后端/context/；禁止 git commit/push；不写 key
- 不动表单字段/业务逻辑/模板库交互

Checkpoint
a) DevTools 实测定位根因（有证据）；b) 修复后源码级保证 max-width 生效；c) build 通过；
d) 自检报告存 check-reports/ 并如实汇报上传状态
