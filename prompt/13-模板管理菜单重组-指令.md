Context（背景）
你在 /Users/liulongfei/个人文件/forge-scrm 仓库工作（Forge 新媒体运营系统）。
动手前先 git pull origin main，最新 HEAD 为 c9a11ae（新建资料免审核）。
问题（用户反馈）：菜单里有两处「提示词模板」入口——「数据分析」下（/analysis/prompts）和
「权限管理」下（/admin/prompt-templates），功能重复冲突（两个页面都调 /prompt-templates CRUD）。
用户要求：提示词模板独立成模块——新增一级菜单「模板管理」，其下二级菜单「提示词模板」。

现状（已查证）：
- MainLayout.tsx 96 行「数据分析」子菜单含 102 行 { key: '/analysis/prompts', label: '提示词模板' }
- MainLayout.tsx 114 行「权限管理」子菜单含 { key: '/admin/prompt-templates', label: '提示词模板' }
- App.tsx 75 行 <Route path="/analysis/prompts" element={<PromptTemplates />} />（analysis 版旧页面）
- App.tsx 已有 /admin/prompt-templates → admin/PromptTemplates.tsx（P0 完整版：内置模板展示/删除保护/版本）
- 两个页面功能重复，保留 admin 版（完整），analysis 版废弃

Request（任务）
A. 菜单重组为独立「模板管理」模块：
1. MainLayout.tsx：
   - 新增一级菜单「模板管理」（放在「内容管理」与「AI 模块」之间，图标自选合理）
     children: [{ key: '/admin/prompt-templates', label: '提示词模板' }]
   - 「数据分析」下删除「提示词模板」子菜单（/analysis/prompts）
   - 「权限管理」下删除「提示词模板」子菜单（/admin/prompt-templates）
   - 权限管理保留其余子菜单不变
2. App.tsx：
   - 删除 <Route path="/analysis/prompts" ...>（analysis/PromptTemplates.tsx 文件保留不删）
   - /admin/prompt-templates 路由保留（模板管理模块入口）

B. 提示词模板启停用（用户补充需求）：
3. 后端（模板已具备 status 字段，PromptTemplateUpdate 已支持 status）：
   - builtin 接口（prompts.py 63-65 行）：模板查询加 .where(PromptTemplate.status == PromptStatus.启用)；
     该类型无启用模板时回退代码常量（fallback）
   - 生成默认模板（topic_service.py 139-142 行 + script_service.py 同款）：默认模板查询加
     .where(PromptTemplate.status == PromptStatus.启用)；无启用模板回退代码常量
   - 明确 template_id 指定的模板：不做状态拦截（用户显式指定即使用）
4. 前端 admin/PromptTemplates.tsx：
   - 表格加「状态」列：启用/停用 Tag（启用=green，停用=default/red）
   - 操作栏加「启停用」Switch：默认态=当前 status；切换后调 PUT /api/prompt-templates/{id}
     传 { status: 启用|停用 }（version 是否 +1 由后端 PUT 逻辑决定，不改语义）；
     切换成功 message 提示并刷新列表
   - 删除保护（最后一条不可删）逻辑不变；停用不限制（停用后不可用于生成，但可再次启用）
   - 【历史解耦确认规则】：启停用/删除仅影响后续生成的模板可选性；历史生成数据的 prompt
     快照（content_snapshot）独立存在，不受启停用/删除影响；不清理、不修改任何历史生成记录
5. 验证：npm run build 通过；菜单结构正确（模板管理一级 → 提示词模板二级，全站仅此一处）；
   停用模板不出现在 builtin/默认模板；TestClient 验证 builtin 过滤与生成默认模板过滤

Output format
1) 修改文件清单；2) 关键改动（含菜单结构调整说明）；3) build + 测试结果

红线
- 前端只改 frontend/src/layouts/MainLayout.tsx + frontend/src/App.tsx + frontend/src/pages/admin/PromptTemplates.tsx
- 后端只改 backend/app/routers/prompts.py + backend/app/services/{topic,script}_service.py
- 不动页面组件文件（analysis/PromptTemplates.tsx 保留）、不动数据库/迁移/context/
- 禁止 git commit/push；不写 key

Checkpoint
a) 模板管理一级菜单+提示词模板二级菜单；b) 数据分析/权限管理下提示词模板入口已移除；
c) /analysis/prompts 路由已删；d) 管理页状态列+一键启停用 Switch 可用；
e) 停用模板不进 builtin/默认模板；f) build+测试通过；g) 自检报告存 check-reports/ 并如实汇报上传状态
