cd ~/个人文件/forge-scrm && git pull origin main

```plaintext
Context:
项目 forge-scrm（Forge 新媒体运营系统），仓库位于 Mac ~/个人文件/forge-scrm，工作分支 main。本任务实现「模板引用资料」迭代，PRD 见 prd-docs/modules/模板引用资料迭代PRD.md（先读）。
背景：prompt_template 表已有 material_combo 字段（"固定关联资料组合"），后端模型/schema/路由均支持存储，但存在两处断点：
1) 生成链路不读取：backend/app/services/topic_service.py 的 build_prompt()（约124-167行）模板分支只取 template.content，未读取 template.material_combo 与用户当场 material_ids 合并。
2) 前端无配置入口：frontend/src/pages/admin/PromptTemplates.tsx 与 frontend/src/pages/analysis/PromptTemplates.tsx 的表单均未提供资料勾选，material_combo 恒为空。
现有可复用逻辑：backend/app/services/topic_service.py 的 build_material_block()（约107-121行）已实现资料注入（仅允许"已生效"状态，R1），合并后直接复用。

Request:
1. 后端（backend/app/services/topic_service.py）：
   - build_prompt() 模板分支：若 template.material_combo 非空，将其与调用方传入的 material_ids 合并去重（保持顺序，模板资料在前）后传入 build_material_block()。
   - snapshot 补充字段 material_combo_snapshot：数组，每项含 {id, title, content}（引用资料的 id、标题、当时内容），与 content_snapshot 并列，用于历史追溯。
   - 保持无模板/无资料时的现有行为完全不变（回归无损）。
2. 前端（frontend/src/pages/admin/PromptTemplates.tsx 与 frontend/src/pages/analysis/PromptTemplates.tsx）：
   - 模板新建/编辑表单新增「引用资料」多选区块，标题「引用资料（生成时自动注入最新内容）」，帮助文案「勾选后，每次使用该模板生成时会自动附带这些资料的最新内容；资料更新无需修改模板」。
   - 资料来源调现有资料列表接口，仅列出 status=已生效 的资料，支持按分类/标签筛选、关键词搜索、多选勾选，选中项可移除。
   - 提交时随表单带上 material_combo（资料 id 数组）；编辑时回显已有引用。
   - UI 风格与页面现有 antd 组件一致（Select mode=multiple 或 Transfer 均可，优先 Select+筛选）。
3. 禁止扩界：不做向量检索/RAG、不做占位符插入位置、不改审核规则（R8）、不改脚本生成与分析链路（本期只覆盖选题生成）、不动 context/ 目录。

Output format:
- 直接修改代码，最小改动，不顺手重构无关文件。
- 完成后跑通构建验证：后端 python 语法检查（或既有 lint），前端 npm run build 成功。
- 生成自检报告 markdown，内容含：任务、改动文件清单、每个文件改动点、自检结果、遗留风险；保存为 check-reports/模板引用资料-自检报告-20260828.md。

红线禁止:
- 禁止不执行 git pull 就改文件。
- 禁止修改 context/ 目录、prd-docs/ 目录（PRD 已写好，不需要你动）。
- 禁止 commit/push/deploy（交付代码与报告即可，提交部署由用户确认后另行执行）。
- 禁止把 token/key/密码写进代码或日志。
- 禁止伪造构建结果：build 失败必须如实报告并修复后再交付。

Checkpoint:
完成后必须执行 AI 自检报告管道上传（若沙箱网络受限，改为保存 check-reports/ 并如实汇报"未上传管道"，禁止声称已上传）：
curl --noproxy '*' -X POST "https://pmlophy.com/p/jarvis/file/upload" -H "X-Jarvis-User: ai-reports" -F "file=@check-reports/模板引用资料-自检报告-20260828.md"
最后汇报：改动文件列表、build 结果、报告文件名与字节数、是否已上传管道。
```
