Context（背景）
你在 /Users/liulongfei/个人文件/forge-scrm 仓库工作（Forge 新媒体运营系统）。
动手前先 git pull origin main。
用户需求（已澄清，重要）：【新建资料免审核】——人工创建/导入的资料直接生效；
【AI 生成结果保留审核】——AI 回写资料、AI 生成的选题/脚本、分析结果仍走审核（R8 规则保留）。
⚠️ 之前给过一版"审核功能下线"指令（prompt/12 旧版），范围理解错误，作废，以本版为准。
只改资料的人工创建路径，其余审核一律不动。

现状（已盘点）：
- 人工创建资料：MaterialForm.tsx 提交 → POST /api/materials（submit_for_review=true 时直接置「待审核」）
- 导入：MaterialImport.tsx → 导入的资料进「待审核」
- AI 回写资料：analysis.py 596 行 → 回写产物置 MaterialStatus.待审核（这是 AI 产物，保留审核）
- 资料审核页面：materials/MaterialReview.tsx（路由 /materials/review，菜单「资料审核」）
- 脚本审核（scripts/review）、分析结果审核（analysis review_task）——【不动，保留】

Request（任务）
新建资料免审核（仅人工路径），AI 产物审核保留：

1. 后端 materials.py：
   - 创建接口（POST /api/materials）：忽略 submit_for_review 参数，人工创建的资料直接置
     MaterialStatus.已生效（不再进待审核）
   - 导入接口：导入资料直接置「已生效」（不再进待审核）
   - AI 回写路径（analysis.py 596 行置待审核）：【不改】，AI 产物仍走审核
   - 资料审核路由（review/通过/驳回）：【保留不动】（AI 回写的待审核资料仍可审核）
2. 前端：
   - MaterialForm.tsx：去掉「提交审核」相关按钮/文案（保存即生效，不再有"保存并提交审核"）
   - MaterialImport.tsx：去掉"导入后进入待审核"提示文案（改为"导入即生效"）
   - MaterialList.tsx：「待审核」状态列保留（AI 回写产物仍可能待审核），不做删除
   - MaterialReview.tsx / 菜单「资料审核」/ 路由 /materials/review：【保留不动】（用于审核 AI 回写产物）
3. 验证：
   - npm run build 通过
   - TestClient：人工创建资料 → 状态直接已生效（不传/传 submit_for_review 均生效）；导入 → 已生效；
     AI 回写路径仍置待审核（mock 验证）
   - 脚本/分析审核相关无改动

Output format
1) 修改文件清单；2) 关键改动；3) build + 测试结果

红线
- 只改 backend/app/routers/materials.py + frontend/src/pages/materials/{MaterialForm,MaterialImport,List}.tsx
- 脚本审核、分析审核、资料审核页面/路由/菜单【一律不动】
- 不动数据库/迁移/context/；禁止 git commit/push；不写 key

Checkpoint
a) 人工创建/导入直接生效；b) AI 回写仍待审核（R8 保留）；c) 其他审核未动；
d) build+测试通过；e) 自检报告存 check-reports/ 并如实汇报上传状态
