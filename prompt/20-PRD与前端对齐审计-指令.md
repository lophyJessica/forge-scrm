cd ~/个人文件/forge-scrm && git pull origin main

```plaintext
Context:
项目 forge-scrm（Forge 新媒体运营系统），Mac ~/个人文件/forge-scrm，main 分支。本任务是【PRD ↔ 前端页面对齐审计】，只读产出报告，不写任何业务代码。
背景：前端迭代多轮后出现双向偏差——①前端做了 PRD 没有的功能（超前实现，可能引出无依据的功能面）；②PRD 定稿的功能前端没实现（缺口）。需要对齐一次，让"文档-代码"回到单一事实源。
文档权威层级（铁律）：context/ > prd-docs/（主PRD.md + modules/ 各模块PRD + 核心字段清单.md + mvp验收清单.md）。审计以这两层为准，draft/ 仅参考不作为依据。

Request:
1. 建立双侧清单：
   A. 页面侧：遍历 frontend/src/pages 全部页面与路由（含路由注册文件），列出每个页面的核心功能点（按钮/表单字段/列表列/操作/状态流转）。
   B. PRD 侧：遍历 prd-docs/ 主PRD + 全部 modules/*/主PRD 与字段清单，列出每个模块承诺的功能点（页面清单、功能清单、字段、状态机、MVP 验收项）。
2. 双向 diff，分三类产出：
   【超前】前端有、PRD 无——列出页面/功能点，并标注风险等级（高=引入新业务概念或写操作；低=纯展示辅助）。
   【缺口】PRD 有、前端无——列出 PRD 位置（文件+章节）+ 承诺内容 + 前端现状。标注是否属于 mvp验收清单 范围（MVP 缺口优先）。
   【偏差】两边都有但口径不一致——字段名/状态值/流程步骤/权限与 PRD 不符（对照 context/05 字段口径）。
3. 特别核对（已知关注点）：
   - 资料库模块：审核流、批量导入、标签/分类、有效期与可信度（R2）是否齐全
   - 选题库：批量生成每方向10条（R4）、去重（R14）、人工筛选（R3）
   - 脚本库：每选题2-3版（R5）、版本历史（R11）、已使用人工标记（R15）
   - 数据分析：任务状态机、结果审核（R6）、回写资料库（R7）
   - 数据报告：推送任务状态机（含已取消）、推送记录
   - 手机端适配：PRD 若承诺"一期桌面端"，前端不应有手机端专用实现（超前项）
4. 输出格式：markdown 审计报告，三张表（超前/缺口/偏差），每条含：页面或模块、PRD 依据（文件:章节）、代码位置（文件:行号）、建议处置（超前→回退或补PRD待确认 / 缺口→排期实现 / 偏差→以谁为准的判断建议）。最后给一个汇总统计（各类条数+最优先修的 Top5）。
5. 报告保存为 audit-reviews/PRD与前端对齐审计-20260828.md（目录不存在则创建）。

红线禁止:
- 禁止不执行 git pull 就开始。
- 只读审计：禁止修改任何业务代码、context/、prd-docs/；唯一可写的是 audit-reports/ 审计报告。
- 禁止猜测 PRD 内容：所有"缺口"判定必须引用 PRD 原文位置；PRD 没写的不能算缺口。
- 禁止把 draft/、requirements/ 当作依据。

Checkpoint:
完成后必须执行 AI 自检报告管道上传（沙箱 DNS 不解析 pmlophy.com 时按兜底规则：保存报告 + 如实汇报"未上传管道"，禁止重试超过1次）：
curl --noproxy '*' -X POST "https://pmlophy.com/p/jarvis/file/upload" -H "X-Jarvis-User: ai-reports" -F "file=@audit-reviews/PRD与前端对齐审计-20260828.md"
最后汇报：三类问题条数统计、Top5 优先项、报告文件名与字节数、是否已上传管道。
```
