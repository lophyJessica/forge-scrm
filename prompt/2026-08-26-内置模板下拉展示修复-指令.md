Context（背景）
你在 /Users/liulongfei/个人文件/forge-scrm 仓库工作（Forge 新媒体运营系统）。
动手前先 git pull origin main，当前最新 HEAD 为 4c00a4b（生成页提示词选择改造已上线）。
问题（用户验收反馈）：内置模板下拉选项被截断——topic_service 改造时 label 用了
`${task_type}：${content.replace(/\s+/g,' ').slice(0,30)}`，内置提示词长（几百字），
用户看不到完整内容。需要完全展示，或鼠标悬浮展示全文。

现状：
- frontend/src/pages/topics/TopicGenerate.tsx 350-360 行：内置模板 Select，options label 用 slice(0,30)
- frontend/src/pages/scripts/ScriptGenerate.tsx 同款实现（约 100-110 行）
- builtin 接口返回：[{task_type, content}]，content 为完整内置提示词

Request（任务）
修复内置模板下拉展示，让用户能看全提示词：

1. TopicGenerate.tsx + ScriptGenerate.tsx 的内置模板 Select 改造：
   - 下拉选项（Dropdown）：显示完整内容——label 用 `${task_type}：${content 原文}`（去掉 slice 截断），
     option 文本允许换行（antd 下拉默认单行截断时给 option 加样式：whiteSpace: 'normal'，
     或自定义 optionRender 渲染多行容器）
   - 选中后显示框（Select 收起态）：显示 `${task_type}`（简短即可）+ Tooltip 悬浮显示完整 content
     （antd Tooltip 包裹 Select，title=完整提示词；或 optionLabelProp 控制收起态显示短文本，
     下拉显示完整）
   - 两个页面行为一致

2. 验证：npm run build 通过；本地起 dev server 目测（如有能力）：下拉展开能看到完整提示词多行，收起后悬浮能看到全文

Output format
1) 修改文件与关键改动；2) build 结果

红线
- 只改 frontend/src/pages/topics/TopicGenerate.tsx + scripts/ScriptGenerate.tsx
- 不改后端、不改 builtin 接口、不改 content 内容
- 禁止 git commit/push；不写 key
- 内置模式仍不提交 prompt_template_id；自定义模式逻辑不动

Checkpoint
a) 下拉选项完整展示（无截断/可换行）；b) 收起态 Tooltip 悬浮显示全文；c) build 通过；d) 自检报告存 check-reports/ 并如实汇报上传状态
