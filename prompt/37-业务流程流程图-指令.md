# SCRM 业务流程与交互流程图 — 生成指令

> 日期：2026-08-31
> 参照物：forge-wms `docs/产品结构图_交互流程.md`（章节：产品结构图 → 各链核心交互流程 → 口径差异 → 设计边界）
> 现状：SCRM docs/flow/ 已有 flowchart.md（结构图）+ index.html（渲染页），缺"业务/交互流程"部分
> 信息源：6 份模块《业务流程推演.md》+ context/04 状态机 + App.tsx 路由
> 红线：只读生成文档；审核字样口径照旧；PRD/context 为准，路由交叉核对

## 指令正文（复制给 Codex，独立会话）

```plaintext
cd forge-scrm 仓库根目录（Mac 本地实际路径），不新建业务目录。开工前先 git pull origin main。

Context:
forge-scrm 的 docs/flow/ 已有产品结构图（flowchart.md + index.html 渲染页），现在补上【业务流程与交互流程】文档。参照 forge-wms 仓库 docs/产品结构图_交互流程.md 的结构（只读参照勿改）：它按业务链组织"核心交互流程"章节，每条链用 Mermaid sequenceDiagram/flowchart 描述页面→动作→状态流转→回写。

信息源（权威层级 context/ > prd-docs/，路由以 frontend/src/App.tsx 为准）：
- prd-docs/modules/ 下 6 份《业务流程推演.md》：资料库、选题库、脚本库、自动采集、数据分析、数据报告、研究助手（若存在）
- context/04-业务规则与状态机.md：资料/选题/脚本/分析任务状态机
- frontend/src/App.tsx 路由 + frontend/src/layouts/MainLayout.tsx 菜单
- 现状：一期=资料/选题/脚本/基础分析；二期=自动采集/研究助手/数据报告/推送/模板

Request:
生成 docs/flow/业务流程图.md，结构对齐 WMS 交互流程文档：

## 1. 主业务闭环（Mermaid flowchart）
资料 → 选题 → 脚本 → 发布（系统外）→ 数据 → 洞察 的完整闭环，标注二期采集/研究助手如何接入

## 2. 各模块核心业务流程（每模块一节，Mermaid flowchart 或 sequenceDiagram 二选一，按业务性质选最合适的）：
- 2.1 资料库：手动录入/导入（CSV/TXT）→ 分类标签 → 有效期/可信度 → 搜索调用/固定组合引用 → 停用/过期
- 2.2 选题库：方向批量生成（每方向10条）→ 跨批次完全重复去重 → 人工筛选（选定/废弃）→ 派生脚本
- 2.3 脚本库：基于选题生成2-3版/独立创建 → 挑选修改（每次生成新版本）→ 版本对比回退 → 标记已使用/废弃
- 2.4 自动采集：任务创建（账号/时间窗/范围，重复任务提示）→ 执行 → 记录级重试（失败记录）→ 结果沉淀为资料/作为分析输入
- 2.5 数据分析：数据准备（手动/导入/采集结果）→ 分析任务 → 执行 → 结果回写资料库/反哺选题（AI产物标记）
- 2.6 研究助手：范围选择（资料库/采集结果/外部检索）→ 研究任务执行（checkpoint/来源数）→ 报告 → 沉淀资料/生成选题/生成脚本
- 2.7 数据报告：新建（类型/周期/模板/来源范围）→ 生成 → 推送（飞书卡片）→ 推送记录明细
每节包含：业务流程图 + 关键状态流转（用 context/04 状态机）+ 涉及页面（真实路由）+ 关键动作按钮（从前端代码提取）

## 3. 权限边界
管理员 vs 成员：登录/个人账号、菜单可见性（以 MainLayout 菜单 + 路由权限为准）

## 4. 口径差异与使用说明
- 与 PRD 业务流程推演的差异（若有，以 PRD 推演为准但标注代码现状）
- 文档快照日期与后续维护提示

## 5. 产品设计边界
系统外人工运营（发布到视频号/小红书）、预留能力（定时采集/微信推送/语义去重）、外部依赖（DeepSeek API/飞书/MySQL）

完成后把新章节并入 docs/flow/flowchart.md 的渲染体系：要么 index.html 增加页签/分区加载业务流程图.md，要么在 index.html 中追加第二节（渲染两个 md 文件）——选实现简单且不破坏现有渲染的方案，说明理由。

红线禁止:
- 禁止修改任何业务代码、PRD、context（只新增 docs/flow/ 下文档与渲染调整）
- 状态描述不得出现"待审核/审核中"等预留态（按审核字样隐藏口径；一期历史规则可一次性说明）
- 禁止虚构页面/按钮/流程——页面路径与动作按钮必须能在 App.tsx/前端代码中找到
- 禁止 commit/push/部署
- 遵守 AGENTS.md 工作流铁律

Checkpoint:
完成后生成自检报告 check-reports/业务流程流程图-自检报告-20260831.md，含：文件清单、每模块流程与 PRD推演/路由的核对结果、审核字样复扫结果、渲染验证（index.html 本地打开 Mermaid 解析通过）、遗留风险。沙箱网络受限则报告留本地，由杰西卡代传管道。
```
