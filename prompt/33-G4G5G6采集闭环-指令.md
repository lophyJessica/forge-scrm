# 采集模块闭环 G4/G5/G6 — 开发指令

> 日期：2026-08-31
> 依据：audit-reviews/PRD与前端对齐复审-20260830.md（G4/G5/G6 均为"未做"）
> 范围：自动采集模块。前后端均可改。G6 撞产品决策时停下标注，不得自造。
> 审核红线：前端永不展示审核/抽查字样或入口。

## 指令正文（复制给 Codex，独立会话）

```plaintext
cd forge-scrm 仓库根目录（Mac 本地实际路径），不新建任何目录。开工前先 git pull origin main。

Context:
二期缺口批量补齐第一包：自动采集模块闭环（G4/G5/G6，复审报告均判"未做"）。本轮前后端均可改。开工前先读 audit-reviews/PRD与前端对齐复审-20260830.md 的 G4/G5/G6 行与对应 PRD 段落。⚠️表单字段命名教训：Form.Item 的 name 禁用 style/name/title/action 等 HTML 原生属性名（曾致 form.style 被遮蔽的线上 bug）。

Request:
按 PRD 补齐三项，形成闭环：

1. 【G4 采集重复任务提示】（PRD: 自动采集主PRD.md:74-76,152-154,273-286）
   - 后端：创建采集任务时，按「同账号+同时间窗+同范围」检测已有任务（利用模型已有的 idempotency_key 字段或等价查询），命中时返回冲突信息（已有任务编号/状态/结果数）
   - 前端：CollectionTasks.tsx 创建时收到冲突响应后，弹提示展示已有任务编号/状态/结果数，提供「查看旧任务」（跳转该任务详情/展开行）与「仍要创建」（若 PRD 允许）两个动作；PRD 未明确允许重复创建时只提供查看入口
2. 【G5 记录级重试】（PRD: :81-84,147-150,284-285）
   - 后端：新增记录级重试接口 POST /collection/records/{id}/retry——只重试该失败记录，成功结果不得重复生成（用记录状态+幂等键保证）
   - 前端：记录表（CollectionTasks.tsx 内）为失败记录行增加"重试"操作（仅 status=失败 可见），成功后刷新该任务记录
3. 【G6 采集结果进入资料库/数据分析】（PRD: :85-86,147-150,288-295；context/04 R8）
   - 后端：新增「结果转资料」接口（按结果生成资料记录，标记 AI 产物字段，遵守 R8 原始事实与 AI 产物区分）与「结果作为分析输入」能力（分析创建接口扩展支持 collection_result_ids，与既有 raw_data_ids 并列，执行器按 PRD 映射拼装输入）
   - 前端：结果表行操作增加「沉淀为资料」（弹确认+选择资料分类/标签，走既有资料创建表单字段）与「作为分析输入」（跳转分析创建页并预选该结果，或弹窗选择既有分析任务类型后创建）
   - ⚠️ G6 的 PRD 里有"待确认映射"项：凡 PRD/context 未明确的字段映射（如资料分类归属、可信度默认值），停下来在自检报告"待拍板项"列出并按最保守默认实现（不虚构），等用户拍板后调整

红线禁止:
- 前端不得展示任何"审核/抽查/待审核/转审核"字样或入口（0828+0830 定稿）
- 禁止新造 context 未定义的字段/状态/枚举；表单字段 name 禁用 style/name/title/action 等 HTML 原生属性名
- 遵守 UI-DESIGN.md（Drawer 三档、8px 栅格、行内禁 primary、Table emptyText、statusTagColor）
- 后端新增接口必须走既有路由风格与鉴权（PERM 权限常量沿用，不新建审核类权限）
- 禁止 commit/push/部署
- 遵守 AGENTS.md 工作流铁律（先 pull、最小改动、上下文一致）

Checkpoint:
完成后生成自检报告 check-reports/G4G5G6采集闭环-自检报告-20260831.md，含：
1. 改动文件清单（前端/后端分列）与每项功能对应关系
2. 新增接口清单（方法/路径/权限/幂等策略）
3. G6 待拍板项列表（若有）
4. npm run build + tsc + 后端 compileall 通过证明
5. 遗留风险
沙箱网络受限则报告留本地，由杰西卡代传管道。
```
