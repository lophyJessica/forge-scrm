Context（背景）
你在 /Users/liulongfei/个人文件/forge-scrm 仓库工作（Forge 新媒体运营系统）。
动手前先 git pull origin main，当前最新 HEAD 为 0c6fdc2（含第一步二期总PRD）。
文档权威层级：context/ > templates/ > prd-docs/ > draft/。context/ 只读，禁止修改。
本步为二期四步中的第二步：生成"自动采集模块"PRD 套件（只做文档，不做代码、不做原型）。

已确认的权威输入（必须先读）：
1. requirements/09-二期调研结论.md（2026-08-26 用户拍板：P0=自动采集+研究助手+默认通过；P1=周报+推送+定时采集；不做手机端/多人权限/抖音/公众号）
2. prd-docs/modules/00-二期总PRD.md（二期总览，自动采集的实体、状态机、规则、边界都在这里）
3. prd-docs/modules/审核流程变更说明.md（默认通过规则）
4. templates/prd-template.md（模块 PRD 模板，9 节结构）
5. context/05-术语与字段口径.md（字段 SSOT，复用字段以它为准）

格式参考（一期模块套件范例，完全对齐）：
- prd-docs/modules/选题库/选题库主PRD.md
- prd-docs/modules/选题库/选题库字段清单.md
- prd-docs/modules/选题库/选题库_业务规则规格.md
- prd-docs/modules/选题库/选题库_用例数据推演.md
- prd-docs/modules/选题库/选题库_业务流程推演.md

Request（任务）
新建目录 prd-docs/modules/自动采集/，生成以下 5 份文档：

1. 自动采集主PRD.md
按 templates/prd-template.md 的 9 节结构：模块概述/功能清单/交互流程/关键规则/状态机/数据字段/权限/验收标准/待确认事项。
内容边界以 00-二期总PRD.md 为准：对标账号管理、人工触发采集任务、对标账号内容/数据采集（优先）、评论/热点/行业报告采集（次之，标注待实测）、采集记录与采集结果独立落库、失败重试与重复任务提示。定时采集属于 P1 只预留不设计细节。功能清单给出功能名称、优先级（P0/P1）、In/Out、关键规则列。状态机对齐总PRD 5.1：待执行→执行中→已完成/部分成功/失败，用 mermaid stateDiagram-v2 表达，部分成功标注"新增建议，待确认"。

2. 自动采集字段清单.md
对齐一期核心字段清单格式（prd-docs/核心字段清单.md 与一期各模块字段清单）。覆盖实体：对标账号、采集任务、采集记录、采集结果。每个表给出：表名、字段名、类型、必填、默认值、说明。规则：
- 复用 context/05 已有字段口径（如 data_source 的业务对象/平台/账号标识/对标标记/原始内容/时间窗）时直接引用，不重造同名不同义字段。
- 本模块新增字段全部标注"新增建议，待确认"。
- 采集结果必须区分原始事实与 AI 产物，is_ai_product 字段语义与一期一致（采集结果默认 false）。
- 字段清单末尾附表关系总览（简单 mermaid erDiagram 或文字关系）。

3. 自动采集_业务规则规格.md
细化规则：R13 人工触发（定时 P1）；官方 API 优先、网页抓取合规边界（robots/服务条款/频率/版权，全部标注待实测）；原始事实直接信不经过 AI 审核；采集失败任务级/记录级错误与重试；同账号同时间窗同范围重复任务提示；来源追溯（URL/账号/时间窗/任务）。每条规则编号（AC-R1、AC-R2...），并与 context/04 已有规则编号建立引用关系。

4. 自动采集_用例数据推演.md（强制关卡）
对齐一期用例数据推演格式，至少覆盖 5 个用例：正常采集全流程（对标账号→触发→完成）、部分失败（记录级失败→部分成功→重试失败记录）、任务级失败（重试）、重复任务提示（幂等）、采集结果进入资料库/分析链路（原始事实与 AI 产物区分）。每个用例包含：场景、前置条件、操作步骤、预期结果、关键数据示例（具体到字段值）。推演不通过不能进入后续设计，这是铁律。

5. 自动采集_业务流程推演.md
对齐一期业务流程推演格式：主流程步骤推演（维护对标账号→选择账号与范围→人工触发→任务执行→结果落库→摘要与使用链路）、异常分支（账号停用、来源不可达、重复任务）、页面与状态对应关系。

Output format（输出要求）
- 5 份文档全部中文，结构对齐一期对应文件，markdown 格式。
- 新增实体、字段、状态、配置一律标注"新增建议，待确认"，禁止冒充 context/05 已确认口径。
- 引用一期规则（R1-R20）时沿用原编号，新增规则用 AC- 前缀。
- 完成后生成自检报告 markdown（任务/项目、改动文件清单、每个文件改了什么、自检结果、遗留风险），存到 check-reports/2026-08-26-自动采集模块套件-自检报告.md，并上传 AI 自检报告管道：curl -X POST "https://pmlophy.com/p/jarvis/file/upload" -H "X-Jarvis-User: ai-reports" -F "file=@<报告绝对路径>"（URL 中 /p/jarvis/ 为历史命名，与罗宾助手无关）。

红线禁止（违反=返工）
- 不 pull 就动手
- 修改 context/、requirements/、一期模块 PRD、一期字段清单、主PRD.md、mvp验收清单.md
- 在 prd-docs/ 写 context 未定义且未标注"新增建议，待确认"的字段/状态/枚举
- 把 token/key/密码写进任何文件
- 本步不生成 Demo 原型 PRD、不写代码、不动前端（那是后续步骤）
- 不 commit、不 push（交付后由用户确认）
- 声称"已部署/已上线"

Checkpoint（完成自检后停止）
对照 00-二期总PRD.md 检查：实体是否只覆盖对标账号/采集任务/采集记录/采集结果（研究任务/研究报告/周期报告/推送记录属于后续步骤，本步不展开）；状态机与总PRD 5.1 一致；9 节结构完整；用例推演覆盖正常/部分失败/失败重试/重复提示/链路使用五类场景。全部通过后输出文件清单（含行数统计），停止等待确认，不进入下一步。
