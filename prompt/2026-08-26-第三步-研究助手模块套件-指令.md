Context（背景）
你在 /Users/liulongfei/个人文件/forge-scrm 仓库工作（Forge 新媒体运营系统）。
动手前先 git pull origin main，当前最新 HEAD 为 b1eb1ef（含第二步自动采集模块套件）。
文档权威层级：context/ > templates/ > prd-docs/ > draft/。context/ 只读，禁止修改。
本步为二期四步中的第三步：生成"研究助手模块"PRD 套件（只做文档，不做代码、不做原型）。

已确认的权威输入（必须先读）：
1. requirements/09-二期调研结论.md（2026-08-26 用户拍板：P0=自动采集+研究助手+默认通过；P1=周报+推送+定时采集；不做手机端/多人权限/抖音/公众号）
2. prd-docs/modules/00-二期总PRD.md（二期总览，研究助手的实体、状态机、规则、边界都在这里）
3. prd-docs/modules/审核流程变更说明.md（默认通过规则：AI 结果默认通过 + 轻量抽查）
4. templates/prd-template.md（模块 PRD 模板，9 节结构）
5. context/05-术语与字段口径.md（字段 SSOT，复用字段以它为准）
6. prd-docs/modules/自动采集/（第二步刚完成的套件，作为本步格式对齐范例，也用于确认采集结果与研究助手的关系）

格式参考（与本步同期的模块套件范例，完全对齐）：
- prd-docs/modules/自动采集/自动采集主PRD.md
- prd-docs/modules/自动采集/自动采集字段清单.md
- prd-docs/modules/自动采集/自动采集_业务规则规格.md
- prd-docs/modules/自动采集/自动采集_用例数据推演.md
- prd-docs/modules/自动采集/自动采集_业务流程推演.md

Request（任务）
新建目录 prd-docs/modules/研究助手/，生成以下 5 份文档：

1. 研究助手主PRD.md
按 templates/prd-template.md 的 9 节结构：模块概述/功能清单/交互流程/关键规则/状态机/数据字段/权限/验收标准/待确认事项。
内容边界以 00-二期总PRD.md 为准：研究助手 = 操盘手发起研究主题 → 系统联网检索并汇总 → 结果沉淀为研究报告/AI 产物（必须标 is_ai_product=true）→ 引用来源可追溯 → 进入审核默认通过 + 轻量抽查。实体以总 PRD 为准（预期为研究任务 + 研究结果/报告，若总 PRD 定义了引用来源实体则一并覆盖；不得自行扩展总 PRD 未定义的实体）。联网检索能力：DS 官方搜索待实测、Tavily 免费层 1000 次/月兜底、超量 PAYG，来源接入标注待实测。功能清单给出功能名称、优先级（P0/P1）、In/Out、关键规则列。状态机对齐总PRD 5.1（研究任务：待执行→执行中→已完成→待审核→已确认/已废弃，失败可重试），用 mermaid stateDiagram-v2 表达。

2. 研究助手字段清单.md
对齐自动采集字段清单格式。覆盖实体：研究任务、研究结果/报告（以及总 PRD 定义的引用来源实体）。每个表给出：表名、字段名、类型、必填、默认值、说明。规则：
- 复用 context/05 已有字段口径（如 data_source、AI 产物标记、来源链接、业务对象）时直接引用，不重造同名不同义字段。
- 本模块新增字段全部标注"新增建议，待确认"。
- 研究报告/结果必须 is_ai_product=true（AI 生成产物，与原始事实相反），引用来源（source_url/来源名称/检索时间）必须可追溯。
- 字段清单末尾附表关系总览（简单 mermaid erDiagram）。

3. 研究助手_业务规则规格.md
细化规则：研究任务人工发起（P0）、定时研究 P1 预留；联网检索来源优先序与合规边界（官方 API/授权接口 > 公开数据 > 合规抓取，robots/条款/频率/版权标注待实测）；研究结果默认 AI 产物标记（R8 延展）；审核默认通过 + 轻量抽查（对齐审核流程变更说明，抽查比例/触发时机标注待确认）；引用追溯（来源 URL/名称/检索时间/原始内容留档）；失败重试（任务级/记录级错误区分）；研究结果进入资料库/分析链路遵循目标模块规则。每条规则编号（RS-R1、RS-R2...），并与 context/04 已有规则编号建立引用关系。

4. 研究助手_用例数据推演.md（强制关卡）
对齐自动采集用例数据推演格式，至少覆盖 5 个用例：发起研究→联网检索→结果汇总（正常全流程）、检索来源部分失败（部分成功→重试失败来源）、任务级失败（重试）、研究结果审核默认通过+抽查、研究结果沉淀资料库/进入分析链路（AI 产物标记与引用追溯）。每个用例包含：场景、前置条件、操作步骤、预期结果、关键数据示例（具体到字段值）。推演不通过不能进入后续设计，这是铁律。

5. 研究助手_业务流程推演.md
对齐自动采集业务流程推演格式：主流程步骤推演（发起研究→选来源范围→任务执行→检索与汇总→结果沉淀→审核/抽查→使用链路）、异常分支（来源不可达、无结果、重复研究）、页面与状态对应关系。

Output format（输出要求）
- 5 份文档全部中文，结构对齐自动采集套件对应文件，markdown 格式。
- 新增实体、字段、状态、配置一律标注"新增建议，待确认"，禁止冒充 context/05 已确认口径。
- 引用一期规则（R1-R20）时沿用原编号，新增规则用 RS- 前缀（不要用 AC-，那是自动采集的前缀）。
- 完成后生成自检报告 markdown（任务/项目、改动文件清单、每个文件改了什么、自检结果、遗留风险），存到 check-reports/2026-08-26-研究助手模块套件-自检报告.md，并上传 AI 自检报告管道：curl -X POST "https://pmlophy.com/p/jarvis/file/upload" -H "X-Jarvis-User: ai-reports" -F "file=@<报告绝对路径>"（URL 中 /p/jarvis/ 为历史命名，与罗宾助手无关）。

红线禁止（违反=返工）
- 不 pull 就动手
- 修改 context/、requirements/、一期模块 PRD、一期字段清单、主PRD.md、mvp验收清单.md、第二步自动采集套件
- 在 prd-docs/ 写 context 未定义且未标注"新增建议，待确认"的字段/状态/枚举
- 把 token/key/密码写进任何文件
- 本步不生成 Demo 原型 PRD、不写代码、不动前端（那是后续步骤）
- 不 commit、不 push（交付后由用户确认）
- 声称"已部署/已上线"

Checkpoint（完成自检后停止）
对照 00-二期总PRD.md 检查：实体是否只覆盖研究任务/研究结果/引用来源（周报/推送记录/自动采集实体属于其他模块，本步不展开）；状态机与总PRD 5.1 一致；9 节结构完整；用例推演覆盖正常/部分失败/失败重试/默认通过抽查/链路使用五类场景；研究结果 is_ai_product=true 与引用追溯贯穿全部文档。全部通过后输出文件清单（含行数统计），停止等待确认，不进入下一步。
