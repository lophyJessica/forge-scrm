Context（背景）
你在 /Users/liulongfei/个人文件/forge-scrm 仓库工作（Forge 新媒体运营系统）。
动手前先 git pull origin main，当前最新 HEAD 为 0751ee5（含二期四步文档套件）。
文档权威：context/ > templates/ > prd-docs/。context/ 只读。
本次只做后端骨架，不碰前端，不接 AI 能力（采集执行/检索后续步骤）。

现有后端结构（backend/，FastAPI 0.115 + SQLAlchemy 2.0 + Alembic）：
- backend/app/main.py 注册路由
- backend/app/models/ SQLAlchemy 模型（一期已有 materials/topics/scripts/analysis 等）
- backend/app/routers/ CRUD 路由
- backend/app/schemas/ Pydantic 模型
- backend/app/services/ 业务逻辑
- backend/alembic/ 迁移
- 数据库：MySQL（一期已建 20 表）

权威依据（必读）：
- prd-docs/modules/自动采集/自动采集字段清单.md（四实体：对标账号/采集任务/采集记录/采集结果）
- prd-docs/modules/研究助手/研究助手字段清单.md（三实体：研究任务/研究报告/研究引用关系）
- context/05-术语与字段口径.md（字段 SSOT，禁止新造未定义字段）
- prd-docs/modules/00-二期总PRD.md 5.1/5.2（状态机）

Request（任务）
第一步：后端数据模型 + Alembic 迁移 + CRUD API（纯骨架，不实现采集/检索逻辑）

1. 按两个字段清单新增 7 个模型（表名以字段清单为准）：
   - 自动采集：benchmark_account、collection_task、collection_record、collection_result
   - 研究助手：research_task、research_report、research_reference
   - 字段、类型、约束严格对照字段清单；字段清单标注"待确认"的字段先按建议实现并在代码注释标注 TODO(待确认)
   - 状态字段用字符串枚举，值严格对齐二期总 PRD 5.1/5.2：
     采集任务：pending→running→success/partial_success/failed
     研究任务：pending→searching→organizing→success/failed
   - is_ai_product 字段：research_report 必须 true，collection_result 默认 false
   - 时间字段：created_at/updated_at 与一期模型保持一致风格

2. Alembic 迁移：生成新迁移文件（alembic revision），可成功 upgrade

3. CRUD 路由（沿用一期 routers 的写法）：
   - 自动采集：对标账号 CRUD + 采集任务 CRUD（含创建/触发占位/重试占位）+ 采集记录列表 + 采集结果列表
   - 研究助手：研究任务 CRUD（含创建/重试占位）+ 研究报告详情 + 引用关系列表
   - 列表接口带分页（一期分页风格）+ 按状态筛选
   - "触发/执行"类动作只留占位（返回 501 NotImplemented），后续步骤接 AI 能力
   - 路由注册进 main.py，路径前缀 /api/v1/（对齐一期）

4. 不新增依赖（如确需，说明理由）

Output format
1) 新增/修改文件清单；2) 每个文件的关键改动说明；3) alembic upgrade head 执行结果；4) 后端启动冒烟测试结果（uvicorn 启动 + 新路由 GET 200）

红线
- 只改 backend/，禁止碰 frontend/、context/、prd-docs/、requirements/
- 禁止修改一期已有模型/路由（除非明确必要的依赖调整并说明）
- 禁止 git commit/push；改完 git status 确认改动范围
- 表名/字段名以字段清单为准，禁止自由命名
- 不写任何密钥/token 到代码

Checkpoint
a) 7 个模型字段与字段清单逐条对照一致；b) alembic upgrade 成功；c) 新路由注册且冒烟 200；d) 未改动受保护目录；e) 自检报告保存到 check-reports/ 并如实汇报（沙箱网络受限则说明，不声称已上传管道）
