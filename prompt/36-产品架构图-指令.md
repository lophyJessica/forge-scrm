# Forge SCRM 产品架构图 — 生成指令

> 日期：2026-08-31
> 参照物：forge-wms 仓库 `docs/forge-产品架构图.md`（总览+模块全景+作业链+系统关系）与 `docs/flow/flowchart.md`（Mermaid 产品结构图+结构解读）及 `docs/flow/index.html`（Mermaid 渲染页，深色主题+缩放控件）
> 产物：forge-scrm 仓库 `docs/flow/` 三件套（同 WMS 结构）
> 红线：只读 PRD/前端路由生成文档，不改任何业务代码；审核字样口径照旧（文档中状态描述用 displayStatus 映射后的用户可见状态）

## 指令正文（复制给 Codex，独立会话）

```plaintext
cd forge-scrm 仓库根目录（Mac 本地实际路径），不新建业务目录。开工前先 git pull origin main。

Context:
参照 forge-wms 仓库（路径：/Users/liulongfei/个人文件/forge-wms，只读参照，禁止修改）的 docs/ 三件套结构，为 forge-scrm 生成同构的产品架构图文档。WMS 参照物结构：
1. docs/forge-产品架构图.md——总览 Mermaid（双系统关系）→ 各系统模块全景表 → 核心作业链 → 系统外部关系 ASCII 图 → 两系统模块对应关系
2. docs/flow/flowchart.md——产品结构 Mermaid flowchart（单据链+库存边界+主数据虚线）+ 结构解读表（业务域/核心单据/上下游/边界）
3. docs/flow/index.html——深色主题 Mermaid 渲染页（mermaid@10 CDN + marked，含缩放控件），把 md 内容嵌入

forge-scrm 的信息源（权威层级 context/ > prd-docs/，前端路由作交叉核对）：
- prd-docs/主PRD.md（产品概述/功能范围/用户故事）
- prd-docs/modules/ 下 8 个模块：资料库、选题库、脚本库、自动采集、研究助手、数据分析、数据报告、权限账号
- prd-docs/mvp验收清单.md、核心字段清单.md
- frontend/src/App.tsx 全部路由 + frontend/src/layouts/MainLayout.tsx 侧栏菜单（页面路径与菜单分组以代码为准）
- context/04-业务规则与状态机.md（状态机：资料/选题/脚本/分析任务）
- 补充现状：二期已落地自动采集/研究助手/数据报告/推送（飞书卡片推送+报告模板）/权限账号；一期为资料/选题/脚本/基础分析

Request:
生成 forge-scrm 三件套到 docs/ 目录（新建 docs/ 与 docs/flow/）：

1. docs/forge-产品架构图.md，结构对齐 WMS 版：
   - 一、总览 Mermaid：围绕「资料 → 选题 → 脚本 → 发布 → 数据 → 洞察」闭环画三库与数据链路；标注自动采集为数据入口之一、数据分析回写资料库/反哺选题、飞书推送为输出通道
   - 二、Forge SCRM 模块全景表：一级模块（资料库/选题库/脚本库/自动采集/研究助手/数据分析/数据报告/权限账号/推送）× 二级功能 × PRD 状态（✅ 一期 / ✅ 二期 / 预留）× 说明；状态描述用用户可见口径（不含"待审核"等预留态，按审核字样隐藏口径）
   - 三、核心作业链：资料链（录入/导入→审核隐藏→可用资料→固定组合）、选题链（方向批量生成→去重→人工筛选→可用选题）、脚本链（选题生成2-3版→挑选修改→版本历史→标记已使用）、数据链（手动/导入/采集→分析→回写资料/反哺选题→报告→飞书推送）、研究链（范围选择→执行→报告→沉淀/生成选题/脚本）
   - 四、系统外部关系：DeepSeek API（生成/分析）、飞书（卡片推送）、视频号/小红书（二期采集目标，预留）、MySQL/对象存储
   - 五、与 Forge WMS/ERP 的关系：同一产品矩阵下的独立系统（新媒体运营域），说明数据无直接打通、方法论同源
2. docs/flow/flowchart.md：全系统产品结构 Mermaid flowchart LR——按「数据入口层（手动/导入/自动采集）→ 资料库 → 选题库 → 脚本库 → 数据分析/研究助手 → 数据报告 → 飞书推送」画主链，标注状态机关键节点（资料有效期/可信度、选题筛选与去重边界、脚本版本、分析任务回写）、权限边界（管理员/成员）、以及每条链的边界说明；文末结构解读表（业务域/核心页面/上下游/边界）
3. docs/flow/index.html：复制 forge-wms/docs/flow/index.html 的骨架（深色主题样式+mermaid+marked+缩放控件），替换为 flowchart.md 内容；标题改「Forge SCRM 产品结构图与核心流程」
4. 路由核对：文中提到的每个页面必须在 App.tsx 路由中存在，页面路径写实际路由（如 /materials、/scripts、/report-templates）；发现 PRD 描述与代码路由不一致时以代码为准并在文末"PRD 与代码差异"小节列出

红线禁止:
- 禁止修改任何业务代码、PRD、context（只读信息源，只新增 docs/ 三个文件）
- 禁止虚构模块或功能——一切以 PRD 与路由代码为准，PRD 预留未实现的能力标"预留"
- 状态描述不得出现"待审核/审核中"等预留态（按审核字样隐藏口径；历史状态在讲一期规则时可提"一期含管理员审核（已隐藏）"一次性说明）
- 禁止 commit/push/部署
- 遵守 AGENTS.md 工作流铁律

Checkpoint:
完成后生成自检报告 check-reports/产品架构图-自检报告-20260831.md，含：三文件清单与行数、路由核对结果（每个提及页面 vs App.tsx 实际路由）、PRD 与代码差异清单、遗留风险。沙箱网络受限则报告留本地，由杰西卡代传管道。
```
