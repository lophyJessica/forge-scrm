# 脚本列表卡片丢失 — 32-b 根因锁定与修复指令

> 日期：2026-08-31
> 前置：32 号指令的修复（独立对象 + key）已部署但无效。杰西卡 Playwright 深挖出决定性证据，根因已收窄到最后一层。
> 范围：仅前端 scripts 相关。禁止改后端、禁止改其他 7 个正常页面。

## 杰西卡实测证据链（已验证，勿重复排查）

1. 源码正确：`ScriptList.tsx:67` 已用 `style={filterCardStyle}`（独立对象）+ `key="scripts-filter"`
2. bundle 正确：线上 `index-DkyLcU7J.js` 脚本模块 `style:t`，`t={...Ji}`，Ji 定义完整
3. React 层正确：form DOM 的 fiber `memoizedProps.style` 有完整样式对象（Playwright 读 fiber 实锤）
4. DOM 层丢失：`<form>` 的 style attribute 为 null；资料页同结构正常
5. **决定性证据**：在脚本页对该 form 执行 `el.style.setProperty(...)` 抛出 **`TypeError: el.style.setProperty is not a function`** —— **该 form 元素的 `style` 属性不是 CSSStyleDeclaration 实例，被替换成了普通对象！**
6. 由此推论：React commit 时逐条赋值 `node.style.background = ...` 全部写进了假对象（静默失败），DOM style attribute 从未写入。这解释了全部现象（props 有/DOM 无/无报错/hook 抓不到/手动 setAttribute 不被清除）
7. 其他排查已排除：缓存、CDN、懒加载、重复组件、构建缓存、React fiber 复用、MutationObserver 删除、JS 报错

## 指令正文（复制给 Codex，独立会话）

```plaintext
cd forge-scrm 仓库根目录（Mac 本地实际路径），不新建目录。开工前先 git pull origin main。

Context:
/scripts 页筛选 Form 的卡片样式丢失。杰西卡 Playwright 深挖已锁定根因层级：该页 form 元素的 style 属性被某段代码替换成了普通 JS 对象（非 CSSStyleDeclaration 实例），导致 React 的样式写入全部静默失败。证据：在该 form 上调用 el.style.setProperty 抛 TypeError "is not a function"；而 fiber props 里样式完整。资料列表等其他 7 页正常。React StrictMode 已开启（main.tsx）。脚本页特有代码路径：useSearchParams + useEffect 里 form.setFieldValue；Form 带 key="scripts-filter"。

Request:
1. 【运行时定位替换点】用 Playwright（npx playwright，本地无头）以 admin/admin123 登录 https://scrm.pmlophy.com/scripts，在 addInitScript 阶段对 HTMLElement.prototype 的 style getter 挂 Object.defineProperty 拦截（记录 getter 被调用后返回值 instanceof CSSStyleDeclaration 的结果），并对 Object.defineProperty 本身打补丁记录"对 form 元素实例 defineProperty('style')"的调用栈；同时记录 form 元素创建时机。复现"style 是普通对象"后，从 window.__hookLog 拿到替换发生的调用栈，定位到具体源码位置。
2. 若运行时拦截定位不到（例如替换发生在原生属性而非 defineProperty），改用源码二分法：全仓 grep 以下嫌疑模式，逐个核对是否在脚本页激活路径上：
   a. `defineProperty`、`.style =`、`style =`（非 JSX style 属性的赋值语句）
   b. useMetaStore / meta store 初始化里对 Form/元素的操作
   c. 脚本页 useEffect（useSearchParams + form.setFieldValue）与 antd Form 内部 rc-field-form 实例注册的交互——检查 antd 版本（package.json）已知 issue
   d. `key="scripts-filter"` 与 rc-field-form 的 FormStore 缓存交互（尝试去掉 key 或换 key 值验证）
3. 修复要求：
   - 修复方式必须治根（消除替换行为），禁止在渲染后用 useEffect 手动把样式写回 DOM（那是糊补丁）
   - 最小改动，只动 scripts 相关文件或定位到的全局工具文件
   - 禁止动其他 7 个正常页面；禁止改 theme.ts 的 FILTER_CARD_STYLE 定义；禁止改后端
4. 本地验证（不部署）：
   - npm run build 后本地起 vite preview（或 dist 静态服务），Playwright 登录本地副本验证 /scripts 的 form 满足：①el.style instanceof CSSStyleDeclaration === true ②getAttribute('style') 包含 background ③computed backgroundColor 为 rgb(250, 250, 250)
   - 回归 /materials 与 /collection/tasks 仍正常
5. 禁止 commit/push/部署，停在本地等待用户执行 deploy.sh

红线禁止:
- 禁止修改 backend/
- 禁止修改其他 7 个正常列表页
- 禁止改 theme.ts 的 FILTER_CARD_STYLE
- 禁止用 useEffect 手写 DOM 样式的方式糊补丁
- 禁止跳过运行时定位直接瞎改
- 禁止 commit/push/部署
- 遵守 AGENTS.md 工作流铁律

Checkpoint:
完成后生成自检报告 check-reports/脚本列表卡片32b修复-自检报告-20260831.md，必须包含：
1. 替换点的调用栈证据（或二分定位过程）
2. 根因一句话结论
3. 修复方式与改动文件
4. 本地 Playwright 验证：/scripts 三项断言 + /materials、/collection/tasks 回归
5. build/tsc 证明
沙箱网络受限则报告留本地，由杰西卡代传管道。
```
