Context（背景）
你在 /Users/liulongfei/个人文件/forge-scrm 仓库工作（Forge 新媒体运营系统）。
动手前先 git pull origin main，当前最新 HEAD 为 87fca93（P0 提示词模板管理页面）。
一/二期后端已就绪：7 实体 CRUD + 采集执行器 + 研究检索（Tavily）+ DeepSeek 报告。
本次做二期前端页面：自动采集 + 研究助手。前端 React + antd（一期风格）。
context/ 只读。

现有后端 API（backend/app/routers/phase2.py，全部 /api/v1）：
- 对标账号：GET/POST /benchmark-accounts、GET/PUT/DELETE /benchmark-accounts/{id}（platform/enabled/keyword 筛选）
- 采集任务：GET/POST /collection-tasks、GET /collection-tasks/{id}（status 筛选）、POST /collection-tasks/{id}/execute、POST /collection-tasks/{id}/retry
- 采集记录：GET /collection-tasks/{id}/records（task 关联）
- 采集结果：GET /collection-tasks/{id}/results、GET /collection-records/{id}/results
- 研究任务：GET/POST /research-tasks、GET /research-tasks/{id}、POST /research-tasks/{id}/execute、POST /research-tasks/{id}/retry
- 研究报告：GET /research-tasks/{id}/report（含引用 research_reference）

状态枚举：
- 采集任务：pending/running/success/partial_success/failed（重试仅 failed）
- 研究任务：pending/searching/organizing/success/failed（重试仅 failed）
- 采集结果 is_ai_product=false；研究报告 is_ai_product=true（前端展示"AI 生成"标记）

一期前端参考（沿用风格）：
- frontend/src/pages/topics/TopicList.tsx（列表页：Table + 状态 Tag + 分页）
- frontend/src/pages/analysis/AnalysisTasks.tsx（任务页：创建表单 + 列表 + 执行按钮）
- frontend/src/App.tsx（路由注册）、frontend/src/layouts/MainLayout.tsx（菜单）

Request（任务）
第三步：二期前端页面——自动采集 + 研究助手

1. 自动采集页（frontend/src/pages/collection/ 下）：
   - BenchmarkAccounts.tsx：对标账号列表（platform/enabled 筛选 + keyword 搜索 + 分页）+ 新建/编辑/停用（enabled 切换）
   - CollectionTasks.tsx：采集任务列表（status 筛选 + 分页）+ 新建任务弹窗（选对标账号、时间窗、public_urls 可选）+ 详情展开（记录/结果 tabs）+ 执行/重试按钮（failed 显示重试）
   - 状态展示：pending=默认/running=processing/success=success/partial_success=warning/failed=error（antd Tag 或 Badge）

2. 研究助手页（frontend/src/pages/research/ 下）：
   - ResearchTasks.tsx：研究任务列表（status 筛选 + 分页）+ 新建任务（topic/objective/scope_config 可选）+ 执行/重试按钮
   - ResearchReport.tsx：报告详情页（title/summary/content/sections/conclusions 展示 + 引用列表 source_url/source_title/search_provider + "AI 生成"标记）
   - 进度展示：searching/organizing 显示进行中（Spin/进度）

3. 路由/菜单：
   - App.tsx 注册：/collection/benchmark-accounts、/collection/tasks、/research/tasks、/research/reports/{id}
   - MainLayout.tsx 菜单：内容管理下加"对标账号"、"自动采集"；AI 模块下加"研究助手"（参考现有菜单分组）

4. 构建验证：npm run build 通过

Output format
1) 新增/修改文件清单；2) 关键改动；3) build 结果

红线
- 只改 frontend/src/，禁止碰 backend/、context/、prd-docs/、requirements/
- 不新增依赖（如需说明理由）
- 状态/字段严格对齐后端 API 和 context/05 语义，不发明新字段
- 禁止 git commit/push；改完 git status 确认

Checkpoint
a) 四个页面可用（列表/筛选/分页/新建/执行/重试/详情）；b) 状态展示正确；c) 报告页含引用和 AI 标记；d) build 通过；e) 自检报告存 check-reports/ 并如实汇报上传状态
