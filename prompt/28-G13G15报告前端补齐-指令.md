# G15+G13 报告前端补齐 — 指令

> 日期：2026-08-30
> 依据：audit-reviews/PRD与前端对齐复审-20260830.md（G13/G15 均为"部分完成"，后端就绪仅缺前端）
> 范围：仅前端。禁止改动 backend/ 任何文件。可与 27 号指令并行（不同文件域）。

## 指令正文（复制给 Codex，独立会话）

```plaintext
cd forge-scrm 仓库根目录（Mac 本地实际路径），不新建任何目录

Context:
forge-scrm 二期缺口复审（audit-reviews/PRD与前端对齐复审-20260830.md）确认 G13/G15 为"部分完成"：后端能力就绪，仅缺前端。本轮只补前端。开工前先 git pull origin main。注意：本指令与 27 号指令（G7/G9/G10）可能并行执行，只改本条列出的文件，不碰 CollectionTasks/ResearchTasks/ResearchReport。

Request:
按根目录 UI-DESIGN.md 规范补齐两项前端能力：

1. 【G15 推送记录完整查看】frontend/src/pages/reports/ReportDetail.tsx 推送抽屉（:243-295，现只显示任务状态/重试次数/最新失败文案）：展开后端已返回的 records 明细（backend/app/schemas/report.py:71-106、backend/app/routers/reports.py:189-217），每条展示脱敏目标、摘要、发送时间、状态、错误码/原因、attempt_no。

2. 【G13 报告来源范围配置】frontend/src/pages/reports/ReportList.tsx 新建报告表单（:49-66,159-167，现只有类型和周期）：按报告类型展示来源选择和时间窗，提交 source_config（后端 schema 已支持，见 backend/app/schemas/report.py:19-31；执行器按配置读取分析/采集/原始数据/研究报告/引用，见 backend/app/services/report_executor.py:122-249）。生成前可确认口径。

红线禁止:
- 禁止修改 backend/ 任何文件（发现字段缺失时停下来汇报，不得自造接口）
- 前端不得展示任何"审核/抽查/待审核/转审核"字样或入口（0828+0830 定稿：二期默认通过，审核仅后端预留）。涉审核 UI 一律不实现，在自检报告标注"按审核字样隐藏口径搁置"
- 禁止新增 context 未定义的字段/状态
- 遵守 UI-DESIGN.md：Drawer 720px 高密度详情、间距8px栅格、行内操作禁 primary、Table 必设 emptyText、状态标签用 statusTagColor
- 禁止 commit/push/部署
- 遵守 AGENTS.md 全部工作流铁律（先 pull、最小改动）

Checkpoint:
完成后生成自检报告 check-reports/G13G15报告前端补齐-自检报告-20260830.md（含改动文件清单、每项验证方式、遗留风险），沙箱网络受限则留在本地如实说明，由杰西卡代传管道。
```
