cd ~/个人文件/forge-scrm && git pull origin main

```plaintext
Context:
项目 forge-scrm（Forge 新媒体运营系统），Mac ~/个人文件/forge-scrm，main 分支。本任务是最终审计修复：两天审计（功能+UI）发现的全部问题一次修完。先读 audit-reports/（如无则本指令自带全部上下文）。
审计确认已通过的方面（不要动）：seed 内容不覆盖、R1 校验、28KB 截断、token 失效重试、删除状态机（已推送=凭证不可删）、前端状态渲染、push-config 脱敏、迁移链。

Request:
【P0-1 并发推送竞态】backend/app/routers/reports.py execute_push_task：
- 状态检查+commit 改为条件更新占位：UPDATE report_push_task SET status='推送中' WHERE id=:id AND status IN ('待推送','失败')，rowcount=0 → raise BizError("任务状态已变更，请刷新后操作", code=409)。该占位机制同样应用到 cancel（WHERE status IN ('待推送','失败') SET '已取消'）与 delete（WHERE status != '推送中' AND status != '已推送'），消除竞态。
【P0-2 推送中卡死】execute_push_task 主体：
- 推送循环包 try/except Exception：任何未捕获异常 → 任务置「失败」+ 写 record（error_message=异常摘要）+ commit，不向上抛 500。
- 陈旧复位：execute/cancel 入口检测 status=='推送中' 且 updated_at 距今 >10 分钟 → 视为僵尸，直接置「失败」（error_message='推送超时自动复位'）后允许本次操作继续走正常校验。
【P1-3 快照链断裂】backend/app/services/topic_service.py build_prompt：
- material_combo_snapshot 写入移出 template 条件：凡最终 materials 非空即写快照；每项加 source 字段区分 "template_combo"（来自模板）/ "manual"（用户当场勾选）。
【P1-4 权限闸】backend/app/core/enums.py Permission 增加权限码 "报告推送"；reports.py 的 create/execute/cancel/delete 推送任务接口挂 require_permission（管理员恒有；成员须显式授权，对齐 context/06 §2.2 口径与提示词模板接口现有写法）。push-config 维持 CurrentUser。
【P1-5 重发去重】execute_push_task 多 open_id 循环：
- 重试时读 records 中 status=已推送 的 target_object 集合，这些 open_id 跳过不重发；record 里已有全部成功 → 提示"所有接收人已推送成功"。
【P1-6 open_id 掩码】reports.py 出参（ReportPushTaskOut/ReportPushRecordOut 的 target_object）：
- 序列化前对 open_id 掩码：保留前 6 后 4 字符（ou_234...39d44），中间 ****。注意只在 Out 层掩码，发送逻辑内部仍用完整值。
【P1-7 状态污染】execute_push_task 的 channel/配置校验分支：
- 去掉 task.status = 待推送; commit 回写——校验不通过直接 raise，状态保持原值。
【P2-8 seed 幂等键】backend/app/services/seed.py：
- 幂等判定从 name LIKE '内置%' 改为按 task_type 查"created_by=种子管理员 且 name 以'内置'开头"的第一条；BUILTIN_PROMPTS.get(task_type) 取不到则跳过（log warning），不 KeyError。
【P2-9 token 脱敏窗口】feishu_push.py _safe_msg：
- 维护最近 2 个 token 的列表（当前+上一个），脱敏时两个都替换。
【P2-10 迁移冗余】f2a8c4d91e70 迁移：
- report_push_record 的 ENUM 不扩「待推送/推送中/已取消」——检查迁移文件，若 record 列被扩了这三个值，新增一个后续迁移把 record 枚举收窄回（已推送/失败/待推送）；若 task 扩已取消、record 未动则跳过此项。
【UI-严重 3 项】frontend：
- topics/TopicList.tsx:143 淘汰按钮补 size="small"。
- 行内 primary 改 link：scripts/ScriptList.tsx:144,149（提交审核/标记已使用）、reports/ReportList.tsx:127,130（生成/重试，保留 loading）。
- 裸 danger 加确认：topics/TopicDetail.tsx:55 淘汰、scripts/ScriptDetail.tsx:55,66 驳回/废弃、analysis/AnalysisTaskDetail.tsx:146 驳回 → 包 Modal.confirm（文案"确认淘汰该选题？"/"确认驳回？"等）。
【UI-建议 高性价比】frontend：
- 全站列表补 locale emptyText：抽 src/components/tableEmpty.ts 常量（如"暂无数据"按页定制太散，统一默认"暂无数据，条件不匹配时可调整筛选"），批量给 20 个缺 emptyText 的 Table 加上。
- reports/ReportDetail.tsx:214 Drawer 显式 width={480}。
- materials/MaterialReview.tsx:60 与 MaterialList.tsx:136 的"AI 产物" Tag 统一文案（两处已一致则跳过）。
- Home.tsx:266 待办计数 Tag color="red" 改 <Badge count> 语义。
- 间距归档：Tags.tsx:49 marginBottom 12→16、admin/Users.tsx:201 gap 6→8、AnalysisTaskDetail.tsx:200 paddingLeft 18→16。
【P2-11 推送卡片折叠】backend/app/services/feishu_push.py _report_card + draft/待办-推送卡片折叠优化.md（先读）：
- elements 改为：div(生成时间) → div(摘要800截断) → div(数据概览) → 折叠容器(报告正文, 默认收起)。
- 折叠实现：先查项目 lark_oapi 版本与卡片 JSON 2.0 是否支持 collapsible_panel 组件（查 site-packages lark_oapi 或现有卡片构造代码），支持则用 collapsible_panel（header 行文案"报告正文（点击展开）"）；不支持则降级：正文 div 保留但仅放正文前 500 字 + 末尾提示"正文已折叠展示，完整内容以系统为准"。
- 28KB 截断逻辑保留（对折叠层内正文同样生效）。手机端不引入固定宽度元素。
- 不确定组件支持情况时用降级方案，不臆造组件字段。
【AGENTS.md+UI-DESIGN.md 解耦】：
- 新建 frontend/UI-DESIGN.md，写入完整《前端 UI 标准》（间距8px五档/Drawer 480-640-720 三档/Modal 520-720/状态色板 statusTagColor 唯一来源/按钮层级:primary 每视图≤1+行内禁 primary+danger 只给不可逆+操作列 size=small/空态反馈规则/页面骨架模板）。标准正文全部在此文件维护。
- AGENTS.md 在「工作流程」章节之后只加一小节引用（不复制标准正文）：
  ## 前端 UI 标准
  > 完整规范见 frontend/UI-DESIGN.md（唯一维护处）。要点：视觉 token 唯一来源 theme.ts；间距 8px 栅格五档；行内操作禁 primary；danger 仅限不可逆操作；所有 Table 必设 emptyText。写前端/改样式前必读该文件。
- 单一来源原则：UI 标准以后只改 UI-DESIGN.md，AGENTS.md 只保留指针，两处不得出现重复正文。

Output format:
- 按上面编号逐项修，最小改动。后端 python -m compileall -q backend/app 通过 + 前端 npm run build 通过。
- 若 P2-10 需要新迁移，文件命名规范与现有一致，报告中注明（不执行迁移，部署阶段用户跑）。
- 生成自检报告 check-reports/最终审计修复-自检报告-20260828.md：逐项编号对应对策+改动文件+自检结果+遗留风险。

红线禁止:
- 禁止不执行 git pull 就改文件。
- 禁止修改 context/、prd-docs/、requirements/（AGENTS.md 本次明确允许改）。
- 禁止 commit/push/deploy。
- 禁止把 open_id 完整值/secret/token 写进日志、DB 记录或前端响应（P1-6 修复后前后端均须掩码）。
- 禁止伪造构建结果。

Checkpoint:
完成后必须执行 AI 自检报告管道上传（沙箱 DNS 不解析 pmlophy.com 时按兜底规则：保存 check-reports/ + 如实汇报"未上传管道"，禁止重试超过1次）：
curl --noproxy '*' -X POST "https://pmlophy.com/p/jarvis/file/upload" -H "X-Jarvis-User: ai-reports" -F "file=@check-reports/最终审计修复-自检报告-20260828.md"
最后汇报：逐项编号完成状态、改动文件列表、是否新增迁移、build/compileall 结果、报告文件名与字节数、是否已上传管道。
```

部署阶段用户操作：
- Mac: ./deploy/deploy.sh
- VPS: 若报告注明有新迁移 → cd /opt/forge-scrm/backend && .venv/bin/alembic upgrade head && systemctl restart forge-scrm-api
