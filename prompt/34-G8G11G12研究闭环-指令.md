# 研究助手闭环 G8/G11 + 报告模板 G12 — 开发指令

> 日期：2026-08-31
> 依据：audit-reviews/PRD与前端对齐复审-20260830.md（G8/G11/G12 均为"未做"）
> 范围：研究助手 + 数据报告模块。前后端均可改。与 33 号（采集模块）文件域基本不重叠，可并行；但 G11 与采集结果有关联，若需调用 33号新增接口，标注依赖并按既有接口写。
> 审核红线：前端永不展示审核/抽查字样或入口。G11 的"轻量抽查"按口径搁置 UI，仅后端预留。

## 指令正文（复制给 Codex，独立会话）

```plaintext
cd forge-scrm 仓库根目录（Mac 本地实际路径），不新建任何目录。开工前先 git pull origin main。

Context:
二期缺口批量补齐第二包：研究助手闭环（G8/G11）+ 报告模板（G12），复审报告均判"未做"。本轮前后端均可改。开工前先读复审报告 G8/G11/G12 行与对应 PRD 段落。⚠️表单字段 name 禁用 style/name/title/action 等 HTML 原生属性名。

Request:
按 PRD 补齐三项：

1. 【G8 研究范围的产品化选择】（PRD: 研究助手主PRD.md:84-86,142-147）
   - 前端：ResearchTasks.tsx 新建研究任务的 scope_config 从 JSON 文本框改为结构化表单——来源类型多选（资料库/自动采集结果/外部检索）、资料选择器（多选已有资料）、采集结果选择器（多选，若33号指令已提供列表接口则复用）、检索偏好（关键词/条数）
   - 后端：research_executor 按新 scope_config 结构读取并执行（资料范围过滤、采集结果范围、外部检索参数）；保持旧 JSON 结构的向后兼容（旧任务不炸）
2. 【G11 研究报告下游动作】（PRD: :93-97,153-159,276-282）
   - 后端：新增三个动作接口——①沉淀为资料（报告全文/章节转资料记录，标 AI 产物，遵守 R8）②基于报告生成选题（调既有选题生成服务，预填报告要点）③基于报告生成脚本（调既有脚本生成服务）
   - 前端：ResearchReport.tsx 报告页头部/侧边加动作区：「沉淀为资料」「生成选题」「生成脚本」——各自弹确认（选择目标分类/提示词模板等既有字段），成功后跳转对应详情或列表
   - ⚠️ PRD 中的"轻量抽查"按审核字样隐藏口径搁置 UI，仅后端预留抽查字段，前端不实现任何入口，自检报告标注"按审核口径搁置"
   - ⚠️ 撞到 PRD 待确认映射时：最保守默认实现+自检报告"待拍板项"列出
3. 【G12 报告模板管理/选择】（PRD: 数据报告主PRD.md:94-100,125-131,337-347）
   - 后端：新建 report_template 模型（名称/类型/默认模板标记/模板内容结构）、CRUD 路由；创建报告时 template_id 关联校验；报告生成时按模板组装输出结构（无模板回退现有行为）
   - 前端：新增「报告模板」管理页（列表+新建/编辑 Drawer+设默认+启停用，参考既有提示词模板管理页结构）；ReportList 新建报告弹窗增加模板选择（按报告类型过滤，默认模板预选）
   - UI 遵守 UI-DESIGN.md 与既有页面骨架；侧栏菜单加入数据分析分组下
   - 注意：模型/迁移走 alembic（新增 migration），禁止手改数据库

红线禁止:
- 前端不得展示任何"审核/抽查/待审核/转审核"字样或入口
- 禁止新造 context 未定义的字段/状态/枚举；表单 name 禁用 style/name/title/action 等 HTML 原生属性名
- 遵守 UI-DESIGN.md；后端新接口走既有路由风格与鉴权
- 禁止 commit/push/部署
- 遵守 AGENTS.md 工作流铁律

Checkpoint:
完成后生成自检报告 check-reports/G8G11G12研究闭环-自检报告-20260831.md，含：
1. 改动文件清单（前端/后端分列）
2. 新增接口与模型/迁移清单
3. 与33号指令（采集模块）的依赖说明（若引用了采集结果接口，标注"需33号先部署"）
4. G11 搁置项与 G8/G11 待拍板项列表
5. npm run build + tsc + 后端 compileall + alembic 迁移可执行证明
沙箱网络受限则报告留本地，由杰西卡代传管道。
```
