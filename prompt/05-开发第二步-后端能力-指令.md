Context（背景）
你在 /Users/liulongfei/个人文件/forge-scrm 仓库工作（Forge 新媒体运营系统）。
动手前先 git pull origin main，当前最新 HEAD 为 2e01047（开发第一步后端骨架已完成并通过审查）。
第一步已交付：7 实体模型 + Alembic 迁移 + CRUD 路由（执行/重试为 501 占位）。
本次实现真实能力：采集执行器 + 研究检索（Tavily），把 501 占位落地。
文档权威：context/ > templates/ > prd-docs/。context/ 只读。

现有结构（第一步产物）：
- backend/app/models/phase2.py（7 实体 + 状态枚举）
- backend/app/routers/phase2.py（CRUD + 501 占位：execute/retry）
- backend/app/schemas/phase2.py
- backend/app/services/ 一期业务逻辑目录

权威依据（必读）：
- prd-docs/modules/自动采集/自动采集主PRD.md + 自动采集_业务规则规格.md（AC-R1~R12）
- prd-docs/modules/研究助手/研究助手主PRD.md + 研究助手_业务规则规格.md（RS-R1~R7）
- context/05-术语与字段口径.md（字段 SSOT）

Request（任务）
第二步：后端能力落地——采集执行器 + 研究检索（Tavily）

1. 采集执行器（backend/app/services/collection_executor.py 或同层）：
   - 实现 collection-tasks/{id}/execute：校验任务（存在/状态 pending 或 failed）→ 状态置 running → 按 scope_config 执行采集 → 写 collection_record + collection_result → 状态流转 success/partial_success/failed → 记录 total/success/failure 计数和 error_message
   - 采集数据源：实现"对标账号公开接口适配器"骨架（benchmark_account 的平台+账号标识 → 拉取内容），真实平台 API（视频号/小红书）未实测的部分：用适配器接口 + 清晰 TODO(待实测)，提供一个可配置的通用 HTTP 拉取实现（fetch 公开 URL/JSON），不伪造假数据
   - 记录级失败：单账号失败不影响整体，标记 record failed + retryable，任务最终 partial_success；全部失败才 failed
   - 重试：collection-tasks/{id}/retry 复用 execute 逻辑，retry_count+1
   - 幂等：同一任务重复 execute 直接拒绝（状态非 pending/failed 时 409）
   - 执行方式：FastAPI BackgroundTasks 异步执行（接口立即返回 202/200 + 任务状态 running），或同步执行但说明取舍——以一期代码风格为准

2. 研究检索（backend/app/services/research_executor.py 或同层）：
   - 实现 research-tasks/{id}/execute：状态置 searching → 调用检索 → 状态 organizing → 生成研究报告（标题/摘要/正文/结论，AI 生成部分调用 DeepSeek API，标注 is_ai_product=true）→ 状态 success；失败置 failed + 错误信息
   - 检索客户端：Tavily API 客户端（key 从环境变量 TAVILY_API_KEY 读取，未配置时明确报错提示），search 结果映射为 research_reference（source_kind=external_url，source_url/source_title/search_provider=tavily）
   - 供应商不锁定：检索客户端抽象接口，Tavily 为实现，DS 官方搜索后续可替换（接口注释说明）
   - 检索结果聚合进报告引用；引用缺失时报告需显式标注（对齐 RS 规则：来源先于结论）
   - 重试：research-tasks/{id}/retry 复用 execute

3. DeepSeek API 调用（报告生成）：
   - 用一期已有 AI 调用封装（如 app/services/ 里的 ai_client 或 utils），复用现有 key 配置
   - 不新增依赖（如确需，说明理由）

4. 测试：
   - 后端启动冒烟（uvicorn 或 TestClient）：execute/retry 不再 501，返回正确状态流转
   - 无 TAVILY_API_KEY 时检索报错信息清晰
   - 幂等/状态校验（重复 execute 409）测试

Output format
1) 新增/修改文件清单；2) 每个文件关键改动；3) 测试结果（状态流转/幂等/无 key 报错）；4) alembic 无新增迁移（模型未变则说明）

红线
- 只改 backend/，禁止碰 frontend/、context/、prd-docs/、requirements/
- 禁止伪造采集/检索数据；未实测平台 API 用适配器骨架 + TODO(待实测)
- 禁止把 key/token 写进代码或提交（TAVILY_API_KEY 只读环境变量）
- 禁止 git commit/push；改完 git status 确认
- 不修改第一步已交付的模型结构（如确需改，说明理由并附迁移）

Checkpoint
a) execute/retry 真实实现且状态流转正确；b) 无 key 时错误清晰；c) 重复 execute 409；d) 未动受保护目录；e) 自检报告保存 check-reports/ 并如实汇报上传状态
