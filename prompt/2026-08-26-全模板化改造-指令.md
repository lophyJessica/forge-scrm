Context（背景）
你在 /Users/liulongfei/个人文件/forge-scrm 仓库工作（Forge 新媒体运营系统）。
动手前先 git pull origin main，当前最新 HEAD 为 0dc1e9d（内置模板下拉展示修复指令）。
任务（用户拍板，产品演进：取消内置 → 全模板化）：
1. 自定义提示词支持一键保存为提示词模板
2. 取消"内置默认提示词"概念：内置内容 seed 进模板表，全部走模板管理（可编辑/可删）
3. 模板删除保护：每个 task_type 至少保留一条模板（删最后一条禁止）+ 删除二次确认
4. 生成页 Radio「内置模板」改名为「模板库」
已确认决策（不必再问）：A=删除保护要+二次确认；B=文案改"模板库"。

现状（已查证）：
- 内置常量：topic_service.DEFAULT_SYSTEM_PROMPT（选题）、script_service.DEFAULT_SCRIPT_SYSTEM_PROMPT（脚本）
- 模板表 PromptTemplate（name/task_type/content/version；无 description/updated_at 字段）
- 模板 API：GET/POST /api/prompt-templates、PUT/DELETE /{template_id}（PUT 时 version+1）
- builtin 接口：GET /api/prompt-templates/builtin 返回代码常量（{task_type, content} 列表）
- 生成：POST /api/topics/generate、/api/scripts/generate → build_prompt(db, direction..., template_id, prompt_content)
- 生成页：topics/TopicGenerate.tsx + scripts/ScriptGenerate.tsx 已有 Radio（内置模板/自定义）+ builtin 下拉 + 自定义 TextArea
- 管理页：admin/PromptTemplates.tsx 顶部有"内置默认提示词"只读卡片区 + 模板表格（新建/编辑/删除 Popconfirm）

Request（任务）
全模板化改造：

1. 后端 seed（Alembic 数据迁移）：
   - 新增迁移文件：把两条内置提示词插入 prompt_template 表
     （task_type=选题生成，name=内置选题提示词，content=DEFAULT_SYSTEM_PROMPT 全文）
     （task_type=脚本生成，name=内置脚本提示词，content=DEFAULT_SCRIPT_SYSTEM_PROMPT 全文）
     version=1；幂等（已存在同名+task_type 则跳过）
   - 迁移放 backend/alembic/versions/，upgrade 插入、downgrade 删除这两行

2. 后端生成逻辑（topic_service/script_service build_prompt）：
   - template_id 为空时：按 task_type 查模板表【第一条】作为默认模板（替代内置常量）
   - 模板表为空（异常兜底）：回退 DEFAULT_*_SYSTEM_PROMPT（不报错）
   - prompt_content > template_id > 默认模板(表内第一条) > 内置常量回退
   - 快照记录：默认模板时 snapshot 记录 {template_id, name, version}（沿用现有结构）

3. 后端 builtin 接口改造（routers/prompts.py）：
   - GET /api/prompt-templates/builtin 改为：返回模板表中各 task_type 的第一条模板
     （结构保持 [{task_type, content}]，前端无需大改；内容=该类型默认模板全文）
   - 模板表空时回退代码常量（兼容）

4. 后端删除保护（routers/prompts.py DELETE /{template_id}）：
   - 删除前校验：该模板的 task_type 下剩余模板数 >= 1（即不能删掉该类型的最后一条）
   - 违反时返回 400，detail 明确"该任务类型至少需要保留一条模板"
   - 正常删除逻辑不变

5. 前端管理页（admin/PromptTemplates.tsx）：
   - 删除"内置默认提示词"只读卡片区（该区内容已进模板表，可直接在表格编辑/删除）
   - 表格操作列删除按钮：保留 Popconfirm 二次确认，文案写明"删除后不可恢复"+ 若为最后一条会提示后端 400 错误（antd message 展示后端 detail）

6. 前端生成页（topics/TopicGenerate.tsx + scripts/ScriptGenerate.tsx）：
   - Radio 文案「内置模板」→「模板库」（value 保持 builtin 或改 template_lib，内部一致即可）
   - 「模板库」下拉：选项来自 builtin 接口（已改=模板表默认模板）
     * 下拉选项【完整展示】内置/模板内容——label 用 `${task_type}：${content 原文}`，不允许 slice 截断；option 文本允许换行（antd 默认单行截断时给 option 加样式 whiteSpace:'normal'，或自定义 optionRender 渲染多行容器）
     * 收起态（Select 收起后）显示 `${task_type}` 短名 + Tooltip 悬浮显示完整 content（antd Tooltip 包裹 Select 或 optionLabelProp 控制）
     * 两个生成页行为一致
   - 自定义模式 TextArea 下方加「保存为模板」按钮：
     * 点击弹 Modal：名称输入框（默认"自定义{选题|脚本}提示词"）+ 确认
     * 调 POST /api/prompt-templates（task_type=当前业务类型，content=TextArea 内容，name=输入值）
     * 成功 message 提示"已保存到模板库"，并可选刷新模板列表
     * TextArea 为空时按钮禁用或提示
   - 提交生成时：模板库模式不传 prompt_template_id（走默认模板）；自定义模式传 prompt_content（不变）

7. 验证：
   - alembic upgrade head 成功且幂等（重复执行无新增）
   - TestClient：默认模板生成 200（用 seed 的模板）；删最后一条模板 400；builtin 返回模板表内容；保存模板 200
   - npm run build 通过

Output format
1) 新增/修改文件清单；2) 关键改动；3) 迁移+测试+build 结果

红线
- 只改 backend/app/{routers/prompts.py, services/{topic,script}_service.py} + backend/alembic/versions/ 新迁移 + frontend/src/pages/{admin/PromptTemplates.tsx, topics/TopicGenerate.tsx, scripts/ScriptGenerate.tsx}
- context/ 只读；禁止 git commit/push；不写 key
- 内置常量保留（作为空库兜底），不删 DEFAULT_*_SYSTEM_PROMPT 定义
- 迁移必须幂等

Checkpoint
a) seed 迁移建好且幂等；b) 默认模板逻辑生效（模板表优先，空库回退内置）；c) 删最后一条 400；d) builtin 返回模板表内容；e) 保存为模板按钮可用；f) Radio 文案"模板库"；g) build+测试通过；h) 自检报告存 check-reports/ 并如实汇报上传状态
