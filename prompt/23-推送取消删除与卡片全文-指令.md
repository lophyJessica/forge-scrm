cd ~/个人文件/forge-scrm && git pull origin main

```plaintext
Context:
项目 forge-scrm（Forge 新媒体运营系统），仓库位于 Mac ~/个人文件/forge-scrm，工作分支 main。飞书推送已上线实测通过（open_id 直发卡片成功）。本任务两个子任务：①推送任务取消/删除；②卡片飞书内闭环展示全文。PRD：prd-docs/modules/推送任务取消删除与卡片跳转PRD.md（先读，B 方案：不跳转系统，全文在飞书内读）。
现状：
- 推送状态机 ReportPushStatus 在 backend/app/models/report.py 约57行：待推送/推送中/已推送/失败，无取消/删除。
- 推送接口在 backend/app/routers/reports.py（create/execute/list 已有）；卡片组装在 backend/app/services/feishu_push.py 的 _report_card()（约131行，elements 只有生成时间+摘要两个 div）。
- 前端抽屉在 frontend/src/pages/reports/ReportDetail.tsx（Drawer 内任务列表，actions 按 status 条件渲染）。
- Report 模体的正文字段以 backend/app/models/report.py 中 Report 类实际字段为准（先读再写）。

Request:
1. 任务一 取消/删除（后端 reports.py + models/report.py）：
   a) ReportPushStatus 增加「已取消」枚举值；检查该字段落库类型（enum_type 包装），若为 native DB enum 需新增 alembic 迁移 ALTER 加入枚举值；若 varchar 直接可用。以最小方案执行。
   b) POST /api/reports/push-tasks/{task_id}/cancel：仅待推送/失败可取消，置已取消；推送中/已推送 → BizError。
   c) DELETE /api/reports/push-tasks/{task_id}：推送中 → BizError("推送执行中，请稍后再试")；其余状态删任务+关联 records。
   d) 权限对齐现有推送接口。
2. 任务一 前端（ReportDetail.tsx 抽屉）：
   a) actions 按 status：待推送/失败 → [发送][取消]；已推送/已取消 → [删除]。
   b) 取消直接调用不二次确认；删除用 Modal.confirm「确认删除该推送任务？删除后不可恢复（已推送的飞书消息不会撤回）」。
   c) 已取消 Tag 灰色；成功/失败操作后刷新列表。
3. 任务二 卡片全文闭环（feishu_push.py + reports.py）：
   a) send_report_card 增加 report_body 参数；_report_card 重构：
      - header 保留（标题）
      - elements 第一组：生成时间 + 摘要（截断800）+ 数据概览行（report 实体来源数量等字段，有则展示）
      - elements 第二组：正文 lark_md（md 轻量转换：## → **粗体**，表格行转列表；不做复杂解析器）；正文整体截断至卡片 JSON 总体积 < 28KB（超限截断并在末尾加一行「正文超长已截断，完整内容请登录系统查看」）
      - 若飞书卡片支持折叠组件（collapsible panel）则正文放折叠层默认收起；不支持则平铺。先查项目内 lark_oapi 版本与卡片组件常量，拿不准就用平铺+截断兜底，不臆造组件。
   b) reports.py execute_push_task 调用处传 report 正文字段（字段名以 Report 模型为准）。
4. 禁止扩界：不做跳转链接/分享 token、不做消息撤回、不做批量操作、不做微信、不动 context/ 与 prd-docs/；数据库仅允许任务一 a) 所需的最小迁移（若必须）。

Output format:
- 最小改动。完成后 python -m compileall -q backend/app 通过 + npm run build 通过（前端有改动）。
- 若新增 alembic 迁移，报告里注明迁移文件名，且不执行迁移（部署阶段由用户跑）。
- 生成自检报告 check-reports/推送取消删除与卡片全文-自检报告-20260828.md。

红线禁止:
- 禁止不执行 git pull 就改文件。
- 禁止修改 context/、prd-docs/、requirements/。
- 禁止 commit/push/deploy。
- 禁止把 open_id/secret 写进日志/记录。
- 禁止伪造构建结果。

Checkpoint:
完成后必须执行 AI 自检报告管道上传（沙箱 DNS 不解析 pmlophy.com 时按兜底规则：保存 check-reports/ + 如实汇报"未上传管道"，禁止重试超过1次）：
curl --noproxy '*' -X POST "https://pmlophy.com/p/jarvis/file/upload" -H "X-Jarvis-User: ai-reports" -F "file=@check-reports/推送取消删除与卡片全文-自检报告-20260828.md"
最后汇报：改动文件列表、是否新增迁移、build/compileall 结果、报告文件名与字节数、是否已上传管道。
```

部署阶段用户操作（若报告注明有迁移）：
ssh 后 cd /opt/forge-scrm/backend && .venv/bin/alembic upgrade head && systemctl restart forge-scrm-api
