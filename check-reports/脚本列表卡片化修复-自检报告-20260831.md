# 脚本列表卡片化修复自检报告（杰西卡转录）

> 说明：Codex 产出的原报告在 Mac 本地未入库（文件未随 commit push），本文件为杰西卡依据用户转发的报告原文转录存档。curl 上传失败（EXIT 26 读文件失败，原文件不在 VPS），故转录后上传。

- 项目：forge-scrm
- 任务：定位并修复构建产物中 /scripts 未呈现 FILTER_CARD_STYLE 的问题
- 日期：2026-08-31
- 约束：未修改 backend/，未 commit/push/部署

## 根因排查
1. 重复源码入口：rg 扫描仅 frontend/src/pages/scripts/ScriptList.tsx 一份，无第二副本；App.tsx 静态 import + /scripts 路由，无懒加载。
2. 构建缓存：清理 frontend/dist、node_modules/.vite、*.tsbuildinfo 后重新构建，产物仍为 index-DNe43-g0.js（单 chunk），文件名稳定。
3. 源码核对：ScriptList.tsx:65 已用 style={FILTER_CARD_STYLE}；theme.ts:7-13 定义正常。

## 结论
源码与构建产物均正确。线上 bundle 内脚本列表模块实际包含 style:Ji 引用（Ji={background:"#fafafa",border:"1px solid #f0f0f0",borderRadius:8,padding:16,marginBottom:24}，Rollup 将共享 token 提升至 chunk 前部 1415052 位置）。此前"字面量不在关键词附近=缺失"的判断为排查方法论错误，样式引用存在。

## 本地 dist 验证证据
- dist/assets/index-DNe43-g0.js 含"正文关键词"×3（资料列表/脚本列表/模板引用组件）
- 脚本列表命中（位置1450432）窗口内 style:Ji=true；资料列表（1424358）同样 style:Ji=true
- Ji 定义在 1415052，与使用点同 chunk——共享 token 压缩提升的正常形态

## 构建
- npm run build 通过；tsc -b 通过；git diff --check 通过

## 判定
线上/构建产物无问题，ego 实测所见 style 缺失最可能为浏览器/CDN 缓存拿旧资源。处置：部署最新 dist（已重建）+ 浏览器无痕/强刷复测 /scripts；若仍复现，检查 nginx 静态资源缓存头与 CF 缓存策略。

## 遗留
- 本轮无业务代码改动；部署由用户执行 deploy.sh
