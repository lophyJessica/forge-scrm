# 详情页返回按钮修复自检报告

## 任务与项目

- 项目：Forge 新媒体运营系统
- 任务：为缺少明确返回入口的详情页补充返回按钮，并全量检查详情、历史和快照类页面。
- 工作区：`/Users/liulongfei/个人文件/forge-scrm`
- 约束：仅修改 `frontend/src/pages/` 页面 JSX 及本报告；未修改后端、API、路由定义或业务逻辑。
- Git：未执行 commit 或 push。

## 修改文件清单

1. `frontend/src/pages/scripts/ScriptDetail.tsx`
   - 引入 `ArrowLeftOutlined`。
   - 在脚本详情 Card 标题区右侧操作栏首位增加“返回列表”按钮。
   - 点击后明确导航到 `/scripts`。
2. `frontend/src/pages/topics/TopicDetail.tsx`
   - 引入 `ArrowLeftOutlined`。
   - 在选题详情 Card 标题区右侧操作栏首位增加“返回列表”按钮。
   - 点击后明确导航到 `/topics`。
3. `frontend/src/pages/research/ResearchReport.tsx`
   - 引入 `useNavigate` 和 `ArrowLeftOutlined`。
   - 在研究报告标题行左侧增加“返回列表”按钮。
   - 点击后明确导航到 `/research/tasks`。

## 全量自查

扫描了 `frontend/src/App.tsx` 中所有带 `:id` 的路由及对应页面：

- `/materials/:id` → `MaterialForm.tsx`：已有“取消”，明确回到 `/materials`。
- `/topics/:id` → `TopicDetail.tsx`：本次新增“返回列表”，回到 `/topics`。
- `/topics/:id/edit` → `TopicForm.tsx`：已有“取消”返回上一级。
- `/scripts/:id` → `ScriptDetail.tsx`：本次新增“返回列表”，回到 `/scripts`。
- `/scripts/:id/edit` → `ScriptForm.tsx`：已有“取消”返回上一级。
- `/scripts/:id/versions` → `ScriptVersions.tsx`：已有“返回脚本”，回到当前脚本详情。
- `/analysis/tasks/:id` → `AnalysisTaskDetail.tsx`：已有“返回列表”，回到 `/analysis/tasks`。
- `/reports/:id` → `ReportDetail.tsx`：已有“返回列表”，回到 `/reports`。
- `/research/reports/:id` → `ResearchReport.tsx`：本次新增“返回列表”，回到 `/research/tasks`。

其他审核页（资料审核、脚本审核）是列表/队列页面，不是带详情参数的独立详情页；采集任务记录/结果是列表页内展开区域；资料列表详情是 Modal，均未新增重复返回入口。

## 验证结果

- `npm run build`：通过。
- `git diff --check`：通过。
- 返回目标静态检查：3 个新增按钮均使用明确路径导航，不依赖浏览器 history。
- 按钮样式：沿用 antd `Button`，使用 `ArrowLeftOutlined`，位于标题区操作区域或标题行。
- 未修改页面字段、数据请求、业务操作、审核逻辑和路由定义。
- 报告上传：已执行规定的上传命令；当前环境 DNS 无法解析 `pmlophy.com`，curl 返回 exit code 6（`Could not resolve host`），因此未上传成功，未重复尝试。

## 遗留风险

- 本轮未进行线上浏览器点击验收；构建和源码级路由目标检查通过。
- 编辑页原有“取消”按钮在部分页面使用 `navigate(-1)`，本轮保持现状以遵守最小改动范围；独立详情页新增按钮均使用固定列表路径。
