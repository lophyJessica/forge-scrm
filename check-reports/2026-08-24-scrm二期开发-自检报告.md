# 2026-08-24 forge-scrm 二期开发 · AI 自检报告

- 日期：2026-08-24
- 任务：forge-scrm（Forge 新媒体运营系统）一期系统前后端编码实现 · 第二阶段
- 执行者：Claude（Opus 5）

## 一、任务摘要

按第一阶段确认的《开发方案》与已裁决的技术决策（D-T1 同步执行 / D-T2 固定模板 CSV / D-T3 惰性过期 /
D-T4 本地磁盘存储 / D-T5 JWT 24h 无 refresh），从零实现一期后端与前端，共 8 个步骤全部完成：

1. 后端基础设施（配置、数据库、日志、异常、鉴权、枚举、Alembic 初始迁移）
2. 认证与权限（模块 05：登录/登出/改密/账号管理/功能权限/数据范围）
3. 资料库（模块 01：状态机、审核、标签、分类、搜索、组合引用、CSV 导入、惰性过期）
4. 选题库（模块 02：批量生成、跨批次去重、批次留痕、人工筛选、手动新增）
5. 脚本库（模块 03：基于选题生成、独立创建、版本链、diff、回退、审核、标记已使用）
6. 数据分析（模块 04：数据源、原始数据、分析任务同步执行、结果审核、回写资料/反哺选题、提示词模板）
7. 前端 9 个页面组、25 个页面组件、28 条路由
8. MVP Must 45 条本地自测 + README + 完成报告

## 二、实现统计

| 项 | 数量 | 说明 |
|---|---|---|
| 后端 API 端点 | 85 | 另有 4 个 FastAPI 自带文档端点 |
| 数据库表 | 17 | 与《核心字段清单》MVP 必建表逐张对齐 |
| 后端 Python 文件 | 45 | core / models / schemas / services / routers / utils |
| 前端路由 | 28 | 含根路径重定向与兜底 |
| 前端页面组件 | 25 | 分属 9 个页面组 |
| 前端 TS/TSX 文件 | 33 | — |
| 前端构建 | 通过 | vite 6.4.3，3117 modules，1,439.87 kB / gzip 451.54 kB |

## 三、MVP 验收清单 Must 45 条自测结果

自测脚本：`backend/scripts/selftest_mvp.py`（独立临时 SQLite 库、DeepSeek 打桩），
明细见 `backend/scripts/selftest_result.md`。

**通过 43 / 未通过 0 / 需 VPS 环境 2。**

| 模块 | 条目 | 结果 |
|---|---|---|
| 资料库 | M01-M08（8 条） | 全部通过 |
| 选题库 | M10-M15、M40（7 条） | 全部通过 |
| 脚本库 | M16-M22、M39（8 条） | 全部通过 |
| 数据分析 | M23-M31（9 条） | 全部通过 |
| 权限账号 | M32-M38（7 条） | 全部通过 |
| 系统 | S01、S03、S04、S05（4 条） | 全部通过 |
| 系统 | S02（服务自恢复）、S06（4C+4G 资源观察） | 需 VPS 环境，本地不可验证 |

M09（自动采集架构预留）为架构预留项、不计入 Must 45：`data_source` 表与 `collection_method`
（手动录入 / CSV 导入 / 自动采集）已建，**未实现任何采集逻辑**，符合一期范围。

本轮修复的 2 个自测暴露问题：

- **未选提示词模板时不落提示词快照**（影响 M25/S04 追溯完整性）：
  `analysis_service` / `topic_service` / `script_service` 的 `build_prompt` 现在在使用内置提示词时
  也写入 `prompt_version_snapshot`（`template_id=null` + 内置内容快照），未新增任何字段。
- **前端分析页状态枚举写错**：`AnalysisTasks.tsx` / `AnalysisTaskDetail.tsx` 用了不存在的「待确认」，
  已改为后端实际的「待审核」；同时修正执行失败路径（后端返回 400，改为由拦截器提示并重载任务）。

## 四、红线遵守自检

| 红线 | 结果 |
|---|---|
| 不修改 context/ requirements/ draft/ templates/ | ✅ 未改动 |
| 不修改 AGENTS.md / CLAUDE.md | ✅ 未改动 |
| 不硬编码 DeepSeek key / 任何密码 | ✅ 全部走环境变量；S05 已用正则扫描全部源码确认无 key 字面量 |
| `.env` 被 .gitignore 排除 | ✅ 已排除，仅保留 `.env.example` |
| 不实现自动采集 / 定时任务 / 语义去重 / 扫码登录 / 选题版本表 / 深度分析指标 / 对象存储 | ✅ 均未实现，仅按文档预留结构 |
| 不新增文档中不存在的枚举/状态/字段 | ⚠️ 1 处标注待确认（见第五节），未擅自定稿 |
| 不 git commit / push | ✅ 代码全部留在本地工作区 |
| 不部署 / rsync / curl 到 VPS | ✅ 未执行任何部署动作 |
| 密码 bcrypt、JWT secret 走环境变量 | ✅ 库中仅存 `$2b$` 哈希，接口与日志均无明文 |

## 五、自检结论

一期功能范围内的四大业务模块 + 权限账号已全部编码完成并本地跑通，MVP Must 清单在本地可验证的
43 条全部通过，后端可正常启动、前端构建通过。代码未提交、未部署，等待调研者确认。

## 六、遗留风险

1. **DeepSeek 真实连通性未验证（重要）**：`.env` 中 `DEEPSEEK_API_KEY` 为空，所有 AI 链路以打桩方式
   验证了提示词组装、结构化校验、失败重试、原始响应留档、状态流转的完整代码路径，**未打过真实 API**。
   拿到有效 key 后需真机联调，重点看真实返回是否稳定满足 JSON schema、单次耗时在同步执行下是否可接受。
2. **需人工确认 1 项**：`analysis_task.task_type` 取值（资料分析 / 数据分析）。context/05 §4.7 未枚举
   分析任务类型，当前为按上下文推定的最小集合，代码内已标注 `TODO 需人工确认`。
3. **S02 / S06 需 VPS 环境**：服务自恢复守护与 4C+4G 连续 60 分钟资源观察，待部署通道打通后执行。
4. **同步执行体感**：D-T1 已确认一期同步执行；后端 DeepSeek 单次超时 120s + 3 次指数退避，前端 axios
   超时 300s，资料上下文较多时前端 loading 可能较久。实测不佳则二期上异步队列。
5. **前端首包 1.44 MB（gzip 452 kB）**：antd 全量引入所致，低并发内网可接受；如需优化可做路由级懒加载。
6. **CSV 导入为固定模板**（D-T2）：列名不匹配整体拒绝并提示；若实际导出文件列名多变，二期做映射配置。
