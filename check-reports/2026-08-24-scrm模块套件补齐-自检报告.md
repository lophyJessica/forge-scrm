# forge-scrm 模块 PRD 套件补齐自检报告

## 1. 任务摘要

本次任务按 `prd-docs/modules/选题库/` 样板，为资料库、脚本库、数据分析、权限账号补齐模块 PRD 套件。字段、状态、枚举和权限口径以 `context/01-06` 及 `prd-docs/核心字段清单.md` 为准；Demo 文档逐页对照 `frontend/src/pages/` 与 `frontend/src/App.tsx` 的现有实现编写。

未修改 `context/`、`draft/`、`requirements/`、`templates/`、`AGENTS.md`、`CLAUDE.md`、选题库样板、首页 PRD、主 PRD、核心字段清单和 MVP 验收清单；未 commit、push 或部署。

## 2. 文档清单与计数

| 模块 | 文档数 | 必备规则/推演 | Demo 覆盖 |
|---|---:|---|---|
| 资料库 | 9 | 主 PRD、字段清单、业务规则规格、业务流程推演、用例数据推演 | 列表、详情、新增编辑、导入/审核/分类/标签 |
| 脚本库 | 8 | 主 PRD、字段清单、业务规则规格、用例数据推演 | 列表、生成、详情/版本、独立创建/审核 |
| 数据分析 | 9 | 主 PRD、字段清单、业务规则规格、业务流程推演、用例数据推演 | 任务列表、新建任务、结果审核、数据源/原始数据/提示词 |
| 权限账号 | 6 | 主 PRD、字段清单、业务规则规格 | 成员列表、成员编辑、登录/个人账号/主框架 |
| **合计** | **32** | 3 个有状态机模块均含规则与用例推演；权限账号按管理类简配 | 覆盖四模块现有页面 |

另更新 `prd-docs/README.md` 模块索引和套件计数。

## 3. 前端页面对照

- 资料库：`MaterialList.tsx`、`MaterialForm.tsx`、`MaterialImport.tsx`、`MaterialReview.tsx`、`MaterialClasses.tsx`、`Tags.tsx`。
- 脚本库：`ScriptList.tsx`、`ScriptGenerate.tsx`、`ScriptForm.tsx`、`ScriptDetail.tsx`、`ScriptVersions.tsx`、`ScriptReview.tsx`。
- 数据分析：`DataSources.tsx`、`RawData.tsx`、`AnalysisTasks.tsx`、`AnalysisTaskDetail.tsx`、`PromptTemplates.tsx`。
- 权限账号：`Login.tsx`、`Profile.tsx`、`admin/Users.tsx`、`MainLayout.tsx`。

页面文档明确区分“当前前端实现”和“SSOT 业务口径”；前端使用但尚未在 context 确认的字段（如资料分类排序、数据源启停、分析任务名称/重试次数、脚本修改备注、最后登录时间）均标注“新增建议，待确认”。

## 4. 自检结论

- 必备文件检查：通过。资料库 9/9、脚本库 8/8、数据分析 9/9、权限账号 6/6 已生成。
- 状态机检查：通过。资料、脚本、分析任务分别引用 context 已确认状态；脚本版本历史与人工“已使用”标记已覆盖。
- 字段检查：通过。字段清单以 context/05 和核心字段清单为来源；前端扩展字段未确认前未写成强制业务字段。
- 前端对照检查：通过。四模块现有页面组件均有对应 Demo 规格；未凭空增加页面。
- Markdown 静态检查：通过。新文档代码围栏成对、无行尾空白；`git diff --check` 通过。
- 受项目删除确认守则约束，四个旧版单文件尚未删除，待用户明确确认后再执行删除；这不影响新套件内容的完整性。

## 5. 遗留风险

1. 旧单文件 `01-资料库PRD.md`、`03-脚本库PRD.md`、`04-数据分析PRD.md`、`05-权限与账号PRD.md` 与新目录并存，存在误读旧口径的风险；需用户确认后删除。
2. context 中仍标为待确认的成员动作权限、资料/提示词版本实体、登录失败锁定阈值及部分技术扩展字段，在模块文档中保留“待确认”，未自行定规则。
3. 部分前端页面已有超出 SSOT 的展示或动作（例如数据源启停、脚本已通过后的废弃按钮），Demo 文档已记录为实现差异/遗留风险，开发时应以 context 为准。
