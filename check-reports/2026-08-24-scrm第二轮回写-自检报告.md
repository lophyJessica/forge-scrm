# forge-scrm 第二轮确认回写自检报告

> 日期：2026-08-24  
> 项目：姐夫新媒体运营系统（forge-scrm）  
> 基线：main 分支 `c7e7055`（执行修改前已 `git pull origin main`，结果为已是最新）

## 1. 任务摘要

将 `draft/待确认问题-模块PRD补充-20260824.md` 中已定稿的第二轮 9 项回复，按 D1-D9 回写到 `context/` SSOT、主 PRD、5 个模块 PRD、核心字段清单和 MVP 验收清单，并同步规则摘要与目录索引。未修改 `requirements/`、`templates/`、`CLAUDE.md` 或 `.cursor/`，未执行 commit、push 或部署。

## 2. D1-D9 落位统计

| 决策 | 回写结论 | 主要落位 | 结果 |
|---|---|---|---|
| D1 | 标签自由创建，标签组可选且不强制分组 | context/01/02/03/04/05；模块01；主PRD；核心字段清单 | ✅ |
| D2 | 自动采集未来启用时人工触发，一期不做定时任务 | context/01/02/04/06；模块01/04；主PRD | ✅ |
| D3 | 一期仅完全重复去重，语义去重二期 | context/01/02/04；模块02；主PRD；MVP 清单 | ✅ |
| D4 | 脚本“已使用”为正式状态，由人工从“已通过”标记 | context/01/02/03/04/05；模块03；主PRD；核心字段清单；MVP 清单 | ✅ |
| D5 | 自动采集仅架构预留，MVP 以手动录入 + CSV/TXT 导入为主 | context/01-06；模块01/04；主PRD；核心字段清单；MVP 清单 | ✅ |
| D6 | 一期仅基础分析框架，具体指标与深度分析二期 | context/01-06；模块04；主PRD；核心字段清单；MVP 清单 | ✅ |
| D7 | 一期仅账号密码登录，扫码/验证码二期 | context/01/04/05/06；模块05；主PRD；核心字段清单；MVP 清单 | ✅ |
| D8 | 选题可人工修改；一期不建独立版本表、不保留版本历史 | context/01/03/04/06；模块02；主PRD；核心字段清单；MVP 清单 | ✅ |
| D9 | 成员数量不设业务上限，按系统容量控制 | context/01/04/06；模块05；主PRD | ✅ |

## 3. 改动文件数

- 既有业务文档：17 个文件。
- 自检报告：1 个新增文件。
- 本次工作区总计：18 个文件。

既有文件包括：`AGENTS.md`、context 六件套、`context/README.md`、`prd-docs/README.md`、主 PRD、核心字段清单、MVP 验收清单及 5 个模块 PRD。

## 4. 自检结论

- ✅ D1-D9 均已进入 SSOT，并同步至相关 PRD 与验收文档。
- ✅ R1-R11 核心规则未删除或推翻；新增 R12-R20 映射 D1-D9，R4 仅按 D3 收紧去重边界。
- ✅ 脚本状态统一为待审核/已通过/已使用/已废弃，草稿为派生；已使用为人工标记正式状态。
- ✅ 自动采集已移出 MVP Must；M09 位于架构预留区，不计入通过条件。
- ✅ 手动录入和 CSV/TXT 导入保留在 Must；脚本已使用为 M39；选题人工修改为 M40。
- ✅ MVP 用例编号 M01-M40 无缺号、无重复；其中 M09 单列架构预留。Must 为 39 条 M 用例 + 6 条 S 用例，共 45 条；另有 1 条架构预留。
- ✅ 5 个模块 PRD 均保留模板要求的 9 个章节。
- ✅ Markdown 表格列数检查无异常，`git diff --check` 通过。
- ✅ `requirements/`、`templates/`、`CLAUDE.md`、`.cursor/` 无改动；未 commit/push/deploy。

## 5. 遗留风险

- 二期具体账号指标字段（问卷 5.5/5.6）与优先分析类型（5.15）仍待确认，已明确不阻塞一期。
- 资料版本、独立提示词版本以及部分标记为“新增建议，待确认”的技术字段仍未决。
- 部分成员动作权限仍待确认，文档保持“待确认”，实现不得默认扩权。
- 登录失败锁定/限流阈值、管理员自停用保护、POC 资源阈值仍需后续确认。
- 本地未安装 Mermaid CLI；本次仅检查代码块闭合和状态/流转文本一致性，未做渲染级验证。

## 6. 总结

本次回写覆盖完整，D1-D9 在 context 与 prd-docs 之间口径一致，可作为一期开发基线。遗留项均被明确限制在二期或待确认技术边界内，不应被实现为默认业务规则。
