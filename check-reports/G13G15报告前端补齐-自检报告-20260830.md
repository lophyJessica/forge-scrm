# G13/G15 报告前端补齐自检报告

## 任务/项目

- 项目：forge-scrm（Forge 新媒体运营系统）
- 任务：仅补齐 G13 报告来源范围配置与 G15 推送记录完整查看的前端能力。
- 开工前同步：`git pull origin main` 执行成功，返回 `Already up to date.`。
- 边界：未修改 `backend/`，未修改 `CollectionTasks/ResearchTasks/ResearchReport`，未 commit/push/deploy，未删除文件。

## 改动文件清单

### `frontend/src/pages/reports/ReportList.tsx`

- 新建报告弹窗调整为 720px 复杂表单。
- 运营数据报告按后端执行器契约提供 `analysis_task_ids`、`collection_result_ids`、`raw_data_ids` 选择。
- 市场分析周报按后端执行器契约提供 `research_report_ids`、`collection_result_ids`、`research_reference_ids` 选择。
- 来源选项按统一来源时间窗过滤；未指定具体 ID 时，沿用后端“时间窗内全部可用数据”语义。
- 创建请求提交 `source_config`，生成前二次确认报告类型、时间窗和各类来源范围。
- 来源列表请求并行执行；切换报告类型时防止旧请求响应覆盖新选项。

### `frontend/src/pages/reports/ReportDetail.tsx`

- 推送任务 Drawer 由 480px 调整为 720px，窄屏使用 `maxWidth: 100vw` 约束。
- 每个推送任务可展开 `records` 目标级明细，Table 设置明确空态。
- 每条记录展示脱敏目标、消息摘要、发送时间、状态、错误码/原因和尝试序号。
- 推送任务和记录状态标签统一使用 `statusTagColor`。
- 按审核字样隐藏口径搁置：未实现任何审核/抽查入口，并移除报告详情中已有的审核状态展示。

### `check-reports/G13G15报告前端补齐-自检报告-20260830.md`

- 新增本轮改动、验证和风险记录。

## 验证方式与结果

| 验证项 | 方式 | 结果 |
|---|---|---|
| 前端类型与生产构建 | `cd frontend && npm run build` | 通过；`tsc -b` 与 Vite build 成功 |
| 补丁格式 | `git diff --check` | 通过，无空白/冲突标记问题 |
| 审核字样红线 | 在两个报告页搜索禁止字样及 `review_status` | 通过，无匹配 |
| G13 后端契约 | 对照 `report.py` 的 `source_config` 与 `report_executor.py` 五类 ID 读取键 | 通过，未新造接口/字段 |
| G15 后端契约 | 对照 `ReportPushRecordOut` 与推送任务列表 `records` 加载 | 通过，要求字段均有展示 |
| 新建报告布局 | 本地 Vite + ego-browser，1686×845 桌面视口 | 弹窗实测宽 720px、高 590px，无页面级横向溢出；运营报告三类来源控件按类型正常出现 |

## 遗留风险

1. 本地后端来源列表接口返回 HTTP 500，没有可用报告/推送数据，因此未完成真实数据下的端到端生成与推送 Drawer 点击验证；已完成契约静态核对、TypeScript 构建和可访问表单布局验证。
2. 现有来源列表 API 单次最多返回 200 条，具体 ID 选择器只能展示该页数据；留空时后端仍会读取时间窗内全部可用数据。
3. 后端暂无研究报告批量列表接口；前端需先取成功研究任务，再并行读取对应报告。研究任务量较大时会产生较多请求，本轮按“只补前端、不改后端”边界保留。
4. Vite 构建仍有现存的主 chunk 超过 500kB 警告，不影响本轮构建通过，本轮未做超范围拆包。

## 审核管道上传

- 状态：未上传。
- 原因：Codex 沙箱网络受限，单次上传尝试返回 `Could not resolve host: pmlophy.com`；按 `AGENTS.md` 兜底规则未重试。
- 后续：报告已保存在 `check-reports/`，由杰西卡在 VPS 侧代传审核管道。
