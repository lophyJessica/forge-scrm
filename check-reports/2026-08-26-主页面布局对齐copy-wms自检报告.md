# forge-scrm 主页面布局对齐 copy-wms 自检报告

- 日期：2026-08-26
- 任务：主内容不要和侧栏、顶栏割裂；参照 copy-wms「波次管理」壳层，全部页面走同一套布局
- 对照：copy-wms `AppShell.jsx`（右侧白底、顶栏放页面标题、正文 `p-6`、表格单独描边圆角）

## 改动文件

| 文件 | 改了什么 |
|---|---|
| `frontend/src/layouts/MainLayout.tsx` | 顶栏改为页面标题 + 用户区；折叠按钮移到侧栏底部；正文包 `forge-page` |
| `frontend/src/index.css` | 壳层 `100dvh` 锁视口；右侧顶栏/正文同一块白底、无外边距；标题与正文左对齐 24px；单卡片页去掉浮层 |
| `frontend/src/theme.ts` | `colorBgLayout` 改白；表格头浅灰、卡片去阴影 |

## 本地浏览器核对（`http://localhost:5173`）

视口高度 923px，已登录 admin。

- 资料列表 / 数据报告 / 批量生成 / 首页：顶栏标题分别为「资料列表」「数据报告」「批量生成」「首页」，与正文起点同为 `left: 280`（侧栏 256 + padding 24）
- 顶栏、正文计算背景均为 `rgb(255, 255, 255)`，正文 `margin: 0; padding: 24px`，不再出现灰底包围的整页白卡片
- 文档不滚：`documentElement.scrollHeight === innerHeight`，`scrollTop = 0`
- 侧栏可滚：`.forge-sider-nav` client 819 / scroll 1228；`scrollTop = 409` 时 Logo `top: 15.5`、顶栏 `top: 0`
- 正文可滚：内容超视口时 `.forge-content` 可到 `scrollTop = 500`，侧栏 Logo / 顶栏仍钉在视口顶部

## 自检结果

- 通过：壳层已与 copy-wms 同一套「侧栏深色 + 右侧白底一体 + 标题在顶栏 + 表格单独成块」
- 未执行 commit/push；生产 https://scrm.pmlophy.com 需部署后才生效

## 遗留

- 首页欢迎条仍在正文里（顶栏已是「首页」）；统计卡/快捷入口仍用卡片，与 copy-wms 工作台一致
- 研究报告本地无 id=1 数据，长页滚动用内容探测验证壳层，不依赖该条数据
