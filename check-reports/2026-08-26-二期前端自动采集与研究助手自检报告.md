# 二期前端自动采集与研究助手自检报告

## 一、任务与项目

- 项目：Forge 新媒体运营系统
- 仓库：`/Users/liulongfei/个人文件/forge-scrm`
- 任务：新增自动采集与研究助手前端页面、路由和菜单
- 执行日期：2026-08-26
- 基线：已执行 `git pull origin main`，结果为 `Already up to date.`

## 二、改动文件清单

- `frontend/src/pages/collection/BenchmarkAccounts.tsx`
  - 对标账号列表、平台/启用状态/关键词筛选、分页。
  - 新建、编辑、启用/停用账号。
- `frontend/src/pages/collection/CollectionTasks.tsx`
  - 采集任务状态筛选、分页、新建任务。
  - 支持多选启用账号、时间窗和可选 `public_urls` JSON。
  - 展开查看采集记录/采集结果 Tabs。
  - pending 执行、failed 重试及运行中 loading 状态。
- `frontend/src/pages/research/ResearchTasks.tsx`
  - 研究任务状态筛选、分页、新建任务。
  - 支持主题、目标、时间窗和可选 `scope_config` JSON。
  - searching/organizing 显示进行中进度，failed 支持重试。
  - 成功任务可进入报告详情。
- `frontend/src/pages/research/ResearchReport.tsx`
  - 展示标题、摘要、正文、章节、结论和来源引用。
  - `is_ai_product=true` 显示“AI 生成”标记。
  - 展示来源 URL、来源标题、检索供应商和证据摘要。
- `frontend/src/types/index.ts`
  - 新增二期 7 个实体的前端类型和状态联合类型。
- `frontend/src/App.tsx`
  - 注册四个页面路由：
    - `/collection/benchmark-accounts`
    - `/collection/tasks`
    - `/research/tasks`
    - `/research/reports/:id`
- `frontend/src/layouts/MainLayout.tsx`
  - 新增“内容管理”菜单组：对标账号、自动采集。
  - 新增“AI 模块”菜单组：研究助手。
  - 新增研究报告面包屑映射。
- `check-reports/2026-08-26-二期前端自动采集与研究助手自检报告.md`
  - 本次自检报告。

## 三、实现检查

- [x] 自动采集账号列表支持平台、启用状态、关键词筛选和分页。
- [x] 账号新建、编辑、启用/停用使用现有 CRUD API。
- [x] 采集任务支持账号选择、时间窗、`public_urls` 配置、执行和失败重试。
- [x] 采集任务展开区展示记录和结果。
- [x] 采集状态映射：pending 默认、running processing、success success、partial_success warning、failed error。
- [x] 研究任务支持主题、目标、范围配置、执行和失败重试。
- [x] searching/organizing 展示进行中状态和进度。
- [x] 报告页展示 AI 生成标记及引用信息。
- [x] 未新增依赖，复用 React、antd、axios、dayjs 和 react-router-dom。
- [x] 未修改 backend、context、prd-docs 或 requirements。

## 四、验证结果

### 4.1 构建

执行：

```text
npm run build
```

结果：通过。TypeScript 检查和 Vite 生产构建均成功。Vite 输出现有单 chunk 超过 500 kB 的警告，不影响构建。

### 4.2 本地运行

- 使用 `npm run dev -- --host 127.0.0.1 --port 5173` 启动成功。
- Vite 输出 `Local: http://127.0.0.1:5173/` 和 `ready`。
- 当前 Codex 沙箱与本地服务进程网络隔离，另一个沙箱命令访问该端口返回 connection refused/000，因此未完成命令行 HTTP 页面访问。
- 开发服务器已停止，未留下后台进程。

### 4.3 API 路径兼容

代码按用户给定的二期 `/api/v1` 路径调用。由于当前本地工作树中部分列表/报告接口仍存在旧式平铺路径，页面做了非侵入式回退：

- 记录：优先 `/collection-tasks/{id}/records`，404 时回退 `/collection-records?task_id={id}`。
- 结果：优先 `/collection-tasks/{id}/results`，404 时回退 `/collection-results?task_id={id}`。
- 报告：优先 `/research-tasks/{id}/report`，404 时回退 `/research-reports/{id}`，再请求 `/research-references` 补齐引用。

## 五、范围与安全检查

- [x] 最终工作树只包含 `frontend/src/` 源码改动和本自检报告。
- [x] 构建产生的 `frontend/tsconfig.app.tsbuildinfo` 已恢复，不作为交付改动。
- [x] 没有写入 key、token、密码或数据库连接串。
- [x] 没有执行 commit、push、部署或删除文件。
- [x] `git diff --check` 通过。

## 六、遗留风险

1. 当前只完成构建和运行启动检查，未使用真实登录浏览器逐页完成视觉验收；本地沙箱无法从另一命令访问 Vite 端口。
2. 采集和研究页面依赖后端真实数据及权限，未配置数据时会显示空列表或后端错误提示。
3. 后端部分二期列表/报告接口与任务描述中的嵌套路由存在差异，页面已提供 404 回退，但正式接口最好统一。
4. 研究报告路由使用任务 ID 进入，若后端仅支持报告 ID 且任务 ID 与报告 ID 不一致，需要后端返回 report_id 或提供任务到报告的查询接口。
5. Vite 仍有既有大 chunk 警告，本次未扩展拆包范围。

## 七、结论

四个二期前端页面、路由、菜单和实体类型已完成，构建通过，状态与字段按现有二期 API 对齐，采集结果和研究报告均保留原始/AI 产物语义。未修改受保护目录，未提交或推送。

## 八、上传状态

本报告已保存到 `check-reports/`。按 `AGENTS.md` 要求尝试上传审核管道，实际结果为：`curl: (6) Could not resolve host: pmlophy.com`。由于当前沙箱 DNS 受限，报告未上传，未虚报成功。
