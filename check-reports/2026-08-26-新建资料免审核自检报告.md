# 新建资料免审核自检报告

## 任务 / 项目

- 项目：Forge 新媒体运营系统。
- 任务：人工创建资料直接生效；批量导入资料和 AI 回写资料继续经过审核。
- 工作区：`/Users/liulongfei/个人文件/forge-scrm`。
- 约束：仅修改 `backend/app/routers/materials.py` 和 `frontend/src/pages/materials/MaterialForm.tsx`；未修改数据库、迁移、`context/`、资料审核页面、脚本审核或分析审核。
- 未执行 commit、push 或部署。

## 修改文件

### `backend/app/routers/materials.py`

- POST `/api/materials` 的状态固定为 `MaterialStatus.已生效`。
- `submit_for_review` 仍可被 Schema 接收，但创建接口不再根据该参数进入待审核，满足旧客户端传参兼容。
- 导入接口仍使用 `MaterialStatus.待审核`，未改动。
- 资料审核、提交审核、AI 回写相关路由未改动。

### `frontend/src/pages/materials/MaterialForm.tsx`

- 删除“存为草稿”和“保存并提交审核”按钮。
- 人工保存改为单一“保存”按钮。
- 创建请求不再发送 `submit_for_review`。
- 编辑路径不再从该页面触发提交审核；审核仍由资料审核页面处理。

## 验证结果

### TestClient

- 人工创建资料，不传 `submit_for_review`：HTTP 200，状态为“已生效”。
- 人工创建资料，传 `submit_for_review=true`：HTTP 200，状态仍为“已生效”。
- CSV 导入资料：HTTP 200，导入记录状态为“待审核”。测试通过 monkeypatch 避免写入导入原文件。
- AI 回写资料：HTTP 200，生成资料状态为“待审核”，`is_ai_product=true`。
- AI 回写代码仍为 `MaterialStatus.待审核`，对齐 R8/R1/R7。
- 测试创建的资料、分析任务、分析结果和关联记录已清理。

### 前端

- 在 `frontend/` 执行 `npm run build`：通过。
- Vite 仅输出既有 JS bundle 体积优化提示，不影响构建。
- `git diff --check`：通过。

### 保护范围

- 当前差异仅包含两个业务源文件及本报告。
- 脚本审核、分析任务审核、资料审核路由和导入审核逻辑均无差异。

## 同步与上传状态

- `git pull origin main`：沙箱首次执行因无法写入 `.git/FETCH_HEAD` 失败；升级权限重试时审批服务返回 `503 Service Unavailable`，未绕过审批，也未重复尝试。
- 自检报告上传：未成功。curl 使用 `--noproxy '*'` 时返回 `Could not resolve host: pmlophy.com`，按项目守则未重复尝试。

## 遗留风险

- 由于远端同步受本机审批服务 503 阻塞，本轮基于当前本地工作区完成，交付前应在审批恢复后重新执行 `git pull origin main` 并检查是否有冲突。
- `MaterialCreate.submit_for_review` 字段仍保留在 Schema，以兼容旧客户端；创建接口已明确忽略它。
- 人工创建资料现在直接生效，调用方必须确保分类、来源类型、可信度和有效期等字段仍然经过现有校验。
