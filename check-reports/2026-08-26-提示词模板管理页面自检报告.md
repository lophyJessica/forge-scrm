# 提示词模板管理页面自检报告

## 一、任务与项目

- 项目：Forge 新媒体运营系统
- 仓库：`/Users/liulongfei/个人文件/forge-scrm`
- 任务：补齐提示词模板管理页面，并展示代码内置默认提示词
- 执行日期：2026-08-26
- 基线：执行 `git pull origin main`，结果为 `Already up to date.`

## 二、改动文件清单

### 后端

- `backend/app/routers/prompts.py`
  - 新增只读 `GET /api/prompt-templates/builtin`。
  - 同时提供用户要求的 `GET /api/v1/prompt-templates/builtin` 兼容别名。
  - 返回选题生成和脚本生成两条代码内置提示词。
  - 不落库、不修改、不删除内置内容。
  - 复用 `topic_service.DEFAULT_SYSTEM_PROMPT` 和 `script_service.DEFAULT_SYSTEM_PROMPT`。

### 前端

- `frontend/src/pages/admin/PromptTemplates.tsx`
  - 新增管理员提示词模板管理页面。
  - 顶部只读展示选题生成/脚本生成内置提示词。
  - 模板列表展示名称、任务类型、版本、正文截断、描述占位和时间信息。
  - 支持新建、编辑、删除二次确认。
  - 任务类型限制为选题生成/脚本生成。
  - 沿用现有提示词 CRUD API，正文更新时版本号由后端递增。
  - 页面明确说明现有模型未提供可持久化描述字段，因此不伪造描述数据。

- `frontend/src/App.tsx`
  - 注册 `/admin/prompt-templates` 路由，并复用管理员路由守卫。

- `frontend/src/layouts/MainLayout.tsx`
  - 在权限管理菜单增加“提示词模板”入口。

- `backend/app/main.py`
  - 注册 builtin 兼容路由；既有 `/api/prompt-templates` CRUD 路由保持不变。

## 三、验证结果

### 3.1 前端构建

执行：

```text
npm run build
```

结果：通过。

TypeScript 检查和 Vite 生产构建均成功。Vite 仅提示现有单 chunk 超过 500 kB 的警告，本次未引入拆包范围外改动。

### 3.2 后端接口与既有 CRUD

使用独立内存 SQLite、管理员依赖覆盖和 FastAPI TestClient 验证，未污染本地业务数据库：

| 场景 | 结果 |
|---|---:|
| `GET /api/prompt-templates/builtin` | 200 |
| `GET /api/v1/prompt-templates/builtin` | 200 |
| 内置任务类型 | 包含“选题生成”和“脚本生成” |
| 内置正文 | 两条均非空 |
| 创建模板 | 200，初始 `version=1` |
| 正文编辑 | 200，`version=2` |
| 模板列表 | 200，可查询新建模板 |
| 模板删除 | 200 |

### 3.3 范围检查

- [x] 未修改 PromptTemplate 模型。
- [x] 未修改既有模板 CRUD 逻辑，只新增只读 builtin 路由。
- [x] 未修改 `context/`、`prd-docs/`、`requirements/`。
- [x] 未新增依赖。
- [x] 未写入 key、token、密码或数据库连接串。
- [x] 未执行 commit、push、部署或删除文件。
- [x] 最终 `git diff --check` 通过。
- [x] 最终工作树仅包含本任务后端路由、前端页面、路由和菜单改动。

## 四、接口路径说明

现有后端提示词 CRUD 实际前缀为 `/api/prompt-templates`，前端 Axios `baseURL` 为 `/api`。因此页面使用 `/api/prompt-templates/builtin`；同时额外提供 `/api/v1/prompt-templates/builtin` 兼容别名，以满足用户要求的路径，不改变既有 CRUD 路径。

## 五、遗留风险

1. 现有 `PromptTemplate` 模型没有 `description` 和 `updated_at` 字段，本页面只能展示描述占位和现有 `created_at` 时间；若产品要求真实保存这两个字段，需要单独确认字段并新增迁移。
2. 页面视觉验收未使用真实浏览器登录会话完成，本次完成了 TypeScript/Vite 构建和后端接口 TestClient 验证。
3. Vite 仍有既有的大 chunk 警告，不影响本次构建结果。
4. 自检报告上传管道受当前沙箱 DNS 限制，未能上传。

## 六、上传结果

按 `AGENTS.md` 执行：

```text
curl --noproxy '*' -X POST "https://pmlophy.com/p/jarvis/file/upload" \\
  -H "X-Jarvis-User: ai-reports" \\
  -F "file=@/Users/liulongfei/个人文件/forge-scrm/check-reports/2026-08-26-提示词模板管理页面自检报告.md"
```

结果：失败，`curl: (6) Could not resolve host: pmlophy.com`。已按守则停止重试，未声称上传成功。
