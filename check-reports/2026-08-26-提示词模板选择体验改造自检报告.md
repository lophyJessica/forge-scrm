# 提示词模板选择体验改造自检报告

## 一、任务与项目

- 项目：Forge 新媒体运营系统
- 仓库：`/Users/liulongfei/个人文件/forge-scrm`
- 任务：批量生成选题/脚本页面改造为“内置模板 / 自定义”单选体验
- 执行日期：2026-08-26
- 基线：已执行 `git pull origin main`，结果为 `Already up to date.`

## 二、改动文件清单

- `frontend/src/pages/topics/TopicGenerate.tsx`
  - 移除生成页数据库模板下拉。
  - 增加“内置模板 / 自定义”Radio.Group。
  - 加载 `/api/prompt-templates/builtin`，以内置任务类型和前 30 字预览作为下拉选项。
  - 默认选择内置模板；自定义模式显示 6 行长文本输入。
  - 内置模式不发送 `prompt_template_id`，走后端内置默认；自定义模式只发送 `prompt_content`。
- `frontend/src/pages/scripts/ScriptGenerate.tsx`
  - 与选题生成页保持同样的单选、内置预览和自定义提示词体验。
- `backend/app/schemas/topic.py`
  - `TopicGenerateRequest` 增加可选 `prompt_content` 字段。
- `backend/app/schemas/script.py`
  - `ScriptGenerateRequest` 增加可选 `prompt_content` 字段。
- `backend/app/services/topic_service.py`
  - `build_prompt` 增加自定义提示词参数。
  - 优先级为 `prompt_content > prompt_template_id > 内置默认`。
  - 自定义提示词快照记录 `prompt_type=custom` 和完整 `content`。
- `backend/app/services/script_service.py`
  - 同上，保持脚本生成与选题生成一致。
- `backend/app/routers/topics.py`
  - 将请求中的 `prompt_content` 透传到 service；这是新字段实际生效所需的最小调用接线。
- `backend/app/routers/scripts.py`
  - 将请求中的 `prompt_content` 透传到 service；既有 `prompt_template_id` 语义保持不变。
- `check-reports/2026-08-26-提示词模板选择体验改造自检报告.md`
  - 本次自检报告。

## 三、功能检查

- [x] Radio 单选项为“内置模板”和“自定义”。
- [x] 默认模式为内置模板，默认选择对应业务类型的第一个 builtin 选项。
- [x] 内置下拉 label 包含任务类型和提示词前 30 字预览。
- [x] 内置下拉提示为“选择内置模板（默认即可）”。
- [x] 自定义模式显示 `rows=6` 的 TextArea。
- [x] 自定义提示词支持长文本并在提交时发送 `prompt_content`。
- [x] 表单临时字段 `prompt_mode`、`builtin_prompt_type` 不会提交给后端。
- [x] 数据库模板 API 兼容逻辑保留，前端本页不再展示模板库选择。
- [x] 内置默认常量内容未修改。

## 四、验证结果

### 4.1 前端构建

执行：

```text
npm run build
```

结果：通过。TypeScript 检查和 Vite 生产构建均成功。仅有既有大 chunk warning，不影响构建。

### 4.2 builtin 接口与生成 TestClient

使用独立内存 SQLite、管理员依赖覆盖和模拟 DeepSeek 响应完成验证，未污染本地业务数据库：

| 场景 | 结果 |
|---|---:|
| 选题生成不传 `prompt_content` | 200，使用原内置选题提示词 |
| 选题生成传 `prompt_content` | 200，使用自定义提示词 |
| 选题自定义快照 | `{"prompt_type":"custom","content":"..."}` |
| 脚本生成不传 `prompt_content` | 200，使用原内置脚本提示词 |
| 脚本生成传 `prompt_content` | 200，使用自定义提示词 |
| 脚本自定义快照 | `{"prompt_type":"custom","content":"..."}` |
| 同时传模板 ID 和自定义内容 | 自定义内容优先 |
| builtin 接口返回任务类型和正文 | 通过 |

### 4.3 兼容性

- 原有 `prompt_template_id` 未删除，service 仍支持数据库模板。
- 未传自定义内容时，原有内置提示词和快照结构保持不变。
- 未新增依赖。
- 未新增数据库迁移。

## 五、范围检查

- [x] 未修改 `context/`、`prd-docs/`、`requirements/`。
- [x] 未修改内置默认提示词常量内容。
- [x] 未写入 key、token、密码或数据库连接串。
- [x] 未执行 commit、push 或部署。
- [x] `git diff --check` 通过。
- [x] 构建缓存文件已恢复，未混入最终业务改动。

## 六、偏差说明

用户限定的文件列表未包含 `backend/app/routers/topics.py` 和 `backend/app/routers/scripts.py`，但仅修改 schema/service 无法让 HTTP 请求中的新字段进入 service。此次仅在两个现有调用点各增加 `payload.prompt_content` 透传，未改变路由、权限、模板 ID 或生成业务逻辑；这是实现“自定义提示词真正生效”的必要最小改动。

## 七、遗留风险

1. 本次使用 TestClient 和模拟 DeepSeek 响应验证提示词选择及优先级，未调用真实 AI 服务。
2. 浏览器视觉验收未在真实登录会话中执行；生产构建已通过。
3. 自定义提示词完整内容写入生成结果的现有提示词快照，长文本对数据库 JSON 大小和展示长度的影响需要后续产品确认。
4. 自检报告上传管道可能受当前沙箱 DNS 限制，上传结果以命令实际返回为准。

## 八、上传状态

本报告已保存至 `check-reports/`。实际上传返回 `curl: (6) Could not resolve host: pmlophy.com`，受当前沙箱 DNS 限制未上传，未声称上传成功。
