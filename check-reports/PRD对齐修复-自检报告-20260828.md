# PRD 对齐修复自检报告

## 任务与项目

- 项目：Forge 新媒体运营系统（forge-scrm）
- 分支：main
- 日期：2026-08-28
- 任务：按审计报告修复 R1-R6 的 MVP 阻塞项，并按任务三清单回写 R7 正式文档口径
- 基线：执行 `git pull origin main`，结果为 `Already up to date.`

## 逐项完成情况

### R1（审计 D1）资料人工创建闭环

- `frontend/src/pages/materials/MaterialForm.tsx`
  - 增加可选有效期范围，提交时映射为 `valid_from`、`valid_until`。
  - 新建资料提供“保存草稿”和“提交审核”双动作。
  - 编辑草稿时保留双动作；提交审核先保存修改，再调用既有 `/materials/{id}/submit`。
  - 编辑非草稿资料时仅“保存修改”，保持原状态，不制造非法状态跃迁。
- `backend/app/routers/materials.py`
  - 恢复既有 `submit_for_review` 语义：false 写入草稿，true 写入待审核。
  - 导入和 AI 回写路径未改动。
- `backend/app/schemas/material.py`
  - 更新有效期默认值注释，使其与当前可选表单口径一致；字段结构未变。

### R2（审计 D2）资料审核权限放开

- `frontend/src/pages/materials/MaterialReview.tsx`
  - 移除管理员专属空态和错误的 context 说明，成员可进入待审核队列。
- 后端核对结论：`review_material` 已使用 `CurrentUser` 并调用 `assert_material_class_visible`，管理员和成员均按资料分类数据范围审核，符合 `context/06 §2.2`。
- 未新增“资料审核”权限码：`context/06 §2.2` 对资料审核的正式口径是成员可做且受数据范围限制，并非“由管理员授权”；新增权限码会与 SSOT 冲突。

### R3（审计 D3）选题每方向固定 10 条

- `frontend/src/pages/topics/TopicGenerate.tsx`
  - 移除 1-10 可编辑数量控件。
  - 页面静态显示“每方向 10 条”，请求固定发送 `count: 10`。
  - 后端 count 弹性未改动。

### R4（审计 D4）模板管理补分析类

- `frontend/src/pages/admin/PromptTemplates.tsx`
  - task_type 增加“资料分析”“数据分析”。
  - 分析类条件展示 `output_schema` JSON 编辑区并设为必填。
  - 保存前校验 JSON 合法且顶层为对象；编辑时格式化回显已有 schema。
  - 编辑模板时保留原启停状态，避免编辑正文意外启用停用模板。
- 后端现有模型、schema、创建接口已支持四类任务和 `output_schema`，未重复修改。

### R5（审计 G3）生成页选择已保存模板

- `frontend/src/pages/topics/TopicGenerate.tsx`
- `frontend/src/pages/scripts/ScriptGenerate.tsx`
  - 两页统一请求 `/prompt-templates?task_type=<类型>&status=启用&page_size=200`。
  - 下拉展示全部启用模板，value 使用模板 ID。
  - 选中模板时发送 `prompt_template_id`；不选时不传，继续走后端默认模板。
  - 自定义模式只发送 `prompt_content`，优先级语义保持不变。
  - 自定义提示词保存为模板后刷新模板列表。

### R6（审计 G1）固定组合引用预览

- `frontend/src/components/MaterialComboField.tsx`
  - 已选资料下方新增默认收起的“引用预览”折叠区。
  - 按 `material_combo` 最终选择顺序展示 `【分类】标题` 和正文前 200 字。
  - 复用现有已生效资料列表，不新增接口。

### R7 文档回写

- `prd-docs/modules/数据报告/数据报告主PRD.md`
  - 报告/推送任务删除改为有限提供，并写明报告关联保护、生成中保护和已推送凭证保护。
- `prd-docs/modules/推送任务取消删除与卡片跳转PRD.md`
  - 删除规则统一为仅待推送/失败/已取消可删，已推送作为发送凭证不可删。
- `prd-docs/modules/数据分析/数据分析主PRD.md`
  - 对数据源、原始数据、分析任务补充删除与归档策略待确认说明。
- `prd-docs/modules/数据分析/数据分析字段清单.md`
  - 在三类实体定义后分别补充同一待确认口径。
- `prd-docs/modules/审核流程变更说明.md`
  - 增加醒目冲突提示：与 context/04 R1/R3/R6/R8 冲突时以 context 为准。
- 未修改其他 `prd-docs/` 文件；全局排序、二期 P0 缺口等仍为后续待办。

## 自检结果

- `git pull origin main`：通过，已是最新。
- 后端语法：`.venv/bin/python -m compileall -q app` 通过。
- 前端构建：`npm run build` 通过；Vite 仅提示既有 bundle 大于 500 kB，无构建错误。
- MVP 自测：`.venv/bin/python scripts/selftest_mvp.py` 通过 43 项、失败 0 项、环境限制 2 项（S02/S06）。其中 M01 验证草稿→待审核，M02/M03 验证审核通过/驳回，M06/M07 验证引用预览与有效期，M10 验证每方向生成 10 条。
- `git diff --check`：通过。
- 受保护范围：未修改 `context/`、`requirements/`、`audit-reports/`、`audit-reviews/`；未改 draft 待办。
- Git 操作：未 commit、未 push、未 deploy。

## 遗留风险

- 未做浏览器端人工点击回归；TypeScript 构建已覆盖组件类型和请求字段拼装。
- 模板列表按现有接口上限加载 200 条；若单类型模板未来超过 200 条，需要产品化分页/搜索。
- `context/06 §2.2` 未定义独立“资料审核”权限码，本轮严格按 SSOT 使用成员数据范围；若产品后续决定审核必须显式授权，应先回写 context 和权限矩阵再新增权限码。
- PRD 回写仅覆盖任务三清单；全局排序、二期 P0 缺口和其他非本轮项仍为待办。
