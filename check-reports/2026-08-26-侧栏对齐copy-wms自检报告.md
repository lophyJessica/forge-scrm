# forge-scrm 侧栏对齐 copy-wms 自检报告

## 1. 任务摘要

- 项目：forge-scrm
- 日期：2026-08-26
- 任务：侧边菜单对齐 copy-wms（截图 + `AppShell.jsx`）
- 范围：仅前端视觉（主题 / 布局 CSS / 侧栏结构），未改业务逻辑与路由数据

## 2. 改动文件

| 文件 | 改动 |
|---|---|
| `frontend/src/layouts/MainLayout.tsx` | 256px 侧栏、紫标 Logo、分组默认可多开、选中叶子高亮 |
| `frontend/src/theme.ts` | Menu/Layout token：14px、圆角 6、中性深色、紫色选中 |
| `frontend/src/index.css` | 子菜单竖线、选中紫渐变、滚动条、Inter 字体栈 |
| `frontend/index.html` | 引入 Inter 400/500/600 |

## 3. 对齐要点

- 侧栏底 `#171717`，右边线 `#262626`
- Logo：32×32 `#7c3aed` 圆角方块 + 线框图标 + `Forge SCRM` 14px/600
- 父级项 32px、子级 36px、`text-sm`、未选中 `#e5e5e5`
- 子菜单 `border-l` 竖线；叶子选中 `from-purple-600 to-purple-500` 圆角条
- 顶层「首页」选中为中性灰底，与 copy-wms 顶级项一致

## 4. 自检结果

| 项 | 结果 |
|---|---|
| `npm run build` | ✅ 通过 |
| Cursor 内置浏览器登录后截图 | ❌ 沙箱无法访问本机 5173（ERR_CONNECTION_REFUSED） |
| commit/push | 未执行 |

## 5. 遗留

若本地曾把侧栏设为折叠，`localStorage.scrm_sider_collapsed=true` 仍会折叠；新会话默认展开以贴近 copy-wms。
