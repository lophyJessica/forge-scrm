# 自动采集模块套件自检报告

## 一、任务与项目

- 项目：Forge 新媒体运营系统
- 仓库：`/Users/liulongfei/个人文件/forge-scrm`
- 任务：二期第二步，生成自动采集模块 PRD 套件
- 执行日期：2026-08-26
- 本步范围：只生成文档，不写代码、不做原型、不修改一期模块
- 基线：本地 `HEAD` 与 `origin/main` 均为 `0c6fdc2529b6239aab2559294896cc60a02e1043`

## 二、权威输入与执行说明

已读取并核对：

1. `requirements/09-二期调研结论.md`
2. `prd-docs/modules/00-二期总PRD.md`
3. `prd-docs/modules/审核流程变更说明.md`
4. `templates/prd-template.md`
5. `context/05-术语与字段口径.md`
6. 一期选题库主 PRD、字段清单、业务规则、业务流程和用例数据推演

执行前尝试运行 `git pull --ff-only origin main`。首次执行受本地沙箱限制无法写入 `.git/FETCH_HEAD`，申请审批时审批服务返回 503。随后以只读方式核对，确认本地 `HEAD` 与 `origin/main` 均为目标提交 `0c6fdc2`，当前工作树无未提交差异后继续。未使用绕过方式替代同步。

## 三、改动文件清单

### 1. 自动采集主 PRD

文件：`prd-docs/modules/自动采集/自动采集主PRD.md`

行数：320 行。

内容：

- 按模板完整覆盖模块概述、功能清单、交互流程、关键规则、状态机、数据字段、权限、验收标准、待确认事项九节。
- 明确 P0 对标账号管理、人工触发、对标账号优先采集、任务/记录/结果独立落库、失败重试、重复任务提示和使用链路。
- 评论、热点、行业报告作为次优先能力，官方接口和网页抓取能力标注待实测。
- 定时采集只保留 P1 预留，不展开调度细节。
- 状态机为待执行→执行中→已完成/部分成功/失败，部分成功标注“新增建议，待确认”。
- 说明原始采集结果默认 `is_ai_product=false`，AI 摘要等衍生结果必须单独标记。

### 2. 自动采集字段清单

文件：`prd-docs/modules/自动采集/自动采集字段清单.md`

行数：151 行。

内容：

- 覆盖且仅覆盖对标账号、采集任务、采集记录、采集结果四类实体。
- 每类实体提供建议表名、字段名、类型、必填、默认值和说明。
- 对 `context/05` 已定义的业务对象、平台、账号标识、对标标记、来源链接、原始数据内容、结构化数据、采集时间和时间窗明确复用其语义。
- 新增主键、外键、任务编号、状态、范围、重试、错误、幂等和追溯字段统一标注“新增建议，待确认”。
- 采集结果默认 `is_ai_product=false`，并明确 AI 衍生内容不得覆盖原始事实。
- 末尾包含 Mermaid `erDiagram` 关系总览。

### 3. 自动采集业务规则规格

文件：`prd-docs/modules/自动采集/自动采集_业务规则规格.md`

行数：206 行。

内容：

- 定义 AC-R1 至 AC-R12，并引用 R1、R2、R4、R6、R7、R8、R13、R14、R16、R17。
- 覆盖人工触发、P1 定时预留、对标账号优先、官方 API 优先、网页合规边界、原始事实直接信、AI 产物区分、来源追溯、任务级/记录级失败、重试和重复任务提示。
- 明确 robots、服务条款、频率、版权、登录/风控等项目的待实测或已确认边界。
- 明确不以空数据伪装成功，不绕过验证码、登录限制、风控或付费墙。
- 任务和记录状态、部分成功、错误码、幂等键和重试策略均标注新增建议，待确认。

### 4. 自动采集业务流程推演

文件：`prd-docs/modules/自动采集/自动采集_业务流程推演.md`

行数：183 行。

内容：

- 推演维护账号→选择账号和范围→创建待执行任务→人工开始→执行和记录拆分→结果落库→资料/分析使用链路。
- 覆盖账号停用、来源不可达、单条失败/部分成功、重复任务和网页采集不合规异常分支。
- 提供页面与实体/状态对应关系，以及待执行、部分成功、已完成状态快照。
- 明确旧错误保留、成功结果不重复写入、历史结果可查和原始事实不被 AI 摘要覆盖。

### 5. 自动采集用例数据推演

文件：`prd-docs/modules/自动采集/自动采集_用例数据推演.md`

行数：257 行。

内容：

- 提供对标账号、任务、记录、结果的具体样例字段值。
- AC-TC-01：正常采集全流程。
- AC-TC-02：记录级失败→部分成功→只重试失败记录且重试仍失败。
- AC-TC-03：任务级初始化失败→任务级重试。
- AC-TC-04：同账号/时间窗/范围重复任务提示与幂等。
- AC-TC-05：采集结果进入资料库/分析链路，原始事实和 AI 产物区分。
- AC-TC-06：停用账号边界。
- 强制关卡表明确五类必需场景均推演闭环。

## 四、自检结果

| 检查项 | 结果 | 证据 |
|---|---|---|
| 5 份文件均存在 | 通过 | 目录实际包含 5 份 Markdown 文件 |
| 主 PRD 行数不少于要求 | 通过 | 320 行，要求为 300 行级内容并按模板完整展开 |
| 字段清单包含四类实体 | 通过 | 对标账号、采集任务、采集记录、采集结果四节 |
| 状态机与总 PRD 5.1 对齐 | 通过 | 待执行、执行中、已完成、部分成功、失败；部分成功标注待确认 |
| 主 PRD 九节结构 | 通过 | `## 1` 至 `## 9` 完整存在 |
| 原始事实与 AI 产物区分 | 通过 | 结果默认 `is_ai_product=false`，衍生内容单独标记 |
| R 编号引用 | 通过 | 引用 R1/R2/R4/R6/R7/R8/R13/R14/R16/R17 |
| 新规则使用 AC- 前缀 | 通过 | AC-R1 至 AC-R12 |
| 合规风险标注 | 通过 | robots、服务条款、频率、版权和接口能力均标注待实测/待确认 |
| 五类用例关卡 | 通过 | AC-TC-01 至 AC-TC-05 覆盖要求，AC-TC-06 补充停用账号 |
| 未展开后续模块实体 | 通过 | 未将研究任务、研究报告、周期报告、推送记录建模为本步实体 |
| 受保护文件未改动 | 通过 | 本步 Git 状态仅新增 `prd-docs/modules/自动采集/` |
| 代码/原型未修改 | 通过 | 本步无 frontend/backend/deploy 改动 |
| 密码/token/key/连接串未写入 | 通过 | 示例未包含真实凭据；明确禁止写入 |

## 五、遗留风险

1. 视频号和小红书官方 API 的可用性、授权方式、配额、历史回溯和字段完整度尚未实测。
2. 评论、热点、行业报告的来源、版权、全文范围、费用和抓取方式尚未确认。
3. robots、服务条款、访问频率、版权存储和删除请求的合规评审尚未完成。
4. “部分成功”是否作为正式任务状态，或用计数 DTO 表达，尚未确认。
5. 任务级和记录级重试的上限、退避、尝试表和幂等键实现尚未确认。
6. 采集结果沉淀到资料库的具体页面动作、资料状态和关联字段尚未确认。
7. AI 摘要是否默认生成、是否异步生成、抽查比例及 AI 衍生实体尚未确认。
8. 本报告不代表代码已实现、接口已接通或线上已部署。

## 六、结论

自动采集模块 PRD 套件已按第二步范围完成，文档关卡通过。下一步需等待用户确认后，才能进入第三步研究助手模块；本轮不生成 Demo、不写代码、不修改权威 context 或一期文档。

