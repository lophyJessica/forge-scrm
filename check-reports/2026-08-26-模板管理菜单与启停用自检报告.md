# 模板管理菜单与启停用自检报告

## 任务与项目

- 项目：Forge 新媒体运营系统
- 任务：将提示词模板独立为一级菜单“模板管理”，并增加模板启停用能力。
- 工作区：`/Users/liulongfei/个人文件/forge-scrm`
- 代码范围：仅修改用户指定的 6 个业务文件；本报告属于项目规定的 `check-reports/` 产物。
- Git：未执行 commit 或 push。

## 改动文件清单

1. `frontend/src/layouts/MainLayout.tsx`
   - 在“内容管理”和“AI 模块”之间新增一级菜单“模板管理”。
   - 一级菜单下仅保留“提示词模板”，路径为 `/admin/prompt-templates`。
   - 移除“数据分析”下的 `/analysis/prompts` 菜单。
   - 移除“权限管理”下的 `/admin/prompt-templates` 菜单，保留“成员与权限”。
2. `frontend/src/App.tsx`
   - 移除旧的 `analysis/PromptTemplates` import。
   - 移除 `/analysis/prompts` 路由。
   - 保留受管理员保护的 `/admin/prompt-templates` 路由。
3. `frontend/src/pages/admin/PromptTemplates.tsx`
   - 新增启用/停用状态 Tag。
   - 新增模板状态 Switch，调用既有 PUT 接口更新状态并刷新列表。
   - 保留删除二次确认和最后一条模板由后端保护的提示文案。
4. `backend/app/routers/prompts.py`
   - builtin 查询增加 `PromptStatus.启用` 过滤。
   - 无启用模板时继续回退代码常量。
   - 删除保护和显式模板更新逻辑未改动。
5. `backend/app/services/topic_service.py`
   - 选题默认模板查询增加启用状态过滤。
   - 显式 `template_id` 仍使用原有 `db.get()`，不拦截停用模板。
6. `backend/app/services/script_service.py`
   - 脚本默认模板查询增加启用状态过滤。
   - 显式 `template_id` 仍使用原有 `db.get()`，不拦截停用模板。

## 自检结果

- `git diff --check`：通过。
- 菜单静态检查：`MainLayout.tsx` 中模板入口仅有一处，旧 `/analysis/prompts` 菜单已移除；`App.tsx` 中仅保留 `/admin/prompt-templates` 路由，旧路由已移除。
- 前端构建：`npm run build` 通过。Vite 仅提示已有 bundle 超过 500 kB 的体积警告，不影响构建结果。
- 后端 TestClient（隔离临时 SQLite，不使用生产数据库）：通过。
  - builtin 对选题生成和脚本生成均只返回启用模板。
  - 选题服务和脚本服务默认均选择启用模板。
  - 显式指定停用模板仍可使用，兼容既有 `template_id` 语义。
  - 某任务类型无启用模板时，builtin 回退代码常量。
  - 删除任务类型最后一条模板返回 HTTP 400，detail 为“该任务类型至少需要保留一条模板”。
  - 新建模板返回 HTTP 200。
- 数据库迁移：本任务未改模型结构，无新增迁移。
- 生产部署：本任务未执行部署，未修改生产数据库、环境配置或历史生成快照。
- 报告上传：已执行守则规定的上传命令；当前环境 DNS 无法解析 `pmlophy.com`，curl 返回 exit code 6（`Could not resolve host`），因此未上传成功，未重复尝试。

## 遗留风险

- 未进行线上浏览器视觉验收；本次改动的菜单路径和组件逻辑已完成源码级检查及构建验证。
- 历史数据中若存在 `status` 为 NULL 的旧模板，按本次规则不会进入 builtin 或默认模板选择；管理员可在模板管理页将其启用。
- Switch 请求失败时由前端现有 axios 拦截器展示后端 `detail`，未新增独立错误处理链路。
- 是否将 `/analysis/prompts` 旧地址重定向到新地址未在本任务范围内实现；当前按要求删除旧路由。
