# 筛选区卡片化自检报告

- 项目：forge-scrm
- 任务：全局筛选区卡片化，保持主区块间距 24px
- 日期：2026-08-31
- 开工前已执行：`git pull origin main`，结果为 Already up to date
- 范围：仅前端主题 token 与目标页面筛选/版本选择容器；未修改 `backend/`

## 改动文件清单

- `frontend/src/theme.ts:7-13`
  - 新增全局 `FILTER_CARD_STYLE`，唯一维护背景、边框、圆角、内边距和区块间距：`#fafafa`、`1px solid #f0f0f0`、`8px`、`16px`、`24px`。
- `frontend/src/pages/materials/MaterialList.tsx:22,83`
  - 引入并应用 `FILTER_CARD_STYLE` 到资料筛选 Form。
- `frontend/src/pages/topics/TopicList.tsx:8,65`
  - 引入并应用 `FILTER_CARD_STYLE` 到选题筛选 Form。
- `frontend/src/pages/analysis/AnalysisTasks.tsx:9,106`
  - 引入并应用 `FILTER_CARD_STYLE` 到分析任务筛选 Form；上方说明 Alert 的 `marginBottom: 16` 未改。
- `frontend/src/pages/reports/ReportList.tsx:8,306`
  - 引入并应用 `FILTER_CARD_STYLE` 到报告筛选 Form；其他非筛选区块间距未改。
- `frontend/src/pages/collection/CollectionTasks.tsx:8,249`
  - 引入并应用 `FILTER_CARD_STYLE` 到采集任务筛选 Form。
- `frontend/src/pages/research/ResearchTasks.tsx:7,97`
  - 引入并应用 `FILTER_CARD_STYLE` 到研究任务筛选 Form。
- `frontend/src/pages/scripts/ScriptList.tsx:8,65`
  - 引入并应用 `FILTER_CARD_STYLE` 到脚本筛选 Form。
- `frontend/src/pages/scripts/ScriptVersions.tsx:8,62`
  - 版本对比选择区与 Table 结构相同，应用同一 token；未改版本查询逻辑。

本轮没有改动任何 `Form.Item`、筛选字段、查询参数、分页逻辑或控件宽度；其余页面和生成文件未在本轮改动。

## 8 页布局说明

8 个页面的前后视觉差异一致：

- 修改前：筛选 Form（或版本选择区）透明无边框，仅与下方 Table 保持 24px 外边距，内容与表格视觉上贴近。
- 修改后：筛选区使用浅灰 `#fafafa` 背景、`#f0f0f0` 细边框、8px 圆角和 16px 内边距；底部仍为 24px，与 Table 形成清晰的区块分层。
- 响应式行为：仍由 antd inline Form/Space 的默认换行和间距负责；未新增固定宽度或改变控件尺寸。

逐页说明：

1. 资料列表：筛选条件集中在浅灰容器内，表格区保持原宽度和分页。
2. 选题列表：关键词、方向、专业方向等筛选项与表格分层显示。
3. 分析任务：筛选卡片与说明 Alert、任务表格保持独立层次，Alert 的 16px 内部间距保留。
4. 报告列表：报告类型/状态筛选卡片与报告表格分层，来源配置等表单逻辑不变。
5. 自动采集任务：状态、平台、时间范围筛选集中在卡片内，任务表格区不变。
6. 研究助手：状态和关键词筛选与研究任务表格分层。
7. 脚本列表：正文关键词、选题、风格、状态筛选与脚本表格分层。
8. 脚本版本历史：左右版本选择与差异按钮形成浅灰操作区，版本表格保持原有比较与回退行为。

## 验证结果

1. `npm run build`（工作目录 `frontend/`）：通过。`tsc -b` 与 Vite production build 均成功；仅有现存主 chunk 超过 500 kB 的 Vite 提示。
2. `npx tsc -b`（工作目录 `frontend/`）：通过，退出码 0。
3. `git diff --check`：通过，无空白错误。
4. 引用检查：目标 8 页均通过 `style={FILTER_CARD_STYLE}` 引用；主题 token 只有一处定义。
5. 后端边界：`git diff --name-only -- backend` 无输出，本轮未修改后端文件。
6. Git 纪律：未执行 commit、push 或部署。

## 遗留风险

- 本报告提供代码级布局说明，未在受限环境中采集 8 页运行截图；实际视觉效果仍建议在浏览器中按桌面与窄屏各检查一次。
- Vite 仍提示主 bundle 超过默认 500 kB 阈值；与本次筛选区样式无关，未扩大改动范围。
- 已尝试执行仓库要求的上传命令，但沙箱 DNS 解析失败：`curl: (6) Could not resolve host: pmlophy.com`。报告仅保存于 `check-reports/`，由杰西卡从仓库代传。
