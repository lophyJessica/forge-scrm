# Forge 新媒体运营系统全仓改名与角色词替换自检报告

## 1. 任务摘要

本次对 forge-scrm 全仓文本内容执行系统名和角色词统一：系统名统一为“Forge 新媒体运营系统”；原角色称谓按语义区分为“操盘手”（产品使用者/操作者/管理员）和“调研者”（调研对象、回复人、确认人）。扫描范围包含 context、prd-docs、draft、requirements、check-reports、.workbuddy、前端、后端及两份 DOCX 问卷内文。

未修改任何文件名；未修改代码逻辑、变量名、函数名或接口标识；未 commit、push 或部署。

## 2. 替换统计

| 类型 | 数量 |
|---|---:|
| 总文本替换 | 142 |
| 系统名替换为“Forge 新媒体运营系统” | 24 |
| 角色词替换为“操盘手” | 53 |
| 角色词替换为“调研者” | 65 |
| 修改文件 | 36 |

角色词合计 118 次；系统名替换单独统计，不重复计入角色词。

## 3. 语义判断

- 产品使用者、账号管理员、页面操作者、实际维护流程的人统一为“操盘手”，例如“操盘手登录/维护/筛选/生成/使用脚本”。
- 问卷填写人、原始回复人、决策确认人统一为“调研者”，例如“调研者回复”“调研者原话”“请调研者确认”。
- “管理员（原角色称谓）”改为“管理员（操盘手）”或直接保留“管理员”，避免把存储角色枚举改成新枚举。
- 问卷中的“谁负责审核脚本”属于业务操作者，改为“操盘手本人”；“技术人员后续确认事项无需填写”属于问卷回复人，改为“调研者无需填写”。
- 历史文件名未改名；文档正文不再展开历史文件名中的旧称谓，改用“历史文件名保留”说明。

## 4. 前端及后端可见文案

- `frontend/src/pages/Login.tsx`：登录标题改为“Forge 新媒体运营系统”。
- `frontend/src/layouts/MainLayout.tsx`：侧栏标题改为“Forge 新媒体运营系统”。
- `frontend/index.html` 与已生成的 `frontend/dist/index.html`：页面 title 改为 Forge 名称。
- `backend/app/main.py`：FastAPI title 改为“Forge 新媒体运营系统 API”。
- 权限账号登录 Demo、首页布局 Demo 同步更新展示文案。

## 5. 残留验证

- 旧系统名与旧角色词的全仓文本扫描：结果为 0。
- 二进制扫描（包含 DOCX XML 内容）：结果为 0。
- 代码标识符扫描：未发现包含旧称谓的变量名、函数名或接口标识。
- DOCX 可读性验证：两份问卷均可由 python-docx 正常读取（209 段落、46 表格）。
- `git diff --check`：通过。

## 6. 遗留风险

1. 按用户要求，历史文件名仍保留旧称谓；使用 `rg --files` 检查时仍会看到这些文件名，但文件内容和 DOCX 内文已清理。
2. `frontend/dist/` 属于已生成前端产物，本次仅同步页面 title，没有重新构建或改变业务逻辑。
3. 本次是文案和文档改名，不改变角色枚举、权限规则或业务事实；后续如新增文案，需继续使用“操盘手/调研者”语义区分。
