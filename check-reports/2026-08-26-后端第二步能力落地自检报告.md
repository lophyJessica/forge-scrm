# 后端第二步能力落地自检报告

## 一、任务与项目

- 项目：Forge 新媒体运营系统
- 仓库：`/Users/liulongfei/个人文件/forge-scrm`
- 任务：将自动采集和研究助手的 501 占位落地为真实执行能力
- 执行日期：2026-08-26
- 本步边界：采集执行器、Tavily 检索和 DeepSeek 报告生成；不改前端、模型、迁移或产品文档

## 二、基线与权威依据

- 已重新读取根目录 `AGENTS.md`。
- 已执行 `git pull origin main`，结果为 `Already up to date.`。
- 已读取自动采集主 PRD、自动采集业务规则规格、研究助手主 PRD、研究助手业务规则规格和 `context/05-术语与字段口径.md`。
- 平台官方 API、授权方式、网页抓取边界和检索供应商能力仍按文档标注为待实测；本步不伪造平台数据。

## 三、改动文件清单

### 1. `backend/app/services/collection_executor.py`

- 新增 `BenchmarkAccountAdapter` 适配器接口。
- 新增通用公开 HTTP/JSON 拉取实现，只访问账号配置的公开 `http/https` URL。
- 按 `scope_config.benchmark_account_ids` 执行账号范围校验。
- 将单账号执行写入 `collection_record` 和 `collection_result`。
- 区分记录级失败、任务级失败、部分成功和全部成功。
- 保留旧记录和旧结果，失败任务重试增加 `retry_count`，成功账号不重复生成结果。
- 对执行中的任务拒绝重复执行，返回 409。
- 平台官方 API 接入位置保留 `TODO(待实测)`，未配置公开 URL 时明确失败。

### 2. `backend/app/services/research_executor.py`

- 新增 `SearchProvider` 抽象接口和 `TavilySearchProvider` 实现。
- 从环境变量 `TAVILY_API_KEY` 读取 Tavily 凭证，未配置时返回明确错误，不写入日志或文件。
- 将 Tavily 返回的 URL、标题、摘要映射为 `research_reference` 外部 URL 引用。
- 实现研究任务状态流转：`pending → searching → organizing → success`；检索或报告生成失败进入 `failed`。
- 复用一期 `deepseek_service.chat_json` 和 `archive_raw` 生成并留档研究报告。
- 研究报告强制写入 `is_ai_product=true`，报告没有可用来源时要求显式标注引用缺失。
- 供应商选择保持可替换，DeepSeek 官方搜索等候选能力未写成固定依赖。

### 3. `backend/app/routers/phase2.py`

- 将采集任务 `execute`、`retry` 从 501 占位改为真实服务调用。
- 将研究任务 `execute`、`retry` 从 501 占位改为真实服务调用。
- 执行接口返回任务状态和进度字段；无效状态统一返回 409。

### 4. `backend/app/core/config.py`

- 新增 `tavily_api_key`、`tavily_base_url`、`tavily_timeout` 配置映射。
- `tavily_api_key` 只通过环境变量 `TAVILY_API_KEY` 或本地运行环境配置读取。

### 5. 自检报告

- 本文件保存于 `check-reports/`，未写入密钥、token、密码或数据库连接串。

## 四、测试结果

### 4.1 静态检查

执行：

```text
backend/.venv/bin/python -m compileall -q backend/app
```

结果：通过。

执行：

```text
backend/.venv/bin/alembic check
```

结果：`No new upgrade operations detected.` 本步没有新增迁移，模型结构未修改。

执行：

```text
git diff --check
```

结果：通过。

### 4.2 TestClient 状态与接口验证

使用独立内存 SQLite、依赖覆盖和模拟真实适配器响应完成进程内 HTTP 验证；未写入本地业务数据库。

| 场景 | 结果 |
|---|---:|
| 采集账号创建 | 200 |
| 采集任务创建 | 200 |
| 采集执行成功并写入记录/结果 | 200，状态 `success` |
| 采集结果列表可查询 | 200，结果数 1 |
| 成功任务重复执行 | 409 |
| 采集任务全部失败 | 200，状态 `failed`，记录含错误码和 `retryable=true` |
| 采集失败任务重试 | 200，状态 `success`，`retry_count=1` |
| 采集重试保留历史记录 | 通过，记录数由 1 增至 2 |
| 无 `TAVILY_API_KEY` 执行研究 | 400，错误明确包含 `TAVILY_API_KEY` |
| 无 key 后任务状态 | 通过，任务状态为 `failed` |
| 模拟 Tavily 检索和 DeepSeek 生成报告 | 200，状态 `success` |
| 研究报告 AI 标记 | 通过，`is_ai_product=true` |
| 研究引用落库 | 通过，包含外部 URL、标题、检索供应商和证据摘要 |
| 成功研究任务重复执行 | 409 |

### 4.3 真实网络源边界

- 本次没有调用真实 Tavily 或平台接口，避免在缺少生产 key、授权和合规确认时产生外部副作用。
- 通用采集适配器只有在账号存在公开 URL 时才会发起真实 GET 请求；没有 URL 时不会生成假数据。
- Tavily 和 DeepSeek 的 key 均未写入代码、报告或日志。

## 五、范围检查

- [x] 只修改 `backend/` 下配置、路由和服务，并新增本目录自检报告。
- [x] 未修改 `frontend/`、`context/`、`prd-docs/`、`requirements/`。
- [x] 未修改第一步模型结构或 Alembic 迁移。
- [x] 未新增 Python 依赖，复用现有 `httpx`、FastAPI、SQLAlchemy 和 DeepSeek 封装。
- [x] 未实现定时采集、平台登录绕过、验证码绕过、AI 采集摘要、推送或前端能力。
- [x] 未执行 git commit、git push、部署或删除文件。

## 六、遗留风险

1. 视频号、小红书官方 API、授权方式、字段映射、配额和历史回溯范围仍需实测；当前通用适配器只支持公开 URL/JSON。
2. 网页抓取的 robots、服务条款、访问频率、版权和数据保存边界仍需产品与合规确认。
3. Tavily 的实际配额、地域可用性、返回质量和成本尚未在当前环境验证；检索接口已保留供应商替换点。
4. 当前执行接口采用同步执行，长任务会占用请求线程；后续若接入耗时平台 API，应评估队列或 BackgroundTasks，并处理独立数据库会话。
5. 研究报告生成仍依赖 DeepSeek key；报告结构校验已覆盖基本字段，但引用与结论的细粒度事实核验仍需后续抽查能力。
6. 真实 MySQL、生产环境变量和目标机服务未在本步执行；本步不是部署验收。
7. 自检报告上传管道受当前沙箱 DNS 限制，未能完成上传。

## 七、结论

采集执行和研究检索已从 501 占位落地为可测试的真实服务边界：采集不伪造来源，按账号隔离失败并支持任务重试；研究使用可替换检索接口，Tavily 缺少 key 时明确失败，DeepSeek 报告保留 AI 标记和引用追溯。模型、迁移、前端和受保护文档未改动。
