# 推送删除规则修正自检报告

## 一、任务与项目

- 项目：Forge 新媒体运营系统（forge-scrm）
- 日期：2026-08-28
- 任务：将推送任务删除规则修正为“只有未发送任务可删除”，保留已推送任务作为发送凭证
- 已执行：`git pull origin main`，结果为 `Already up to date`
- 开始时 HEAD：`6426486`
- 改动边界：仅修改报告推送 DELETE 状态守卫和报告详情抽屉的删除按钮条件

## 二、改动文件与改动点

### `backend/app/routers/reports.py`

- 保留推送中任务不可删除规则及原错误文案。
- 增加已推送状态拦截，返回 `BizError("已推送任务为发送凭证，不可删除")`。
- 待推送、失败、已取消任务继续沿用原删除与 records 级联逻辑。
- 未修改创建、发送、取消、卡片或其他报告逻辑。

### `frontend/src/pages/reports/ReportDetail.tsx`

- `canDelete` 修正为：待推送、失败、已取消。
- 已推送任务不再显示删除按钮，操作区为空。
- 待推送、失败任务仍显示发送、取消、删除；取消规则和其余 UI 不变。

### `check-reports/推送删除规则修正-自检报告-20260828.md`

- 本自检报告。

## 三、自检结果

| 检查项 | 结果 | 说明 |
|---|---|---|
| 拉取最新代码 | 通过 | `git pull origin main` 返回 `Already up to date` |
| 前端构建 | 通过 | `npm run build` 成功，3127 个模块完成转换 |
| 后端编译 | 通过 | `backend/.venv/bin/python -m compileall -q backend/app`，退出码 0 |
| 待推送删除 | 通过 | 内存数据库测试验证可删除 |
| 失败删除 | 通过 | 内存数据库测试验证可删除 |
| 已取消删除 | 通过 | 内存数据库测试验证可删除 |
| 推送中保护 | 通过 | 返回“推送执行中，请稍后再试” |
| 已推送凭证保护 | 通过 | 返回“已推送任务为发送凭证，不可删除” |
| 测试夹具复验 | 通过 | 首次隔离测试漏建 records 表；补齐完整关系表后同组断言全部通过 |
| diff 检查 | 通过 | `git diff --check` 无错误 |
| 受保护目录 | 通过 | `context/`、`prd-docs/`、`requirements/` 无改动 |
| Git 操作边界 | 通过 | 未 commit、未 push、未 deploy |

## 四、遗留风险

1. Vite 构建仍提示主 bundle 超过 500 kB，这是现有项目级警告，本次未扩展到代码分包。
2. 本次规则修正不涉及数据库字段或枚举，因此未新增迁移。

## 五、自检报告管道

- 已按要求执行一次上传尝试。
- 上传未成功：沙箱 DNS 无法解析 `pmlophy.com`。
- `curl` 退出码为 6，错误为 `Could not resolve host`。
- 按兜底规则未再次重试；本报告已保存在 `check-reports/`。
