# 全局列表排序自检报告

## 任务/项目

- 项目：Forge 新媒体运营系统（forge-scrm）
- 任务：落实全局列表默认排序规则：创建/生成时间倒序，`id.desc()` 作为稳定兜底；报告列表使用 `generated_at`。
- 执行日期：2026-08-30
- 开工前已执行：`git pull origin main`，结果为 `Already up to date.`

## 改动文件清单

- `backend/app/routers/phase2.py`
- `backend/app/routers/materials.py`
- `backend/app/routers/analysis.py`
- `backend/app/routers/reports.py`
- `backend/app/routers/prompts.py`
- `backend/app/routers/scripts.py`
- `backend/app/routers/users.py`
- `backend/app/routers/topics.py`
- `UI-DESIGN.md`

并行工作树中已有前端文件改动（`frontend/src/...`），与本任务无关，未修改。

## order_by 前后对比

### 已修正

| 文件/路由 | 修改前 | 修改后 |
|---|---|---|
| `phase2.py` 对标账号列表 | `BenchmarkAccount.id.desc()` | `BenchmarkAccount.created_at.desc(), BenchmarkAccount.id.desc()` |
| `phase2.py` 采集任务列表 | `CollectionTask.id.desc()` | `CollectionTask.created_at.desc(), CollectionTask.id.desc()` |
| `phase2.py` 采集结果列表 | `CollectionResult.id.desc()` | `CollectionResult.created_at.desc(), CollectionResult.id.desc()` |
| `phase2.py` 研究任务列表 | `ResearchTask.id.desc()` | `ResearchTask.created_at.desc(), ResearchTask.id.desc()` |
| `phase2.py` 研究引用关系列表 | `ResearchReference.id.desc()` | `ResearchReference.created_at.desc(), ResearchReference.id.desc()` |
| `materials.py` 资料分类列表 | `MaterialClass.sort, MaterialClass.id` | `MaterialClass.created_at.desc(), MaterialClass.id.desc()` |
| `materials.py` 标签列表 | `Tag.id.desc()` | `Tag.created_at.desc(), Tag.id.desc()` |
| `materials.py` 固定组合预览中的资料查询 | `Material.id.desc()` | `Material.created_at.desc(), Material.id.desc()` |
| `materials.py` 资料列表 | `Material.id.desc()` | `Material.created_at.desc(), Material.id.desc()` |
| `analysis.py` 分析任务列表 | `AnalysisTask.id.desc()` | `AnalysisTask.created_at.desc(), AnalysisTask.id.desc()` |
| `reports.py` 报告列表 | `Report.id.desc()` | `Report.generated_at.desc(), Report.id.desc()` |
| `reports.py` 报告推送任务列表 | `ReportPushTask.id.desc()` | `ReportPushTask.created_at.desc(), ReportPushTask.id.desc()` |
| `prompts.py` 提示词模板列表 | `PromptTemplate.id.desc()` | `PromptTemplate.created_at.desc(), PromptTemplate.id.desc()` |
| `scripts.py` 脚本列表 | `Script.id.desc()` | `Script.created_at.desc(), Script.id.desc()` |
| `users.py` 账号列表 | `User.id.desc()` | `User.created_at.desc(), User.id.desc()` |
| `topics.py` 生成历史批次 | `func.min(Topic.created_at).desc()` | `func.min(Topic.created_at).desc(), func.max(Topic.id).desc()` |
| `topics.py` 选题列表 | `Topic.id.desc()` | `Topic.created_at.desc(), Topic.id.desc()` |

### 因缺少 `created_at` 保留原排序

按指令未自造字段，以下模型没有 `created_at`，对应列表仍保留 `id.desc()`，需后续确认时间字段或回写模型后再改：

| 文件/路由 | 当前排序 | 缺少字段 |
|---|---|---|
| `phase2.py` 采集记录列表 | `CollectionRecord.id.desc()` | `CollectionRecord` 只有 `requested_at`/`completed_at` |
| `analysis.py` 数据源列表 | `DataSource.id.desc()` | `DataSource` 无创建/修改时间字段 |
| `analysis.py` 原始数据列表 | `RawData.id.desc()` | `RawData` 有 `collected_at`，无 `created_at` |
| `analysis.py` 分析结果列表 | `AnalysisResult.id.desc()` | `AnalysisResult` 无时间字段 |

以上接口未使用猜测字段，也未改动其他业务逻辑。

脚本版本历史按版本号倒序，提示词内置模板按首条模板升序，均为业务语义查询而非默认列表页，未修改。

## UI-DESIGN.md

第 7 节“页面骨架”已补充：列表默认排序为创建/生成时间倒序（回退修改时间倒序），后端 `order_by` 必须落实。

## 前端核查

- 未发现列表表格配置 `sorter`、本地 `dataSource.sort` 或其他与后端默认排序冲突的本地排序逻辑。
- `Home.tsx` 的活动流和 `MainLayout.tsx` 的路径匹配排序属于非列表页业务逻辑，未修改。
- 未新增“审核/抽查”字样或入口。

## 自检结果

- `backend/.venv/bin/python -m compileall -q backend/app`：通过。
- `git diff --check`：通过。
- `backend/.venv/bin/python -m pytest -q`：未执行测试，虚拟环境未安装 `pytest`（`No module named pytest`）。
- 未执行 commit、push、部署。

## 遗留风险

1. 上述 4 个缺少 `created_at` 的模型无法按统一时间字段规则排序，当前仍以 `id.desc()` 兜底；需要产品/后端确认采用现有时间字段还是补充模型字段后再处理。
2. 报告 `generated_at` 允许为空，数据库对 NULL 的倒序位置取决于数据库排序语义；当前严格按用户指定的 `generated_at.desc(), id.desc()` 落实。
3. 未运行集成测试或真实 MySQL 查询验证，仅完成静态编译和 diff 检查。

## 报告上传

本报告已保存于 `check-reports/`，并已成功上传审核管道。服务端返回文件路径：`/var/lib/jarvis/files/ai-reports/20260829-202439-a45855-全局列表排序-自检报告-20260830.md`。
