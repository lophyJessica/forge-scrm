# 最终审计修复自检报告

## 任务与项目

- 项目：Forge 新媒体运营系统（forge-scrm）
- 任务：两天功能与 UI 审计问题的最终修复
- 日期：2026-08-28
- 分支：main
- 起始 HEAD：`fd8060b`
- 执行约束：已先执行 `git pull origin main`；未 commit、未 push、未 deploy；未执行数据库迁移。

## 逐项修复结果

| 编号 | 状态 | 对策与结果 | 主要文件 |
|---|---|---|---|
| P0-1 并发推送竞态 | 通过 | execute 使用 `UPDATE ... WHERE status IN ('待推送','失败')` 原子占位；cancel 使用条件 UPDATE；delete 使用排除推送中/已推送的条件 DELETE；rowcount 不符返回 409。 | `backend/app/routers/reports.py` |
| P0-2 推送中卡死 | 通过 | execute/cancel 识别超过 10 分钟的推送中任务，原子复位为失败并写超时记录；占位后的报告读取、消息构造和发送循环全部进入异常保护，未捕获异常转失败并写安全摘要，不返回 500。 | `backend/app/routers/reports.py` |
| P1-3 快照链断裂 | 通过 | 最终资料非空即写 `material_combo_snapshot`；模板资料在前、去重，并以 `template_combo/manual` 标明来源。 | `backend/app/services/topic_service.py` |
| P1-4 权限闸 | 通过 | 新增 `report.push`；create/execute/cancel/delete 接入 `require_permission`，管理员恒有，成员需显式授权；push-config 保持登录态权限。 | `backend/app/core/enums.py`、`backend/app/routers/reports.py` |
| P1-5 重发去重 | 通过 | 重试读取已推送记录并跳过成功接收人；兼容历史完整标识与新掩码标识；全部已成功时返回“所有接收人已推送成功”。 | `backend/app/routers/reports.py` |
| P1-6 open_id 掩码 | 通过 | Task/Record Out 均保留前 6 后 4；新增任务/记录不落完整 open_id，发送仍仅使用环境变量中的完整值；飞书响应与异常写库前递归脱敏。 | `backend/app/routers/reports.py` |
| P1-7 状态污染 | 通过 | channel/配置校验失败直接抛业务错误，不再回写待推送；行为测试确认失败任务仍保持失败。 | `backend/app/routers/reports.py` |
| P2-8 seed 幂等键 | 通过 | 按 task_type + 种子管理员 created_by + `内置%` 第一条判定；缺映射时 warning 并跳过。 | `backend/app/services/seed.py` |
| P2-9 token 脱敏窗口 | 通过 | 进程内保留当前与上一个 token，`_safe_msg` 同时替换两者。 | `backend/app/services/feishu_push.py` |
| P2-10 迁移冗余 | 通过 | 确认旧迁移错误扩展 record 枚举；新增后续迁移将推送中/已取消记录归并为失败，再收窄为已推送/失败/待推送。迁移未执行。 | `backend/alembic/versions/a7c3e5f90b21_narrow_push_record_status.py`、模型与 schema |
| UI-严重：小按钮 | 已满足 | `TopicList` 淘汰按钮当前已为 `size="small"`，未重复修改。 | `frontend/src/pages/topics/TopicList.tsx` |
| UI-严重：行内按钮 | 通过 | 脚本提交审核/标记已使用、报告生成/重试由 primary 改为 link，loading 保留。 | `ScriptList.tsx`、`ReportList.tsx` |
| UI-严重：危险确认 | 通过 | 选题淘汰、脚本驳回/废弃、分析驳回均增加 `Modal.confirm` 和对应不可逆说明。 | 三个详情页 |
| UI-建议：Table 空态 | 通过 | 新增共享 `TABLE_EMPTY`；AST 扫描出的 25 个缺失 Table 全部接入，复扫为 0 个遗漏。 | `frontend/src/components/tableEmpty.ts` 及 22 个列表/生成页面 |
| UI-建议：Drawer | 已满足 | ReportDetail 抽屉当前已为 480px，未重复修改。 | `frontend/src/pages/reports/ReportDetail.tsx` |
| UI-建议：AI Tag | 已满足 | MaterialList/MaterialReview 均为“AI 产物”，未重复修改。 | 两个资料页面 |
| UI-建议：Badge | 通过 | Home 待办计数改为 Badge；推送任务 Badge 删除 showZero，0 时自动隐藏。 | `Home.tsx`、`ReportDetail.tsx` |
| UI-建议：间距 | 通过 | Tags 12→16、Users 6→8、AnalysisTaskDetail 18→16。 | 对应三个页面 |
| P2-11 卡片折叠 | 降级通过 | 项目依赖和代码中未找到 `lark_oapi/collapsible_panel` 可验证支持，未臆造组件；按要求使用平铺降级：正文前 500 字 + 完整内容提示，并保留 28KB 二分截断。 | `backend/app/services/feishu_push.py` |
| P2-12 推送 Badge | 通过 | 仅删除待办角标的 showZero。 | `frontend/src/pages/reports/ReportDetail.tsx` |
| AGENTS/UI 标准解耦 | 通过 | 新建 UI 标准唯一来源；AGENTS 仅增加指针与摘要，不复制正文。 | `frontend/UI-DESIGN.md`、`AGENTS.md` |

## 改动文件清单

### 后端

- `backend/app/core/enums.py`：报告推送权限码。
- `backend/app/models/report.py`：独立推送记录状态枚举。
- `backend/app/schemas/report.py`：记录出参状态类型同步。
- `backend/app/routers/reports.py`：并发占位、僵尸复位、异常回收、权限、重发去重、open_id/响应脱敏。
- `backend/app/services/topic_service.py`：资料快照与来源标识。
- `backend/app/services/seed.py`：种子模板幂等键与缺映射保护。
- `backend/app/services/feishu_push.py`：双 token 脱敏与正文降级展示。
- `backend/alembic/versions/a7c3e5f90b21_narrow_push_record_status.py`：推送记录枚举收窄迁移。

### 前端与规范

- `AGENTS.md`、`frontend/UI-DESIGN.md`：UI 标准单一来源与入口。
- `frontend/src/components/tableEmpty.ts`：共享 Table 空态。
- `frontend/src/pages/Home.tsx`：待办 Badge。
- `frontend/src/pages/topics/TopicDetail.tsx`：淘汰确认。
- `frontend/src/pages/scripts/ScriptDetail.tsx`：驳回/废弃确认。
- `frontend/src/pages/analysis/AnalysisTaskDetail.tsx`：驳回确认与间距。
- `frontend/src/pages/scripts/ScriptList.tsx`、`frontend/src/pages/reports/ReportList.tsx`：行内按钮层级。
- `frontend/src/pages/reports/ReportDetail.tsx`：0 角标隐藏。
- `frontend/src/pages/materials/Tags.tsx`、`frontend/src/pages/admin/Users.tsx`：间距归档。
- 以下页面接入统一 Table 空态：`admin/PromptTemplates.tsx`、`admin/Users.tsx`、`analysis/AnalysisTasks.tsx`、`analysis/DataSources.tsx`、`analysis/PromptTemplates.tsx`、`analysis/RawData.tsx`、`collection/BenchmarkAccounts.tsx`、`collection/CollectionTasks.tsx`、`materials/MaterialClasses.tsx`、`materials/MaterialImport.tsx`、`materials/MaterialList.tsx`、`materials/MaterialReview.tsx`、`materials/Tags.tsx`、`reports/ReportList.tsx`、`research/ResearchTasks.tsx`、`scripts/ScriptGenerate.tsx`、`scripts/ScriptList.tsx`、`scripts/ScriptReview.tsx`、`scripts/ScriptVersions.tsx`、`topics/TopicBatches.tsx`、`topics/TopicGenerate.tsx`、`topics/TopicList.tsx`。
- `frontend/tsconfig.app.tsbuildinfo`：构建增量清单纳入新增 `tableEmpty.ts`。

## 自检结果

| 检查 | 结果 |
|---|---|
| `python -m compileall -q backend/app` | 通过 |
| 新迁移 `py_compile` | 通过 |
| `alembic heads` | 通过，单一 head：`a7c3e5f90b21` |
| Alembic upgrade | 按任务要求未执行，部署阶段执行 |
| 后端隔离行为脚本 | 通过：权限、并发 409、僵尸复位、状态无污染、重发去重、异常失败落库、Out 掩码、快照来源、seed 幂等、双 token 脱敏、卡片体积 |
| `npm run build` | 通过，Vite 3128 modules，2.97s |
| Table AST 扫描 | 通过，pages 下所有 Table 均定义 locale |
| `git diff --check` | 通过 |
| 受保护目录 | 通过，context/prd-docs/requirements 无改动 |
| 敏感信息扫描 | 通过，改动中未出现真实 key、secret、token 或完整 open_id |
| Git 操作 | 未 commit、未 push、未 deploy |

## 遗留风险

1. 新迁移尚未在目标 MySQL 执行；部署时需运行迁移并核对 `report_push_record.status` 枚举。
2. 项目未提供可验证的飞书卡片 2.0 折叠组件依赖，因此采用 PRD 指定的 500 字平铺降级；后续确认官方组件版本后可单独升级交互。
3. 并发测试使用本地 SQLite 事务交错验证原子 rowcount 行为，未对生产 MySQL 做压力测试。
4. 前端构建存在既有的单 chunk 超过 500kB 警告，不影响本次构建通过，未在本任务扩界做拆包。
5. 历史版本若已写入完整 open_id，本次按 P1-6 要求只保证 API 出参掩码，并保证新写记录不再落完整值；未擅自清洗历史发送凭证。
