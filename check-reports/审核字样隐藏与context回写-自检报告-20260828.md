# 审核字样隐藏与 context 回写自检报告

## 任务与项目

- 项目：forge-scrm（Forge 新媒体运营系统）
- 日期：2026-08-28
- 任务：资料人工保存默认生效、保留草稿入口、隐藏资料审核菜单，并按授权回写 `context/04-业务规则与状态机.md` 的二期默认通过口径。
- 基线：执行 `git pull origin main`，结果为 `Already up to date.`；实际 HEAD 为 `4071df9`（用户上下文中的 `78d4365` 已是其父提交）。

## 改动文件清单

1. `frontend/src/pages/materials/MaterialForm.tsx`
2. `frontend/src/layouts/MainLayout.tsx`
3. `context/04-业务规则与状态机.md`
4. `check-reports/审核字样隐藏与context回写-自检报告-20260828.md`

## 每个文件改动点

### frontend/src/pages/materials/MaterialForm.tsx

- 操作按钮收敛为主按钮“保存”和次要按钮“存为草稿”。
- 删除 `submit('review')` 的 UI 触发按钮；旧 `action === 'review'` 分支仍保留，未删除审核能力。
- 新建请求固定传 `submit_for_review: false`。
- “保存”使用现有接口依次完成草稿提交和默认通过，使人工新建资料最终进入“已生效”；草稿/待审核状态的编辑保存也按同一默认通过路径处理。
- 成功提示调整为“已存为草稿”和“保存成功，资料已生效”；非可激活的既有终态仅提示“保存成功”，避免错误声称状态变化。
- 未修改状态枚举、接口参数定义或后端状态机判断。

### frontend/src/layouts/MainLayout.tsx

- 注释隐藏 `/materials/review` 的“资料审核”菜单项。
- 路由注册、审核页面及后端审核接口均保留，便于后续重新启用。

### context/04-业务规则与状态机.md

- R1 回写为：资料使用规则：人工新建直接生效（二期默认）；导入与 AI 产物保留审核队列与 AI 产物标记，审核入口暂时隐藏，能力可重新启用（0826 拍板，依据审核流程变更说明.md，2026-08-28 回写）
- R3 回写为：选题生成后默认可用；人工筛选变为主动抽查/审核重启时使用，入口保留（0826 拍板，依据审核流程变更说明.md，2026-08-28 回写）
- R6 回写为：分析结果默认按已确认处理；人工审核变为抽查/审核重启时使用，入口保留（0826 拍板，依据审核流程变更说明.md，2026-08-28 回写）
- R8 保留“AI 产物与原始事实区分”原规则，并补充：AI 产物二期默认可用，保留标识、抽查与撤回能力（0826 拍板，依据审核流程变更说明.md，2026-08-28 回写）
- 资料、选题、分析三处 Mermaid 状态机在阻塞节点旁增加“二期默认通过，跳过阻塞”注释；原流转线未删除。
- 同步修正对应章节中与新 R1/R3/R6 直接冲突的旧规则句，未修改脚本状态机及其他全局规则。

## 全站字样自查

- 已执行 `rg -n "提交审核|资料审核|待审核" frontend/src -g '*.tsx' -g '*.ts'`。
- 本任务指定的资料新建页“提交审核”按钮和主菜单“资料审核”入口已隐藏。
- 保留命中项均属于以下范围：状态枚举/查询与状态判断、无 UI 入口的兼容分支、明确要求不修改的 `MaterialReview.tsx`，以及本任务未要求改变的脚本审核流程和分析状态展示。
- 未改动后端状态值、请求参数和状态机判断逻辑。

## 自检结果

- `git diff --check`：通过。
- `python -m compileall -q backend/app`：通过（退出码 0，无输出）。
- `npm run build`：通过；TypeScript 编译及 Vite 构建成功，3128 个模块完成转换。
- 构建提示：主 JS 产物约 1.54 MB，Vite 给出大于 500 kB 的既存 chunk 警告，不影响本次构建通过。
- 后端文件改动：无。
- `context/` 改动范围：仅 `context/04-业务规则与状态机.md`。
- commit/push/deploy：均未执行。

## 遗留风险

- 受“本轮禁止改后端”约束，人工资料默认生效通过现有 `create -> submit -> review(approved=true)` 顺序请求实现，不是后端单事务。若后两个请求之间发生网络中断，资料可能暂留草稿或待审核；用户可重新进入编辑页点击“保存”继续完成默认通过。
- 审核页面与路由按要求保留，直接输入 `/materials/review` 仍可访问，这是后续可重新启用审核能力的预留行为。
- Vite 大包体积警告为既存构建提示，本任务未扩界处理代码拆分。
