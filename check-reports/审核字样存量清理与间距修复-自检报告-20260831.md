# 审核字样存量清理与间距修复自检报告

- 项目：forge-scrm
- 任务：前端审核字样存量清理；脚本列表筛选区与表格主区块间距修复
- 日期：2026-08-31
- 范围：`frontend/src/`；未修改 `backend/`，未删除路由、组件文件或状态枚举

## 改动文件清单

审核相关 UI 清理与状态展示：

- `frontend/src/layouts/MainLayout.tsx`：移除资料/脚本相关菜单入口；路由保留。
- `frontend/src/pages/Home.tsx`：移除审核待办和统计请求，动态状态改用中性展示；保留“待筛选选题”待办。
- `frontend/src/pages/analysis/AnalysisTasks.tsx`：移除执行完成后的审核提示；状态筛选隐藏预留值，状态标签通过统一映射展示。
- `frontend/src/pages/analysis/AnalysisTaskDetail.tsx`：移除结果审核操作、审核提示和相关 Alert；预留状态映射到既有默认态。
- `frontend/src/pages/materials/MaterialForm.tsx`：移除提交审核操作分支和文案；保存流程仍按既有后端默认生效逻辑运行。
- `frontend/src/pages/materials/MaterialImport.tsx`：导入说明改为中性文案。
- `frontend/src/pages/materials/MaterialList.tsx`：移除提交审核按钮；状态筛选和状态展示隐藏/映射预留值。
- `frontend/src/pages/materials/MaterialReview.tsx`：保留路由组件，改为无审核 UI 的预留页。
- `frontend/src/pages/scripts/ScriptDetail.tsx`：移除提交、通过、驳回操作；状态展示映射到既有默认态。
- `frontend/src/pages/scripts/ScriptList.tsx`：移除提交审核按钮；状态筛选和状态展示隐藏/映射预留值。
- `frontend/src/pages/scripts/ScriptReview.tsx`：保留路由组件，改为无审核 UI 的预留页。
- `frontend/src/theme.ts`：新增 `displayStatus` 与 `visibleStatusOptions`，统一处理展示映射和筛选项过滤。

主区块间距统一为 24px：

- `frontend/src/pages/scripts/ScriptList.tsx`：筛选 Form 与 Table 间距由 16px 调整为 24px。
- `frontend/src/pages/analysis/AnalysisTasks.tsx`
- `frontend/src/pages/analysis/PromptTemplates.tsx`
- `frontend/src/pages/analysis/RawData.tsx`
- `frontend/src/pages/collection/BenchmarkAccounts.tsx`
- `frontend/src/pages/collection/CollectionTasks.tsx`
- `frontend/src/pages/materials/MaterialList.tsx`
- `frontend/src/pages/materials/Tags.tsx`
- `frontend/src/pages/reports/ReportList.tsx`
- `frontend/src/pages/research/ResearchTasks.tsx`
- `frontend/src/pages/scripts/ScriptVersions.tsx`
- `frontend/src/pages/topics/TopicList.tsx`

上述页面仅调整筛选/操作/选择区与数据区之间的间距，没有重构查询或表格逻辑。`frontend/tsconfig.app.tsbuildinfo` 与 `frontend/tsconfig.node.tsbuildinfo` 在本轮开始前已存在工作树改动，未主动回滚。

## 初始全量扫描与处理

扫描命令（以改动前 `HEAD` 为基线）：

```bash
git grep -n -E '审核|抽查|待审核|转审核' HEAD -- frontend/src
```

注释命中 `frontend/src/layouts/MainLayout.tsx:75-76` 为过期的隐藏入口说明，已一并移除。其余命中逐处处理如下：

- `frontend/src/layouts/MainLayout.tsx:102`：移除“脚本审核”菜单项；`App.tsx` 中脚本审核路由保留。
- `frontend/src/pages/Home.tsx:51,140`：分析统计副文案改为“详情查看执行状态”。
- `frontend/src/pages/Home.tsx:107-110`：删除资料/脚本/分析审核状态计数请求；保留选题“待筛选”查询。
- `frontend/src/pages/Home.tsx:146,152`：删除资料、脚本审核待办；保留选题待筛选待办。
- `frontend/src/pages/Home.tsx:171`：将“脚本已通过审核”改为“脚本已通过”。
- `frontend/src/pages/analysis/AnalysisTaskDetail.tsx:57`：执行完成提示改为“执行完成”。
- `frontend/src/pages/analysis/AnalysisTaskDetail.tsx:106`：回写成功提示改为中性文案。
- `frontend/src/pages/analysis/AnalysisTaskDetail.tsx:141`：移除结果审核操作区及权限分支。
- `frontend/src/pages/analysis/AnalysisTaskDetail.tsx:257`：回写 Alert 改为中性说明。
- `frontend/src/pages/analysis/AnalysisTasks.tsx:76`：执行完成提示改为“执行完成”。
- `frontend/src/pages/materials/MaterialForm.tsx:38`：移除前端提交审核分支，但保留草稿/既有预留状态的后端激活调用。
- `frontend/src/pages/materials/MaterialForm.tsx:86`：移除“已提交审核”反馈分支。
- `frontend/src/pages/materials/MaterialImport.tsx:24`：导入说明改为默认规则说明。
- `frontend/src/pages/materials/MaterialList.tsx:174`：移除“提交审核”按钮。
- `frontend/src/pages/materials/MaterialReview.tsx:16,35`：移除审核队列查询、标签和操作，保留无审核 UI 的路由预留页。
- `frontend/src/pages/scripts/ScriptDetail.tsx:46-50`：移除提交、通过、驳回按钮及审核调用。
- `frontend/src/pages/scripts/ScriptDetail.tsx:75`：保留废弃操作对既有预留状态的后端判断；仅移除审核相关入口。
- `frontend/src/pages/scripts/ScriptList.tsx:146-147`：移除“提交审核”按钮及提示。
- `frontend/src/pages/scripts/ScriptList.tsx:155`：保留废弃操作对既有预留状态的后端判断；仅移除审核相关入口。
- `frontend/src/pages/scripts/ScriptReview.tsx:18,37`：移除审核队列查询、标签和操作，保留无审核 UI 的路由预留页。
- `frontend/src/store/meta.ts:43`：保留 `PERM.分析结果审核` 权限常量，作为后端契约预留。
- `frontend/src/theme.ts:74`：保留 `待审核` 状态颜色映射，供后端状态契约使用；不直接用于用户可见审核文案。
- `frontend/src/types/index.ts:377`：保留 `ReportReviewStatus` 类型及其后端枚举。

## 预留层确认

- 路由保留：`frontend/src/App.tsx` 的 `/materials/review`、`/scripts/review`。
- 权限常量保留：`frontend/src/store/meta.ts` 的 `PERM.分析结果审核`。
- 类型/状态枚举保留：`frontend/src/types/index.ts` 的 `ReportReviewStatus`；`frontend/src/theme.ts` 的后端状态颜色映射。
- 后端接口调用契约未修改；本轮未改动 `backend/`。

## 验证结果

1. `npm run build`（工作目录 `frontend/`）：通过。`tsc -b` 与 Vite production build 均成功；仅有现存 bundle 大小提示（主 chunk gzip 约 481 kB），不影响构建结果。
2. `npx tsc -b`（工作目录 `frontend/`）：通过，退出码 0。
3. `git diff --check`：通过，无空白错误。
4. 复扫命令：

   ```bash
   rg -n '审核|抽查|待审核|转审核' frontend/src
   ```

   最终仅剩以下非 UI 契约命中：

   ```text
   frontend/src/store/meta.ts:43:  分析结果审核: 'analysis.result.review'
   frontend/src/pages/scripts/ScriptList.tsx:153:                  ['草稿', '待审核', '已通过'].includes(r.status) ? (
   frontend/src/pages/scripts/ScriptDetail.tsx:51:          {['草稿', '待审核', '已通过'].includes(script.status) && (
   frontend/src/pages/materials/MaterialForm.tsx:38:    if (status === '草稿' || status === '待审核') {
   frontend/src/theme.ts:74:  待审核: 'orange'
   frontend/src/theme.ts:103:  return status === '待审核' ? defaultStatus : status || '—'
   frontend/src/theme.ts:108:  return options.filter((option) => option.value !== '待审核')
   frontend/src/types/index.ts:377:export type ReportReviewStatus = '默认通过' | '抽查中' | '已确认' | '待审核' | '已废弃'
   ```

   以上均为权限常量、后端类型或展示层过滤映射，无用户可见审核文案、状态徽章、菜单入口或操作按钮。

## 遗留风险

- 复审规则要求前端隐藏审核能力，因此审核路由/组件和后端状态契约仍存在但不从导航进入；直接访问预留路由只显示中性占位页。
- 报告构建产物主 chunk 仍超过 Vite 默认 500 kB 警告阈值；本轮未做与任务无关的拆包改造。
- 已尝试执行仓库要求的上传命令，但沙箱 DNS 解析失败：`curl: (6) Could not resolve host: pmlophy.com`。报告仅保存于 `check-reports/`，未声称已上传管道；由杰西卡在 VPS 侧代传。
