# 后端二期骨架自检报告

## 一、任务与项目

- 项目：Forge 新媒体运营系统
- 仓库：`/Users/liulongfei/个人文件/forge-scrm`
- 任务：新增自动采集与研究助手后端数据模型、Alembic 迁移和 `/api/v1` CRUD 骨架
- 执行日期：2026-08-26
- 本步边界：只实现后端数据骨架；不实现采集执行、联网检索、AI 生成、推送和前端

## 二、基线与权威依据

已重新读取仓库根目录 `AGENTS.md`，并读取：

1. `prd-docs/modules/自动采集/自动采集字段清单.md`
2. `prd-docs/modules/研究助手/研究助手字段清单.md`
3. `context/05-术语与字段口径.md`
4. `prd-docs/modules/00-二期总PRD.md` 中 5.1/5.2 状态机
5. 一期模型、schema、路由、数据库和 Alembic 配置

执行前尝试 `git pull --ff-only origin main`，但沙箱禁止写入 `.git/FETCH_HEAD`，命令失败。申请审批执行同一命令时审批服务返回 503。只读核对结果：本地 `HEAD` 与 `origin/main` 为 `84aa9b2`，用户指定基线 `0751ee5` 是当前 HEAD 的祖先，工作树在本步开始前干净。未使用绕过方式替代 pull。

## 三、改动文件清单

### 1. 模型汇总

文件：`backend/app/models/__init__.py`

改动：导出七个二期模型，使 Alembic `Base.metadata` 能发现新表。

### 2. 二期数据模型

文件：`backend/app/models/phase2.py`

改动：新增四个自动采集模型：`benchmark_account`、`collection_task`、`collection_record`、`collection_result`；新增三个研究助手模型：`research_task`、`research_report`、`research_reference`。

- 采集任务状态使用 `pending`、`running`、`success`、`partial_success`、`failed`。
- 研究任务状态使用 `pending`、`searching`、`organizing`、`success`、`failed`。
- `research_report.is_ai_product` 使用数据库 check constraint 强制为 true。
- `collection_result.is_ai_product` 默认 false。
- 新增字段的 SQLAlchemy 注释中标记 `TODO(待确认)`；已确认复用语义字段保留 context/05 口径。
- 时间戳采用与一期一致的无时区 UTC 当前时间函数；`updated_at` 使用 `onupdate`。
- 仅建立字段清单所需外键、索引和唯一约束，不添加采集/检索业务逻辑。

### 3. 二期 Schema

文件：`backend/app/schemas/phase2.py`

改动：新增账号、采集任务/记录/结果、研究任务/报告/引用关系的 CRUD 输入输出模型；包含时间窗校验、分页输出适配和状态枚举输出。

### 4. 二期路由

文件：`backend/app/routers/phase2.py`

改动：注册 `/api/v1` 路由：

- 对标账号 CRUD。
- 采集任务 CRUD、记录列表、结果列表。
- 研究任务 CRUD、研究报告详情、研究引用关系列表。
- 列表接口支持 `page`/`page_size` 和状态筛选。
- 采集任务执行/重试、研究任务执行/重试只返回 501，占位函数不调用 AI、采集或检索服务。

### 5. 路由注册

文件：`backend/app/main.py`

改动：导入并注册 `phase2.router`，不改变一期路由前缀和处理逻辑。

### 6. Alembic 迁移

文件：`backend/alembic/versions/c7293fe1b190_add_phase2_collection_and_research_.py`

改动：自动生成七张新表、字段、索引、外键、唯一约束和研究报告 AI 标记 check constraint。迁移从 `a3f8c2d91e4b` 连接到本次 revision。

### 7. 自检报告

文件：`check-reports/2026-08-26-后端二期骨架自检报告.md`

改动：记录本步文件、验证结果和遗留风险。本文件不包含任何密钥、token、密码或数据库连接串。

## 四、验证结果

### 4.1 模型与字段

- Python `compileall`：通过。
- metadata 表发现：七张目标表全部存在。
- 字段集合逐表断言：通过。
- 实际 SQLite 表字段数量：
  - `benchmark_account`：12
  - `collection_task`：18
  - `collection_record`：15
  - `collection_result`：18
  - `research_task`：20
  - `research_report`：14
  - `research_reference`：15
- `research_report` AI 标记强制约束和 `collection_result` 默认 false 已进入模型/迁移。

### 4.2 Alembic

执行：

```text
.venv/bin/alembic upgrade head
```

结果：通过，从 `a3f8c2d91e4b` 升级到 `c7293fe1b190`。

重复执行 `upgrade head`：通过，无新增动作。

执行：

```text
.venv/bin/alembic check
```

结果：`No new upgrade operations detected.`

### 4.3 后端启动与路由冒烟

Uvicorn 应用启动生命周期和 FastAPI 导入成功；沙箱禁止绑定本地端口，申请审批启动命令时审批服务返回 503，因此未完成真实 socket 端口 curl。随后使用 FastAPI `TestClient` 在进程内完成等价路由冒烟：

| 检查 | 结果 |
|---|---:|
| `/api/health` | 200 |
| 未登录 `/api/v1/benchmark-accounts` | 401 |
| admin 登录 | 200 |
| 对标账号创建/详情/更新 | 200 |
| 采集任务创建/详情/状态筛选 | 200 |
| 采集任务执行占位 | 501 |
| 采集任务重试占位 | 501 |
| 研究任务创建/详情/更新/状态筛选 | 200 |
| 研究任务执行占位 | 501 |
| 研究任务重试占位 | 501 |
| 采集记录列表 | 200 |
| 采集结果列表 | 200 |
| 研究引用关系列表 | 200 |

TestClient 使用独立内存 SQLite 和依赖覆盖，不污染项目本地开发数据库。

## 五、范围检查

- [x] 只修改 `backend/` 下模型、schema、router、main 和 Alembic 迁移。
- [x] 自检报告写入 `check-reports/`。
- [x] 未修改 `frontend/`、`context/`、`prd-docs/`、`requirements/`。
- [x] 未修改一期已有模型或一期路由逻辑；仅在模型汇总和 `main.py` 做必要注册调整。
- [x] 未新增依赖；沿用现有 FastAPI、SQLAlchemy、Pydantic、Alembic 和 TestClient 依赖。
- [x] 未写入密码、token、API key 或数据库连接串。
- [x] 未实现采集、检索、AI、推送或定时执行逻辑。
- [x] 未执行 git commit、git push 或部署。

## 六、遗留风险

1. 七个新模型的“新增建议，待确认”字段已按当前字段清单实现，但最终表名、枚举、索引和字段确认后可能需要后续迁移调整。
2. 采集任务和记录的执行状态、研究报告状态和 501 占位动作尚未接入业务服务。
3. `research_report` 当前按每个研究任务最多一份报告建唯一约束；是否允许多版本报告待后续产品/技术确认。
4. `research_reference` 三类来源的条件必填校验尚未实现，仅保留字段和外键骨架；后续 API 需要补充业务校验。
5. 真实 MySQL 环境的迁移尚未执行，本次验证使用项目本地 SQLite；迁移已使用 MySQL 兼容类型和字符串枚举生成。
6. Uvicorn 真实端口冒烟受当前沙箱端口绑定和审批服务限制，已用 TestClient 完成应用内 HTTP 行为验证。
7. 自检报告上传管道是否成功需以网络环境为准，本报告不虚报上传状态。

## 七、结论

后端二期数据模型、Alembic 迁移和 CRUD API 纯骨架已完成，模型字段、迁移、路由注册和进程内冒烟验证通过。采集执行、联网检索和 AI 能力留待后续步骤。

