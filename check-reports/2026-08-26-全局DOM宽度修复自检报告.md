# 全局 DOM 宽度修复自检报告

## 任务 / 项目

- 项目：Forge 新媒体运营系统。
- 任务：排查其他页面是否存在 antd Form 宽度未生效问题，并对页面主体表单做全局修复。
- 工作区：`/Users/liulongfei/个人文件/forge-scrm`。
- 约束：仅修改 `frontend/src/`；未修改后端、`context/`、表单字段、业务逻辑；未 commit、未 push、未部署。

## DevTools 实测证据

Chrome 当前已登录线上页面的 DOM 检查结果：

### `/scripts/generate`

- bundle：`https://scrm.pmlophy.com/assets/index-DieNsg1w.js`
- `<form>` inline style：空。
- computed `width`：`1603px`。
- computed `max-width`：`none`。
- `.ant-card-body` 宽度：`1651px`。
- Card inline style：`width: 100%`。

### `/topics/generate`

- bundle：同为 `https://scrm.pmlophy.com/assets/index-DieNsg1w.js`。
- `<form>` inline style：`width: 100%; max-width: 720px`。
- computed `width`：`720px`。
- computed `max-width`：`720px`。
- Card inline style：`width: 100%`。

### CSS / 路由对照

- 未发现业务自定义 CSS 覆盖 Form 的 `max-width`。
- `App.tsx` 中两个生成路由都位于同一个 `MainLayout` 下。
- `MainLayout.tsx` 对两页共用同一个 `Content` 与 `Outlet`。
- 证据指向：脚本页线上 Form 没有收到 inline style，导致按 Card body 宽度展开。

Chrome 自动审查拒绝了后续“批量逐页导航所有线上路由”的操作，因此本报告不声称已经完成线上每一条路由的 DOM 实测；其余页面采用完整源码静态审计，并按组件用途分类。

## 全局源码审计结果

- 页面主体编辑/生成表单：资料编辑、选题编辑、脚本编辑、选题生成、脚本生成、个人密码修改。
- 列表筛选表单：资料、选题、脚本、分析、采集、研究等列表页的 inline Form，应占满筛选卡片，保持不变。
- 弹窗表单：用户、提示词、数据源、采集任务、研究任务、分析回写等表单，应占满弹窗内容，保持不变。
- 登录页：Form 受固定 380px Card 约束，保持不变。

## 修改文件

- `frontend/src/pages/materials/MaterialForm.tsx`
  - Card 显式设置 `width: 100%`。
  - Form 外增加 `width: 100%; max-width: 900px` 父容器。
  - Form 改为 `width: 100%`。
- `frontend/src/pages/topics/TopicForm.tsx`
  - Card 显式设置 `width: 100%`。
  - Form 外增加 `width: 100%; max-width: 800px` 父容器。
  - Form 改为 `width: 100%`。
- `frontend/src/pages/scripts/ScriptForm.tsx`
  - Card 显式设置 `width: 100%`。
  - Form 外增加 `width: 100%; max-width: 860px` 父容器。
  - Form 改为 `width: 100%`。

选题生成页和脚本生成页已经在当前代码中使用 `width: 100%; max-width: 720px` 外层父容器，本轮保持一致。各页面原有设计宽度未改变。

## 验证结果

- `npm run build`：通过。
- Vite 生成新 bundle：`index-tS9jCotK.js`。
- `git diff --check`：通过。
- 未发现主体页面仍把 `maxWidth` 直接依赖在 Form 上的源码模式。
- 未修改列表筛选表单、弹窗表单或业务组件行为。
- 本轮未重新启动本地 Vite；此前本地登录后的视觉验证受已有 `127.0.0.1:8000` 后端服务异常影响，未将其报告为通过。
- 报告上传：未成功。上传命令在审批阶段被拒绝，审批服务返回 `503 Service Unavailable`；按项目守则未重复尝试。

## 遗留风险

- 本次未部署，线上页面需部署新 bundle 后强刷，再复查所有主体表单的 computed width。
- Chrome 自动审查阻断了批量线上路由遍历，因此除已取得的两个线上生成页证据外，其他页面的结论来自源码审计，不冒充线上 DOM 结果。
- `frontend/tsconfig.app.tsbuildinfo` 若因构建产生缓存变化，不属于业务逻辑。
